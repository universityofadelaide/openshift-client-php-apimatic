# Getting started

## How to Build

The generated code has dependencies over external libraries like UniRest. These dependencies are defined in the ```composer.json``` file that comes with the SDK. 
To resolve these dependencies, we use the Composer package manager which requires PHP greater than 5.3.2 installed in your system. 
Visit [https://getcomposer.org/download/](https://getcomposer.org/download/) to download the installer file for Composer and run it in your system. 
Open command prompt and type ```composer --version```. This should display the current version of the Composer installed if the installation was successful.

* Using command line, navigate to the directory containing the generated files (including ```composer.json```) for the SDK. 
* Run the command ```composer install```. This should install all the required dependencies and create the ```vendor``` directory in your project directory.

![Building SDK - Step 1](https://apidocs.io/illustration/php?step=installDependencies&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

### [For Windows Users Only] Configuring CURL Certificate Path in php.ini

CURL used to include a list of accepted CAs, but no longer bundles ANY CA certs. So by default it will reject all SSL certificates as unverifiable. You will have to get your CA's cert and point curl at it. The steps are as follows:

1. Download the certificate bundle (.pem file) from [https://curl.haxx.se/docs/caextract.html](https://curl.haxx.se/docs/caextract.html) on to your system.
2. Add curl.cainfo = "PATH_TO/cacert.pem" to your php.ini file located in your php installation. “PATH_TO” must be an absolute path containing the .pem file.

```ini
[curl]
; A default value for the CURLOPT_CAINFO option. This is required to be an
; absolute path.
;curl.cainfo =
```

## How to Use

The following section explains how to use the OpenShiftAPIWithKubernetesLib library in a new project.

### 1. Open Project in an IDE

Open an IDE for PHP like PhpStorm. The basic workflow presented here is also applicable if you prefer using a different editor or IDE.

![Open project in PHPStorm - Step 1](https://apidocs.io/illustration/php?step=openIDE&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Click on ```Open``` in PhpStorm to browse to your generated SDK directory and then click ```OK```.

![Open project in PHPStorm - Step 2](https://apidocs.io/illustration/php?step=openProject0&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)     

### 2. Add a new Test Project

Create a new directory by right clicking on the solution name as shown below:

![Add a new project in PHPStorm - Step 1](https://apidocs.io/illustration/php?step=createDirectory&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Name the directory as "test"

![Add a new project in PHPStorm - Step 2](https://apidocs.io/illustration/php?step=nameDirectory&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)
   
Add a PHP file to this project

![Add a new project in PHPStorm - Step 3](https://apidocs.io/illustration/php?step=createFile&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Name it "testSDK"

![Add a new project in PHPStorm - Step 4](https://apidocs.io/illustration/php?step=nameFile&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Depending on your project setup, you might need to include composer's autoloader in your PHP code to enable auto loading of classes.

```PHP
require_once "../vendor/autoload.php";
```

It is important that the path inside require_once correctly points to the file ```autoload.php``` inside the vendor directory created during dependency installations.

![Add a new project in PHPStorm - Step 4](https://apidocs.io/illustration/php?step=projectFiles&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

After this you can add code to initialize the client library and acquire the instance of a Controller class. Sample code to initialize the client library and using controller methods is given in the subsequent sections.

### 3. Run the Test Project

To run your project you must set the Interpreter for your project. Interpreter is the PHP engine installed on your computer.

Open ```Settings``` from ```File``` menu.

![Run Test Project - Step 1](https://apidocs.io/illustration/php?step=openSettings&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Select ```PHP``` from within ```Languages & Frameworks```

![Run Test Project - Step 2](https://apidocs.io/illustration/php?step=setInterpreter0&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Browse for Interpreters near the ```Interpreter``` option and choose your interpreter.

![Run Test Project - Step 3](https://apidocs.io/illustration/php?step=setInterpreter1&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

Once the interpreter is selected, click ```OK```

![Run Test Project - Step 4](https://apidocs.io/illustration/php?step=setInterpreter2&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

To run your project, right click on your PHP file inside your Test project and click on ```Run```

![Run Test Project - Step 5](https://apidocs.io/illustration/php?step=runProject&workspaceFolder=OpenShift%20API%20%28with%20Kubernetes%29-PHP)

## How to Test

Unit tests in this SDK can be run using PHPUnit. 

1. First install the dependencies using composer including the `require-dev` dependencies.
2. Run `vendor\bin\phpunit --verbose` from commandline to execute tests. If you have 
   installed PHPUnit globally, run tests using `phpunit --verbose` instead.

You can change the PHPUnit test configuration in the `phpunit.xml` file.

## Initialization

### 

API client can be initialized as following.

```php

$client = new OpenShiftAPIWithKubernetesLib\OpenShiftAPIWithKubernetesLibClient();
```

# Class Reference

## <a name="list_of_controllers"></a>List of Controllers

* [CoreController](#core_controller)
* [CoreV1Controller](#core_v1_controller)
* [ApisController](#apis_controller)
* [AppsOpenshiftIoController](#apps_openshift_io_controller)
* [AppsOpenshiftIoV1Controller](#apps_openshift_io_v1_controller)
* [AppsController](#apps_controller)
* [AppsV1beta1Controller](#apps_v1beta1_controller)
* [AuthenticationController](#authentication_controller)
* [AuthenticationV1Controller](#authentication_v1_controller)
* [AuthenticationV1beta1Controller](#authentication_v1beta1_controller)
* [AuthorizationController](#authorization_controller)
* [AuthorizationV1Controller](#authorization_v1_controller)
* [AuthorizationV1beta1Controller](#authorization_v1beta1_controller)
* [AuthorizationOpenshiftIoController](#authorization_openshift_io_controller)
* [AuthorizationOpenshiftIoV1Controller](#authorization_openshift_io_v1_controller)
* [AutoscalingController](#autoscaling_controller)
* [AutoscalingV1Controller](#autoscaling_v1_controller)
* [AutoscalingV2alpha1Controller](#autoscaling_v2alpha1_controller)
* [BatchController](#batch_controller)
* [BatchV1Controller](#batch_v1_controller)
* [BatchV2alpha1Controller](#batch_v2alpha1_controller)
* [BuildOpenshiftIoController](#build_openshift_io_controller)
* [BuildOpenshiftIoV1Controller](#build_openshift_io_v1_controller)
* [CertificatesController](#certificates_controller)
* [CertificatesV1beta1Controller](#certificates_v1beta1_controller)
* [ExtensionsController](#extensions_controller)
* [ExtensionsV1beta1Controller](#extensions_v1beta1_controller)
* [ImageOpenshiftIoController](#image_openshift_io_controller)
* [ImageOpenshiftIoV1Controller](#image_openshift_io_v1_controller)
* [NetworkOpenshiftIoController](#network_openshift_io_controller)
* [NetworkOpenshiftIoV1Controller](#network_openshift_io_v1_controller)
* [OauthOpenshiftIoController](#oauth_openshift_io_controller)
* [OauthOpenshiftIoV1Controller](#oauth_openshift_io_v1_controller)
* [PolicyController](#policy_controller)
* [PolicyV1beta1Controller](#policy_v1beta1_controller)
* [ProjectOpenshiftIoController](#project_openshift_io_controller)
* [ProjectOpenshiftIoV1Controller](#project_openshift_io_v1_controller)
* [QuotaOpenshiftIoController](#quota_openshift_io_controller)
* [QuotaOpenshiftIoV1Controller](#quota_openshift_io_v1_controller)
* [RbacAuthorizationController](#rbac_authorization_controller)
* [RbacAuthorizationV1beta1Controller](#rbac_authorization_v1beta1_controller)
* [RouteOpenshiftIoController](#route_openshift_io_controller)
* [RouteOpenshiftIoV1Controller](#route_openshift_io_v1_controller)
* [SecurityOpenshiftIoController](#security_openshift_io_controller)
* [SecurityOpenshiftIoV1Controller](#security_openshift_io_v1_controller)
* [SettingsController](#settings_controller)
* [SettingsV1alpha1Controller](#settings_v1alpha1_controller)
* [StorageController](#storage_controller)
* [StorageV1Controller](#storage_v1_controller)
* [StorageV1beta1Controller](#storage_v1beta1_controller)
* [TemplateOpenshiftIoController](#template_openshift_io_controller)
* [TemplateOpenshiftIoV1Controller](#template_openshift_io_v1_controller)
* [APIController](#api_controller)
* [UserOpenshiftIoController](#user_openshift_io_controller)
* [UserOpenshiftIoV1Controller](#user_openshift_io_v1_controller)
* [OapiController](#oapi_controller)
* [OsapiController](#osapi_controller)
* [VersionController](#version_controller)

## <a name="core_controller"></a>![Class: ](https://apidocs.io/img/class.png ".CoreController") CoreController

### Get singleton instance

The singleton instance of the ``` CoreController ``` class can be accessed from the API Client.

```php
$core = $client->getCore();
```

### <a name="get_core_api_versions"></a>![Method: ](https://apidocs.io/img/method.png ".CoreController.getCoreAPIVersions") getCoreAPIVersions

> *Tags:*  ``` Skips Authentication ``` 

> get available API versions


```php
function getCoreAPIVersions()
```

#### Example Usage

```php

$result = $core->getCoreAPIVersions();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="core_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".CoreV1Controller") CoreV1Controller

### Get singleton instance

The singleton instance of the ``` CoreV1Controller ``` class can be accessed from the API Client.

```php
$coreV1 = $client->getCoreV1();
```

### <a name="get_core_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getCoreV1APIResources") getCoreV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getCoreV1APIResources()
```

#### Example Usage

```php

$result = $coreV1->getCoreV1APIResources();

```


### <a name="create_core_v1_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1BindingForAllNamespaces") createCoreV1BindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Binding


```php
function createCoreV1BindingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Binding();
$pretty = 'pretty';

$result = $coreV1->createCoreV1BindingForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_component_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1ComponentStatus") listCoreV1ComponentStatus

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ComponentStatus


```php
function listCoreV1ComponentStatus(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 133;
$watch = true;

$result = $coreV1->listCoreV1ComponentStatus($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_read_core_v1_component_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1ComponentStatus") getReadCoreV1ComponentStatus

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ComponentStatus


```php
function getReadCoreV1ComponentStatus(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ComponentStatus |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1ComponentStatus($name, $pretty);

```


### <a name="list_core_v1_config_map_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1ConfigMapForAllNamespaces") listCoreV1ConfigMapForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ConfigMap


```php
function listCoreV1ConfigMapForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 133;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1ConfigMapForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_config_map_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1ConfigMapForAllNamespaces") createCoreV1ConfigMapForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ConfigMap


```php
function createCoreV1ConfigMapForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ConfigMap();
$pretty = 'pretty';

$result = $coreV1->createCoreV1ConfigMapForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_endpoints_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1EndpointsForAllNamespaces") listCoreV1EndpointsForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Endpoints


```php
function listCoreV1EndpointsForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 225;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1EndpointsForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_endpoints_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1EndpointsForAllNamespaces") createCoreV1EndpointsForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create Endpoints


```php
function createCoreV1EndpointsForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Endpoints();
$pretty = 'pretty';

$result = $coreV1->createCoreV1EndpointsForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_event_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1EventForAllNamespaces") listCoreV1EventForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Event


```php
function listCoreV1EventForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 225;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1EventForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_event_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1EventForAllNamespaces") createCoreV1EventForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an Event


```php
function createCoreV1EventForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Event();
$pretty = 'pretty';

$result = $coreV1->createCoreV1EventForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_limit_range_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1LimitRangeForAllNamespaces") listCoreV1LimitRangeForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind LimitRange


```php
function listCoreV1LimitRangeForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 225;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1LimitRangeForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_limit_range_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1LimitRangeForAllNamespaces") createCoreV1LimitRangeForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LimitRange


```php
function createCoreV1LimitRangeForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LimitRange();
$pretty = 'pretty';

$result = $coreV1->createCoreV1LimitRangeForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1Namespace") listCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Namespace


```php
function listCoreV1Namespace(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 61;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1Namespace($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1Namespace") createCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> create a Namespace


```php
function createCoreV1Namespace(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$pretty = 'pretty';

$result = $coreV1->createCoreV1Namespace($body, $pretty);

```


### <a name="delete_core_v1_collection_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespace") deleteCoreV1CollectionNamespace

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Namespace


```php
function deleteCoreV1CollectionNamespace(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 61;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespace($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_binding"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedBinding") createCoreV1NamespacedBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a Binding


```php
function createCoreV1NamespacedBinding(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Binding();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedBinding($body, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedConfigMap") listCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ConfigMap


```php
function listCoreV1NamespacedConfigMap(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 61;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedConfigMap($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedConfigMap") createCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> create a ConfigMap


```php
function createCoreV1NamespacedConfigMap(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ConfigMap();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedConfigMap($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedConfigMap") deleteCoreV1CollectionNamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ConfigMap


```php
function deleteCoreV1CollectionNamespacedConfigMap(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 20;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedConfigMap($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedConfigMap") getReadCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ConfigMap


```php
function getReadCoreV1NamespacedConfigMap(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ConfigMap |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedConfigMap($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedConfigMap") updateReplaceCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ConfigMap


```php
function updateReplaceCoreV1NamespacedConfigMap(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ConfigMap |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ConfigMap();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedConfigMap($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedConfigMap") deleteCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> delete a ConfigMap


```php
function deleteCoreV1NamespacedConfigMap(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ConfigMap |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 111;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedConfigMap($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedConfigMap") patchCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ConfigMap


```php
function patchCoreV1NamespacedConfigMap(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ConfigMap |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedConfigMap($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedEndpoints") listCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Endpoints


```php
function listCoreV1NamespacedEndpoints(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 111;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedEndpoints($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedEndpoints") createCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> create Endpoints


```php
function createCoreV1NamespacedEndpoints(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Endpoints();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedEndpoints($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedEndpoints") deleteCoreV1CollectionNamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Endpoints


```php
function deleteCoreV1CollectionNamespacedEndpoints(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 203;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedEndpoints($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedEndpoints") getReadCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Endpoints


```php
function getReadCoreV1NamespacedEndpoints(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Endpoints |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedEndpoints($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedEndpoints") updateReplaceCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Endpoints


```php
function updateReplaceCoreV1NamespacedEndpoints(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Endpoints |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Endpoints();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedEndpoints($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedEndpoints") deleteCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> delete Endpoints


```php
function deleteCoreV1NamespacedEndpoints(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Endpoints |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 203;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedEndpoints($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedEndpoints") patchCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Endpoints


```php
function patchCoreV1NamespacedEndpoints(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Endpoints |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedEndpoints($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedEvent") listCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Event


```php
function listCoreV1NamespacedEvent(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 161;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedEvent($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedEvent") createCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> create an Event


```php
function createCoreV1NamespacedEvent(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Event();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedEvent($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedEvent") deleteCoreV1CollectionNamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Event


```php
function deleteCoreV1CollectionNamespacedEvent(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 161;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedEvent($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedEvent") getReadCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Event


```php
function getReadCoreV1NamespacedEvent(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Event |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedEvent($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedEvent") updateReplaceCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Event


```php
function updateReplaceCoreV1NamespacedEvent(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Event |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Event();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedEvent($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedEvent") deleteCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> delete an Event


```php
function deleteCoreV1NamespacedEvent(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Event |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 252;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedEvent($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedEvent") patchCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Event


```php
function patchCoreV1NamespacedEvent(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Event |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedEvent($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedLimitRange") listCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind LimitRange


```php
function listCoreV1NamespacedLimitRange(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 252;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedLimitRange($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedLimitRange") createCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> create a LimitRange


```php
function createCoreV1NamespacedLimitRange(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LimitRange();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedLimitRange($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedLimitRange") deleteCoreV1CollectionNamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of LimitRange


```php
function deleteCoreV1CollectionNamespacedLimitRange(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 211;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedLimitRange($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedLimitRange") getReadCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> read the specified LimitRange


```php
function getReadCoreV1NamespacedLimitRange(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the LimitRange |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedLimitRange($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedLimitRange") updateReplaceCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified LimitRange


```php
function updateReplaceCoreV1NamespacedLimitRange(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the LimitRange |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LimitRange();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedLimitRange($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedLimitRange") deleteCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> delete a LimitRange


```php
function deleteCoreV1NamespacedLimitRange(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the LimitRange |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 216;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedLimitRange($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedLimitRange") patchCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified LimitRange


```php
function patchCoreV1NamespacedLimitRange(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the LimitRange |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedLimitRange($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedPersistentVolumeClaim") listCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PersistentVolumeClaim


```php
function listCoreV1NamespacedPersistentVolumeClaim(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 216;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedPersistentVolumeClaim($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedPersistentVolumeClaim") createCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> create a PersistentVolumeClaim


```php
function createCoreV1NamespacedPersistentVolumeClaim(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolumeClaim();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedPersistentVolumeClaim($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedPersistentVolumeClaim") deleteCoreV1CollectionNamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PersistentVolumeClaim


```php
function deleteCoreV1CollectionNamespacedPersistentVolumeClaim(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 174;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedPersistentVolumeClaim($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedPersistentVolumeClaim") getReadCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PersistentVolumeClaim


```php
function getReadCoreV1NamespacedPersistentVolumeClaim(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedPersistentVolumeClaim($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedPersistentVolumeClaim") updateReplaceCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PersistentVolumeClaim


```php
function updateReplaceCoreV1NamespacedPersistentVolumeClaim(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolumeClaim();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedPersistentVolumeClaim($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedPersistentVolumeClaim") deleteCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> delete a PersistentVolumeClaim


```php
function deleteCoreV1NamespacedPersistentVolumeClaim(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 11;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedPersistentVolumeClaim($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedPersistentVolumeClaim") patchCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PersistentVolumeClaim


```php
function patchCoreV1NamespacedPersistentVolumeClaim(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedPersistentVolumeClaim($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_core_v1_namespaced_persistent_volume_claim_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedPersistentVolumeClaimStatus") getReadCoreV1NamespacedPersistentVolumeClaimStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified PersistentVolumeClaim


```php
function getReadCoreV1NamespacedPersistentVolumeClaimStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedPersistentVolumeClaimStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_core_v1_namespaced_persistent_volume_claim_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedPersistentVolumeClaimStatus") updateReplaceCoreV1NamespacedPersistentVolumeClaimStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified PersistentVolumeClaim


```php
function updateReplaceCoreV1NamespacedPersistentVolumeClaimStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolumeClaim();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedPersistentVolumeClaimStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_persistent_volume_claim_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedPersistentVolumeClaimStatus") patchCoreV1NamespacedPersistentVolumeClaimStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified PersistentVolumeClaim


```php
function patchCoreV1NamespacedPersistentVolumeClaimStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedPersistentVolumeClaimStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedPod") listCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Pod


```php
function listCoreV1NamespacedPod(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 102;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedPod($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedPod") createCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> create a Pod


```php
function createCoreV1NamespacedPod(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Pod();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedPod($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedPod") deleteCoreV1CollectionNamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Pod


```php
function deleteCoreV1CollectionNamespacedPod(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 60;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedPod($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedPod") getReadCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Pod


```php
function getReadCoreV1NamespacedPod(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedPod($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedPod") updateReplaceCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Pod


```php
function updateReplaceCoreV1NamespacedPod(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Pod();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedPod($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedPod") deleteCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> delete a Pod


```php
function deleteCoreV1NamespacedPod(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 60;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedPod($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedPod") patchCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Pod


```php
function patchCoreV1NamespacedPod(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedPod($body, $name, $mnamespace, $pretty);

```


### <a name="get_connect_core_v1_get_namespaced_pod_attach"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedPodAttach") getConnectCoreV1GetNamespacedPodAttach

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to attach of Pod


```php
function getConnectCoreV1GetNamespacedPodAttach(
        $name,
        $mnamespace,
        $container = null,
        $stderr = null,
        $stdin = null,
        $stdout = null,
        $tty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | The container in which to execute the command. Defaults to only container if there is only one container in the pod. |
| stderr |  ``` Optional ```  | Stderr if true indicates that stderr is to be redirected for the attach call. Defaults to true. |
| stdin |  ``` Optional ```  | Stdin if true, redirects the standard input stream of the pod for this call. Defaults to false. |
| stdout |  ``` Optional ```  | Stdout if true indicates that stdout is to be redirected for the attach call. Defaults to true. |
| tty |  ``` Optional ```  | TTY if true indicates that a tty will be allocated for the attach call. This is passed through the container runtime so the tty is allocated on the worker node by the container runtime. Defaults to false. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$stderr = true;
$stdin = true;
$stdout = true;
$tty = true;

$result = $coreV1->getConnectCoreV1GetNamespacedPodAttach($name, $mnamespace, $container, $stderr, $stdin, $stdout, $tty);

```


### <a name="create_connect_core_v1_post_namespaced_pod_attach"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedPodAttach") createConnectCoreV1PostNamespacedPodAttach

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to attach of Pod


```php
function createConnectCoreV1PostNamespacedPodAttach(
        $name,
        $mnamespace,
        $container = null,
        $stderr = null,
        $stdin = null,
        $stdout = null,
        $tty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | The container in which to execute the command. Defaults to only container if there is only one container in the pod. |
| stderr |  ``` Optional ```  | Stderr if true indicates that stderr is to be redirected for the attach call. Defaults to true. |
| stdin |  ``` Optional ```  | Stdin if true, redirects the standard input stream of the pod for this call. Defaults to false. |
| stdout |  ``` Optional ```  | Stdout if true indicates that stdout is to be redirected for the attach call. Defaults to true. |
| tty |  ``` Optional ```  | TTY if true indicates that a tty will be allocated for the attach call. This is passed through the container runtime so the tty is allocated on the worker node by the container runtime. Defaults to false. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$stderr = true;
$stdin = true;
$stdout = true;
$tty = true;

$result = $coreV1->createConnectCoreV1PostNamespacedPodAttach($name, $mnamespace, $container, $stderr, $stdin, $stdout, $tty);

```


### <a name="create_core_v1_namespaced_binding_binding"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedBindingBinding") createCoreV1NamespacedBindingBinding

> *Tags:*  ``` Skips Authentication ``` 

> create binding of a Binding


```php
function createCoreV1NamespacedBindingBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Binding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Binding();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedBindingBinding($body, $name, $mnamespace, $pretty);

```


### <a name="create_core_v1_namespaced_eviction_eviction"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedEvictionEviction") createCoreV1NamespacedEvictionEviction

> *Tags:*  ``` Skips Authentication ``` 

> create eviction of an Eviction


```php
function createCoreV1NamespacedEvictionEviction(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Eviction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Eviction();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedEvictionEviction($body, $name, $mnamespace, $pretty);

```


### <a name="get_connect_core_v1_get_namespaced_pod_exec"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedPodExec") getConnectCoreV1GetNamespacedPodExec

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to exec of Pod


```php
function getConnectCoreV1GetNamespacedPodExec(
        $name,
        $mnamespace,
        $command = null,
        $container = null,
        $stderr = null,
        $stdin = null,
        $stdout = null,
        $tty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| command |  ``` Optional ```  | Command is the remote command to execute. argv array. Not executed within a shell. |
| container |  ``` Optional ```  | Container in which to execute the command. Defaults to only container if there is only one container in the pod. |
| stderr |  ``` Optional ```  | Redirect the standard error stream of the pod for this call. Defaults to true. |
| stdin |  ``` Optional ```  | Redirect the standard input stream of the pod for this call. Defaults to false. |
| stdout |  ``` Optional ```  | Redirect the standard output stream of the pod for this call. Defaults to true. |
| tty |  ``` Optional ```  | TTY if true indicates that a tty will be allocated for the exec call. Defaults to false. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$command = 'command';
$container = 'container';
$stderr = true;
$stdin = true;
$stdout = true;
$tty = true;

$result = $coreV1->getConnectCoreV1GetNamespacedPodExec($name, $mnamespace, $command, $container, $stderr, $stdin, $stdout, $tty);

```


### <a name="create_connect_core_v1_post_namespaced_pod_exec"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedPodExec") createConnectCoreV1PostNamespacedPodExec

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to exec of Pod


```php
function createConnectCoreV1PostNamespacedPodExec(
        $name,
        $mnamespace,
        $command = null,
        $container = null,
        $stderr = null,
        $stdin = null,
        $stdout = null,
        $tty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| command |  ``` Optional ```  | Command is the remote command to execute. argv array. Not executed within a shell. |
| container |  ``` Optional ```  | Container in which to execute the command. Defaults to only container if there is only one container in the pod. |
| stderr |  ``` Optional ```  | Redirect the standard error stream of the pod for this call. Defaults to true. |
| stdin |  ``` Optional ```  | Redirect the standard input stream of the pod for this call. Defaults to false. |
| stdout |  ``` Optional ```  | Redirect the standard output stream of the pod for this call. Defaults to true. |
| tty |  ``` Optional ```  | TTY if true indicates that a tty will be allocated for the exec call. Defaults to false. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$command = 'command';
$container = 'container';
$stderr = false;
$stdin = false;
$stdout = false;
$tty = false;

$result = $coreV1->createConnectCoreV1PostNamespacedPodExec($name, $mnamespace, $command, $container, $stderr, $stdin, $stdout, $tty);

```


### <a name="get_read_core_v1_namespaced_pod_log"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedPodLog") getReadCoreV1NamespacedPodLog

> *Tags:*  ``` Skips Authentication ``` 

> read log of the specified Pod


```php
function getReadCoreV1NamespacedPodLog(
        $name,
        $mnamespace,
        $container = null,
        $follow = null,
        $limitBytes = null,
        $pretty = null,
        $previous = null,
        $sinceSeconds = null,
        $tailLines = null,
        $timestamps = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | The container for which to stream logs. Defaults to only container if there is one container in the pod. |
| follow |  ``` Optional ```  | Follow the log stream of the pod. Defaults to false. |
| limitBytes |  ``` Optional ```  | If set, the number of bytes to read from the server before terminating the log output. This may not display a complete final line of logging, and may return slightly more or slightly less than the specified limit. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| previous |  ``` Optional ```  | Return previous terminated container logs. Defaults to false. |
| sinceSeconds |  ``` Optional ```  | A relative time in seconds before the current time from which to show logs. If this value precedes the time a pod was started, only logs since the pod start will be returned. If this value is in the future, no logs will be returned. Only one of sinceSeconds or sinceTime may be specified. |
| tailLines |  ``` Optional ```  | If set, the number of lines from the end of the logs to show. If not specified, logs are shown from the creation of the container or sinceSeconds or sinceTime |
| timestamps |  ``` Optional ```  | If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line of log output. Defaults to false. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$follow = false;
$limitBytes = 38;
$pretty = 'pretty';
$previous = false;
$sinceSeconds = 38;
$tailLines = 38;
$timestamps = false;

$result = $coreV1->getReadCoreV1NamespacedPodLog($name, $mnamespace, $container, $follow, $limitBytes, $pretty, $previous, $sinceSeconds, $tailLines, $timestamps);

```


### <a name="get_connect_core_v1_get_namespaced_pod_portforward"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedPodPortforward") getConnectCoreV1GetNamespacedPodPortforward

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to portforward of Pod


```php
function getConnectCoreV1GetNamespacedPodPortforward(
        $name,
        $mnamespace,
        $ports = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| ports |  ``` Optional ```  | List of ports to forward Required when using WebSockets |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$ports = 38;

$result = $coreV1->getConnectCoreV1GetNamespacedPodPortforward($name, $mnamespace, $ports);

```


### <a name="create_connect_core_v1_post_namespaced_pod_portforward"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedPodPortforward") createConnectCoreV1PostNamespacedPodPortforward

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to portforward of Pod


```php
function createConnectCoreV1PostNamespacedPodPortforward(
        $name,
        $mnamespace,
        $ports = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| ports |  ``` Optional ```  | List of ports to forward Required when using WebSockets |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$ports = 38;

$result = $coreV1->createConnectCoreV1PostNamespacedPodPortforward($name, $mnamespace, $ports);

```


### <a name="get_connect_core_v1_get_namespaced_pod_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedPodProxy") getConnectCoreV1GetNamespacedPodProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to proxy of Pod


```php
function getConnectCoreV1GetNamespacedPodProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to pod. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->getConnectCoreV1GetNamespacedPodProxy($name, $mnamespace, $path);

```


### <a name="update_connect_core_v1_put_namespaced_pod_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateConnectCoreV1PutNamespacedPodProxy") updateConnectCoreV1PutNamespacedPodProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect PUT requests to proxy of Pod


```php
function updateConnectCoreV1PutNamespacedPodProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to pod. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateConnectCoreV1PutNamespacedPodProxy($name, $mnamespace, $path);

```


### <a name="create_connect_core_v1_post_namespaced_pod_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedPodProxy") createConnectCoreV1PostNamespacedPodProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to proxy of Pod


```php
function createConnectCoreV1PostNamespacedPodProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to pod. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->createConnectCoreV1PostNamespacedPodProxy($name, $mnamespace, $path);

```


### <a name="delete_connect_core_v1_delete_namespaced_pod_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteConnectCoreV1DeleteNamespacedPodProxy") deleteConnectCoreV1DeleteNamespacedPodProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect DELETE requests to proxy of Pod


```php
function deleteConnectCoreV1DeleteNamespacedPodProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to pod. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->deleteConnectCoreV1DeleteNamespacedPodProxy($name, $mnamespace, $path);

```


### <a name="get_connect_core_v1_get_namespaced_pod_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedPodProxyWithPath") getConnectCoreV1GetNamespacedPodProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to proxy of Pod


```php
function getConnectCoreV1GetNamespacedPodProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->getConnectCoreV1GetNamespacedPodProxyWithPath($name, $mnamespace, $path);

```


### <a name="update_connect_core_v1_put_namespaced_pod_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateConnectCoreV1PutNamespacedPodProxyWithPath") updateConnectCoreV1PutNamespacedPodProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect PUT requests to proxy of Pod


```php
function updateConnectCoreV1PutNamespacedPodProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateConnectCoreV1PutNamespacedPodProxyWithPath($name, $mnamespace, $path);

```


### <a name="create_connect_core_v1_post_namespaced_pod_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedPodProxyWithPath") createConnectCoreV1PostNamespacedPodProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to proxy of Pod


```php
function createConnectCoreV1PostNamespacedPodProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->createConnectCoreV1PostNamespacedPodProxyWithPath($name, $mnamespace, $path);

```


### <a name="delete_connect_core_v1_delete_namespaced_pod_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteConnectCoreV1DeleteNamespacedPodProxyWithPath") deleteConnectCoreV1DeleteNamespacedPodProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect DELETE requests to proxy of Pod


```php
function deleteConnectCoreV1DeleteNamespacedPodProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->deleteConnectCoreV1DeleteNamespacedPodProxyWithPath($name, $mnamespace, $path);

```


### <a name="get_read_core_v1_namespaced_pod_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedPodStatus") getReadCoreV1NamespacedPodStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Pod


```php
function getReadCoreV1NamespacedPodStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedPodStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_core_v1_namespaced_pod_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedPodStatus") updateReplaceCoreV1NamespacedPodStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Pod


```php
function updateReplaceCoreV1NamespacedPodStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Pod();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedPodStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_pod_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedPodStatus") patchCoreV1NamespacedPodStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Pod


```php
function patchCoreV1NamespacedPodStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedPodStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedPodTemplate") listCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodTemplate


```php
function listCoreV1NamespacedPodTemplate(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 88;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedPodTemplate($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedPodTemplate") createCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> create a PodTemplate


```php
function createCoreV1NamespacedPodTemplate(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodTemplate();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedPodTemplate($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedPodTemplate") deleteCoreV1CollectionNamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PodTemplate


```php
function deleteCoreV1CollectionNamespacedPodTemplate(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 88;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedPodTemplate($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedPodTemplate") getReadCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PodTemplate


```php
function getReadCoreV1NamespacedPodTemplate(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodTemplate |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedPodTemplate($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedPodTemplate") updateReplaceCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PodTemplate


```php
function updateReplaceCoreV1NamespacedPodTemplate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodTemplate |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodTemplate();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedPodTemplate($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedPodTemplate") deleteCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> delete a PodTemplate


```php
function deleteCoreV1NamespacedPodTemplate(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodTemplate |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 180;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedPodTemplate($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedPodTemplate") patchCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PodTemplate


```php
function patchCoreV1NamespacedPodTemplate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodTemplate |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedPodTemplate($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedReplicationController") listCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ReplicationController


```php
function listCoreV1NamespacedReplicationController(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 180;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedReplicationController($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedReplicationController") createCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> create a ReplicationController


```php
function createCoreV1NamespacedReplicationController(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ReplicationController();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedReplicationController($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedReplicationController") deleteCoreV1CollectionNamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ReplicationController


```php
function deleteCoreV1CollectionNamespacedReplicationController(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 138;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedReplicationController($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedReplicationController") getReadCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ReplicationController


```php
function getReadCoreV1NamespacedReplicationController(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedReplicationController($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedReplicationController") updateReplaceCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ReplicationController


```php
function updateReplaceCoreV1NamespacedReplicationController(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ReplicationController();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedReplicationController($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedReplicationController") deleteCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> delete a ReplicationController


```php
function deleteCoreV1NamespacedReplicationController(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 138;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedReplicationController($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedReplicationController") patchCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ReplicationController


```php
function patchCoreV1NamespacedReplicationController(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedReplicationController($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_core_v1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedScaleScale") getReadCoreV1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadCoreV1NamespacedScaleScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedScaleScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_core_v1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedScaleScale") updateReplaceCoreV1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceCoreV1NamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedScaleScale") patchCoreV1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchCoreV1NamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_core_v1_namespaced_replication_controller_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedReplicationControllerStatus") getReadCoreV1NamespacedReplicationControllerStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ReplicationController


```php
function getReadCoreV1NamespacedReplicationControllerStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedReplicationControllerStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_core_v1_namespaced_replication_controller_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedReplicationControllerStatus") updateReplaceCoreV1NamespacedReplicationControllerStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ReplicationController


```php
function updateReplaceCoreV1NamespacedReplicationControllerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ReplicationController();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedReplicationControllerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_replication_controller_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedReplicationControllerStatus") patchCoreV1NamespacedReplicationControllerStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ReplicationController


```php
function patchCoreV1NamespacedReplicationControllerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedReplicationControllerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedResourceQuota") listCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ResourceQuota


```php
function listCoreV1NamespacedResourceQuota(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 188;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedResourceQuota($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedResourceQuota") createCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> create a ResourceQuota


```php
function createCoreV1NamespacedResourceQuota(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ResourceQuota();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedResourceQuota($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedResourceQuota") deleteCoreV1CollectionNamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ResourceQuota


```php
function deleteCoreV1CollectionNamespacedResourceQuota(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 188;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedResourceQuota($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedResourceQuota") getReadCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ResourceQuota


```php
function getReadCoreV1NamespacedResourceQuota(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedResourceQuota($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedResourceQuota") updateReplaceCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ResourceQuota


```php
function updateReplaceCoreV1NamespacedResourceQuota(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ResourceQuota();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedResourceQuota($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedResourceQuota") deleteCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> delete a ResourceQuota


```php
function deleteCoreV1NamespacedResourceQuota(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 24;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedResourceQuota($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedResourceQuota") patchCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ResourceQuota


```php
function patchCoreV1NamespacedResourceQuota(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedResourceQuota($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_core_v1_namespaced_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedResourceQuotaStatus") getReadCoreV1NamespacedResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ResourceQuota


```php
function getReadCoreV1NamespacedResourceQuotaStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedResourceQuotaStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_core_v1_namespaced_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedResourceQuotaStatus") updateReplaceCoreV1NamespacedResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ResourceQuota


```php
function updateReplaceCoreV1NamespacedResourceQuotaStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ResourceQuota();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedResourceQuotaStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedResourceQuotaStatus") patchCoreV1NamespacedResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ResourceQuota


```php
function patchCoreV1NamespacedResourceQuotaStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedResourceQuotaStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedSecret") listCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Secret


```php
function listCoreV1NamespacedSecret(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 24;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedSecret($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedSecret") createCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> create a Secret


```php
function createCoreV1NamespacedSecret(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Secret();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedSecret($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedSecret") deleteCoreV1CollectionNamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Secret


```php
function deleteCoreV1CollectionNamespacedSecret(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 116;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedSecret($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedSecret") getReadCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Secret


```php
function getReadCoreV1NamespacedSecret(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Secret |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedSecret($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedSecret") updateReplaceCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Secret


```php
function updateReplaceCoreV1NamespacedSecret(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Secret |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Secret();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedSecret($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedSecret") deleteCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> delete a Secret


```php
function deleteCoreV1NamespacedSecret(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Secret |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 74;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedSecret($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedSecret") patchCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Secret


```php
function patchCoreV1NamespacedSecret(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Secret |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedSecret($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedServiceAccount") listCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ServiceAccount


```php
function listCoreV1NamespacedServiceAccount(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 74;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedServiceAccount($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedServiceAccount") createCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> create a ServiceAccount


```php
function createCoreV1NamespacedServiceAccount(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ServiceAccount();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedServiceAccount($body, $mnamespace, $pretty);

```


### <a name="delete_core_v1_collection_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNamespacedServiceAccount") deleteCoreV1CollectionNamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ServiceAccount


```php
function deleteCoreV1CollectionNamespacedServiceAccount(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 74;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNamespacedServiceAccount($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedServiceAccount") getReadCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ServiceAccount


```php
function getReadCoreV1NamespacedServiceAccount(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ServiceAccount |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedServiceAccount($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedServiceAccount") updateReplaceCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ServiceAccount


```php
function updateReplaceCoreV1NamespacedServiceAccount(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ServiceAccount |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ServiceAccount();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedServiceAccount($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedServiceAccount") deleteCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> delete a ServiceAccount


```php
function deleteCoreV1NamespacedServiceAccount(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ServiceAccount |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 165;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedServiceAccount($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedServiceAccount") patchCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ServiceAccount


```php
function patchCoreV1NamespacedServiceAccount(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ServiceAccount |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedServiceAccount($body, $name, $mnamespace, $pretty);

```


### <a name="list_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1NamespacedService") listCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Service


```php
function listCoreV1NamespacedService(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 2;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1NamespacedService($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1NamespacedService") createCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> create a Service


```php
function createCoreV1NamespacedService(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Service();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->createCoreV1NamespacedService($body, $mnamespace, $pretty);

```


### <a name="get_read_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedService") getReadCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Service


```php
function getReadCoreV1NamespacedService(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedService($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedService") updateReplaceCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Service


```php
function updateReplaceCoreV1NamespacedService(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Service();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedService($body, $name, $mnamespace, $pretty);

```


### <a name="delete_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1NamespacedService") deleteCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> delete a Service


```php
function deleteCoreV1NamespacedService(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1NamespacedService($name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedService") patchCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Service


```php
function patchCoreV1NamespacedService(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedService($body, $name, $mnamespace, $pretty);

```


### <a name="get_connect_core_v1_get_namespaced_service_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedServiceProxy") getConnectCoreV1GetNamespacedServiceProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to proxy of Service


```php
function getConnectCoreV1GetNamespacedServiceProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->getConnectCoreV1GetNamespacedServiceProxy($name, $mnamespace, $path);

```


### <a name="update_connect_core_v1_put_namespaced_service_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateConnectCoreV1PutNamespacedServiceProxy") updateConnectCoreV1PutNamespacedServiceProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect PUT requests to proxy of Service


```php
function updateConnectCoreV1PutNamespacedServiceProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateConnectCoreV1PutNamespacedServiceProxy($name, $mnamespace, $path);

```


### <a name="create_connect_core_v1_post_namespaced_service_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedServiceProxy") createConnectCoreV1PostNamespacedServiceProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to proxy of Service


```php
function createConnectCoreV1PostNamespacedServiceProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->createConnectCoreV1PostNamespacedServiceProxy($name, $mnamespace, $path);

```


### <a name="delete_connect_core_v1_delete_namespaced_service_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteConnectCoreV1DeleteNamespacedServiceProxy") deleteConnectCoreV1DeleteNamespacedServiceProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect DELETE requests to proxy of Service


```php
function deleteConnectCoreV1DeleteNamespacedServiceProxy(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->deleteConnectCoreV1DeleteNamespacedServiceProxy($name, $mnamespace, $path);

```


### <a name="get_connect_core_v1_get_namespaced_service_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNamespacedServiceProxyWithPath") getConnectCoreV1GetNamespacedServiceProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to proxy of Service


```php
function getConnectCoreV1GetNamespacedServiceProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->getConnectCoreV1GetNamespacedServiceProxyWithPath($name, $mnamespace, $path);

```


### <a name="update_connect_core_v1_put_namespaced_service_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateConnectCoreV1PutNamespacedServiceProxyWithPath") updateConnectCoreV1PutNamespacedServiceProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect PUT requests to proxy of Service


```php
function updateConnectCoreV1PutNamespacedServiceProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateConnectCoreV1PutNamespacedServiceProxyWithPath($name, $mnamespace, $path);

```


### <a name="create_connect_core_v1_post_namespaced_service_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNamespacedServiceProxyWithPath") createConnectCoreV1PostNamespacedServiceProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to proxy of Service


```php
function createConnectCoreV1PostNamespacedServiceProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->createConnectCoreV1PostNamespacedServiceProxyWithPath($name, $mnamespace, $path);

```


### <a name="delete_connect_core_v1_delete_namespaced_service_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteConnectCoreV1DeleteNamespacedServiceProxyWithPath") deleteConnectCoreV1DeleteNamespacedServiceProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect DELETE requests to proxy of Service


```php
function deleteConnectCoreV1DeleteNamespacedServiceProxyWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->deleteConnectCoreV1DeleteNamespacedServiceProxyWithPath($name, $mnamespace, $path);

```


### <a name="get_read_core_v1_namespaced_service_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespacedServiceStatus") getReadCoreV1NamespacedServiceStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Service


```php
function getReadCoreV1NamespacedServiceStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespacedServiceStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_core_v1_namespaced_service_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespacedServiceStatus") updateReplaceCoreV1NamespacedServiceStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Service


```php
function updateReplaceCoreV1NamespacedServiceStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Service();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespacedServiceStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_core_v1_namespaced_service_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespacedServiceStatus") patchCoreV1NamespacedServiceStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Service


```php
function patchCoreV1NamespacedServiceStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespacedServiceStatus($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1Namespace") getReadCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Namespace


```php
function getReadCoreV1Namespace(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Namespace |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1Namespace($name, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1Namespace") updateReplaceCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Namespace


```php
function updateReplaceCoreV1Namespace(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Namespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1Namespace($body, $name, $pretty);

```


### <a name="delete_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1Namespace") deleteCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> delete a Namespace


```php
function deleteCoreV1Namespace(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Namespace |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 10;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1Namespace($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1Namespace") patchCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Namespace


```php
function patchCoreV1Namespace(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Namespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1Namespace($body, $name, $pretty);

```


### <a name="update_replace_core_v1_namespace_finalize"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespaceFinalize") updateReplaceCoreV1NamespaceFinalize

> *Tags:*  ``` Skips Authentication ``` 

> replace finalize of the specified Namespace


```php
function updateReplaceCoreV1NamespaceFinalize(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Namespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespaceFinalize($body, $name, $pretty);

```


### <a name="get_read_core_v1_namespace_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NamespaceStatus") getReadCoreV1NamespaceStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Namespace


```php
function getReadCoreV1NamespaceStatus(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Namespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NamespaceStatus($name, $pretty);

```


### <a name="update_replace_core_v1_namespace_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NamespaceStatus") updateReplaceCoreV1NamespaceStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Namespace


```php
function updateReplaceCoreV1NamespaceStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Namespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NamespaceStatus($body, $name, $pretty);

```


### <a name="patch_core_v1_namespace_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NamespaceStatus") patchCoreV1NamespaceStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Namespace


```php
function patchCoreV1NamespaceStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Namespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NamespaceStatus($body, $name, $pretty);

```


### <a name="list_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1Node") listCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Node


```php
function listCoreV1Node(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 101;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1Node($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1Node") createCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> create a Node


```php
function createCoreV1Node(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Node();
$pretty = 'pretty';

$result = $coreV1->createCoreV1Node($body, $pretty);

```


### <a name="delete_core_v1_collection_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionNode") deleteCoreV1CollectionNode

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Node


```php
function deleteCoreV1CollectionNode(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 101;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionNode($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1Node") getReadCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Node


```php
function getReadCoreV1Node(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1Node($name, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1Node") updateReplaceCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Node


```php
function updateReplaceCoreV1Node(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Node |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Node();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1Node($body, $name, $pretty);

```


### <a name="delete_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1Node") deleteCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> delete a Node


```php
function deleteCoreV1Node(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Node |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 193;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1Node($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1Node") patchCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Node


```php
function patchCoreV1Node(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Node |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1Node($body, $name, $pretty);

```


### <a name="get_connect_core_v1_get_node_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNodeProxy") getConnectCoreV1GetNodeProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to proxy of Node


```php
function getConnectCoreV1GetNodeProxy(
        $name,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to node. |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->getConnectCoreV1GetNodeProxy($name, $path);

```


### <a name="update_connect_core_v1_put_node_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateConnectCoreV1PutNodeProxy") updateConnectCoreV1PutNodeProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect PUT requests to proxy of Node


```php
function updateConnectCoreV1PutNodeProxy(
        $name,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to node. |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->updateConnectCoreV1PutNodeProxy($name, $path);

```


### <a name="create_connect_core_v1_post_node_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNodeProxy") createConnectCoreV1PostNodeProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to proxy of Node


```php
function createConnectCoreV1PostNodeProxy(
        $name,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to node. |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->createConnectCoreV1PostNodeProxy($name, $path);

```


### <a name="delete_connect_core_v1_delete_node_proxy"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteConnectCoreV1DeleteNodeProxy") deleteConnectCoreV1DeleteNodeProxy

> *Tags:*  ``` Skips Authentication ``` 

> connect DELETE requests to proxy of Node


```php
function deleteConnectCoreV1DeleteNodeProxy(
        $name,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to node. |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->deleteConnectCoreV1DeleteNodeProxy($name, $path);

```


### <a name="get_connect_core_v1_get_node_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getConnectCoreV1GetNodeProxyWithPath") getConnectCoreV1GetNodeProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect GET requests to proxy of Node


```php
function getConnectCoreV1GetNodeProxyWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->getConnectCoreV1GetNodeProxyWithPath($name, $path);

```


### <a name="update_connect_core_v1_put_node_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateConnectCoreV1PutNodeProxyWithPath") updateConnectCoreV1PutNodeProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect PUT requests to proxy of Node


```php
function updateConnectCoreV1PutNodeProxyWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->updateConnectCoreV1PutNodeProxyWithPath($name, $path);

```


### <a name="create_connect_core_v1_post_node_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createConnectCoreV1PostNodeProxyWithPath") createConnectCoreV1PostNodeProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to proxy of Node


```php
function createConnectCoreV1PostNodeProxyWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->createConnectCoreV1PostNodeProxyWithPath($name, $path);

```


### <a name="delete_connect_core_v1_delete_node_proxy_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteConnectCoreV1DeleteNodeProxyWithPath") deleteConnectCoreV1DeleteNodeProxyWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect DELETE requests to proxy of Node


```php
function deleteConnectCoreV1DeleteNodeProxyWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->deleteConnectCoreV1DeleteNodeProxyWithPath($name, $path);

```


### <a name="get_read_core_v1_node_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1NodeStatus") getReadCoreV1NodeStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Node


```php
function getReadCoreV1NodeStatus(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1NodeStatus($name, $pretty);

```


### <a name="update_replace_core_v1_node_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1NodeStatus") updateReplaceCoreV1NodeStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Node


```php
function updateReplaceCoreV1NodeStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Node |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Node();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1NodeStatus($body, $name, $pretty);

```


### <a name="patch_core_v1_node_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1NodeStatus") patchCoreV1NodeStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Node


```php
function patchCoreV1NodeStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Node |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1NodeStatus($body, $name, $pretty);

```


### <a name="list_core_v1_persistent_volume_claim_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1PersistentVolumeClaimForAllNamespaces") listCoreV1PersistentVolumeClaimForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PersistentVolumeClaim


```php
function listCoreV1PersistentVolumeClaimForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 151;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1PersistentVolumeClaimForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_persistent_volume_claim_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1PersistentVolumeClaimForAllNamespaces") createCoreV1PersistentVolumeClaimForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PersistentVolumeClaim


```php
function createCoreV1PersistentVolumeClaimForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolumeClaim();
$pretty = 'pretty';

$result = $coreV1->createCoreV1PersistentVolumeClaimForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1PersistentVolume") listCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PersistentVolume


```php
function listCoreV1PersistentVolume(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 243;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1PersistentVolume($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1PersistentVolume") createCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> create a PersistentVolume


```php
function createCoreV1PersistentVolume(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolume();
$pretty = 'pretty';

$result = $coreV1->createCoreV1PersistentVolume($body, $pretty);

```


### <a name="delete_core_v1_collection_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionPersistentVolume") deleteCoreV1CollectionPersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PersistentVolume


```php
function deleteCoreV1CollectionPersistentVolume(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 243;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionPersistentVolume($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1PersistentVolume") getReadCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PersistentVolume


```php
function getReadCoreV1PersistentVolume(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PersistentVolume |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1PersistentVolume($name, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1PersistentVolume") updateReplaceCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PersistentVolume


```php
function updateReplaceCoreV1PersistentVolume(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolume |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolume();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1PersistentVolume($body, $name, $pretty);

```


### <a name="delete_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1PersistentVolume") deleteCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> delete a PersistentVolume


```php
function deleteCoreV1PersistentVolume(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolume |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 243;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1PersistentVolume($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1PersistentVolume") patchCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PersistentVolume


```php
function patchCoreV1PersistentVolume(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolume |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1PersistentVolume($body, $name, $pretty);

```


### <a name="get_read_core_v1_persistent_volume_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1PersistentVolumeStatus") getReadCoreV1PersistentVolumeStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified PersistentVolume


```php
function getReadCoreV1PersistentVolumeStatus(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PersistentVolume |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1PersistentVolumeStatus($name, $pretty);

```


### <a name="update_replace_core_v1_persistent_volume_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1PersistentVolumeStatus") updateReplaceCoreV1PersistentVolumeStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified PersistentVolume


```php
function updateReplaceCoreV1PersistentVolumeStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolume |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PersistentVolume();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1PersistentVolumeStatus($body, $name, $pretty);

```


### <a name="patch_core_v1_persistent_volume_status"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1PersistentVolumeStatus") patchCoreV1PersistentVolumeStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified PersistentVolume


```php
function patchCoreV1PersistentVolumeStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PersistentVolume |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1PersistentVolumeStatus($body, $name, $pretty);

```


### <a name="list_core_v1_pod_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1PodForAllNamespaces") listCoreV1PodForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Pod


```php
function listCoreV1PodForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 79;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1PodForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_pod_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1PodForAllNamespaces") createCoreV1PodForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Pod


```php
function createCoreV1PodForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Pod();
$pretty = 'pretty';

$result = $coreV1->createCoreV1PodForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_pod_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1PodTemplateForAllNamespaces") listCoreV1PodTemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodTemplate


```php
function listCoreV1PodTemplateForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 37;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1PodTemplateForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_pod_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1PodTemplateForAllNamespaces") createCoreV1PodTemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodTemplate


```php
function createCoreV1PodTemplateForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodTemplate();
$pretty = 'pretty';

$result = $coreV1->createCoreV1PodTemplateForAllNamespaces($body, $pretty);

```


### <a name="get_proxy_core_v1_get_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getProxyCoreV1GETNamespacedPod") getProxyCoreV1GETNamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> proxy GET requests to Pod


```php
function getProxyCoreV1GETNamespacedPod(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->getProxyCoreV1GETNamespacedPod($name, $mnamespace);

```


### <a name="update_proxy_core_v1_put_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PUTNamespacedPod") updateProxyCoreV1PUTNamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> proxy PUT requests to Pod


```php
function updateProxyCoreV1PUTNamespacedPod(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->updateProxyCoreV1PUTNamespacedPod($name, $mnamespace);

```


### <a name="create_proxy_core_v1_post_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createProxyCoreV1POSTNamespacedPod") createProxyCoreV1POSTNamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> proxy POST requests to Pod


```php
function createProxyCoreV1POSTNamespacedPod(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->createProxyCoreV1POSTNamespacedPod($name, $mnamespace);

```


### <a name="delete_proxy_core_v1_delete_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteProxyCoreV1DELETENamespacedPod") deleteProxyCoreV1DELETENamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> proxy DELETE requests to Pod


```php
function deleteProxyCoreV1DELETENamespacedPod(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->deleteProxyCoreV1DELETENamespacedPod($name, $mnamespace);

```


### <a name="update_proxy_core_v1_patch_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PATCHNamespacedPod") updateProxyCoreV1PATCHNamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> proxy PATCH requests to Pod


```php
function updateProxyCoreV1PATCHNamespacedPod(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->updateProxyCoreV1PATCHNamespacedPod($name, $mnamespace);

```


### <a name="get_proxy_core_v1_get_namespaced_pod_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getProxyCoreV1GETNamespacedPodWithPath") getProxyCoreV1GETNamespacedPodWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy GET requests to Pod


```php
function getProxyCoreV1GETNamespacedPodWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->getProxyCoreV1GETNamespacedPodWithPath($name, $mnamespace, $path);

```


### <a name="update_proxy_core_v1_put_namespaced_pod_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PUTNamespacedPodWithPath") updateProxyCoreV1PUTNamespacedPodWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy PUT requests to Pod


```php
function updateProxyCoreV1PUTNamespacedPodWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateProxyCoreV1PUTNamespacedPodWithPath($name, $mnamespace, $path);

```


### <a name="create_proxy_core_v1_post_namespaced_pod_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createProxyCoreV1POSTNamespacedPodWithPath") createProxyCoreV1POSTNamespacedPodWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy POST requests to Pod


```php
function createProxyCoreV1POSTNamespacedPodWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->createProxyCoreV1POSTNamespacedPodWithPath($name, $mnamespace, $path);

```


### <a name="delete_proxy_core_v1_delete_namespaced_pod_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteProxyCoreV1DELETENamespacedPodWithPath") deleteProxyCoreV1DELETENamespacedPodWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy DELETE requests to Pod


```php
function deleteProxyCoreV1DELETENamespacedPodWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->deleteProxyCoreV1DELETENamespacedPodWithPath($name, $mnamespace, $path);

```


### <a name="update_proxy_core_v1_patch_namespaced_pod_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PATCHNamespacedPodWithPath") updateProxyCoreV1PATCHNamespacedPodWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy PATCH requests to Pod


```php
function updateProxyCoreV1PATCHNamespacedPodWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateProxyCoreV1PATCHNamespacedPodWithPath($name, $mnamespace, $path);

```


### <a name="get_proxy_core_v1_get_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getProxyCoreV1GETNamespacedService") getProxyCoreV1GETNamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> proxy GET requests to Service


```php
function getProxyCoreV1GETNamespacedService(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->getProxyCoreV1GETNamespacedService($name, $mnamespace);

```


### <a name="update_proxy_core_v1_put_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PUTNamespacedService") updateProxyCoreV1PUTNamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> proxy PUT requests to Service


```php
function updateProxyCoreV1PUTNamespacedService(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->updateProxyCoreV1PUTNamespacedService($name, $mnamespace);

```


### <a name="create_proxy_core_v1_post_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createProxyCoreV1POSTNamespacedService") createProxyCoreV1POSTNamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> proxy POST requests to Service


```php
function createProxyCoreV1POSTNamespacedService(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->createProxyCoreV1POSTNamespacedService($name, $mnamespace);

```


### <a name="delete_proxy_core_v1_delete_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteProxyCoreV1DELETENamespacedService") deleteProxyCoreV1DELETENamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> proxy DELETE requests to Service


```php
function deleteProxyCoreV1DELETENamespacedService(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->deleteProxyCoreV1DELETENamespacedService($name, $mnamespace);

```


### <a name="update_proxy_core_v1_patch_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PATCHNamespacedService") updateProxyCoreV1PATCHNamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> proxy PATCH requests to Service


```php
function updateProxyCoreV1PATCHNamespacedService(
        $name,
        $mnamespace)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';

$result = $coreV1->updateProxyCoreV1PATCHNamespacedService($name, $mnamespace);

```


### <a name="get_proxy_core_v1_get_namespaced_service_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getProxyCoreV1GETNamespacedServiceWithPath") getProxyCoreV1GETNamespacedServiceWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy GET requests to Service


```php
function getProxyCoreV1GETNamespacedServiceWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->getProxyCoreV1GETNamespacedServiceWithPath($name, $mnamespace, $path);

```


### <a name="update_proxy_core_v1_put_namespaced_service_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PUTNamespacedServiceWithPath") updateProxyCoreV1PUTNamespacedServiceWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy PUT requests to Service


```php
function updateProxyCoreV1PUTNamespacedServiceWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateProxyCoreV1PUTNamespacedServiceWithPath($name, $mnamespace, $path);

```


### <a name="create_proxy_core_v1_post_namespaced_service_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createProxyCoreV1POSTNamespacedServiceWithPath") createProxyCoreV1POSTNamespacedServiceWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy POST requests to Service


```php
function createProxyCoreV1POSTNamespacedServiceWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->createProxyCoreV1POSTNamespacedServiceWithPath($name, $mnamespace, $path);

```


### <a name="delete_proxy_core_v1_delete_namespaced_service_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteProxyCoreV1DELETENamespacedServiceWithPath") deleteProxyCoreV1DELETENamespacedServiceWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy DELETE requests to Service


```php
function deleteProxyCoreV1DELETENamespacedServiceWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->deleteProxyCoreV1DELETENamespacedServiceWithPath($name, $mnamespace, $path);

```


### <a name="update_proxy_core_v1_patch_namespaced_service_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PATCHNamespacedServiceWithPath") updateProxyCoreV1PATCHNamespacedServiceWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy PATCH requests to Service


```php
function updateProxyCoreV1PATCHNamespacedServiceWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $coreV1->updateProxyCoreV1PATCHNamespacedServiceWithPath($name, $mnamespace, $path);

```


### <a name="get_proxy_core_v1_get_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getProxyCoreV1GETNode") getProxyCoreV1GETNode

> *Tags:*  ``` Skips Authentication ``` 

> proxy GET requests to Node


```php
function getProxyCoreV1GETNode($name)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |



#### Example Usage

```php
$name = 'name';

$result = $coreV1->getProxyCoreV1GETNode($name);

```


### <a name="update_proxy_core_v1_put_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PUTNode") updateProxyCoreV1PUTNode

> *Tags:*  ``` Skips Authentication ``` 

> proxy PUT requests to Node


```php
function updateProxyCoreV1PUTNode($name)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |



#### Example Usage

```php
$name = 'name';

$result = $coreV1->updateProxyCoreV1PUTNode($name);

```


### <a name="create_proxy_core_v1_post_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createProxyCoreV1POSTNode") createProxyCoreV1POSTNode

> *Tags:*  ``` Skips Authentication ``` 

> proxy POST requests to Node


```php
function createProxyCoreV1POSTNode($name)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |



#### Example Usage

```php
$name = 'name';

$result = $coreV1->createProxyCoreV1POSTNode($name);

```


### <a name="delete_proxy_core_v1_delete_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteProxyCoreV1DELETENode") deleteProxyCoreV1DELETENode

> *Tags:*  ``` Skips Authentication ``` 

> proxy DELETE requests to Node


```php
function deleteProxyCoreV1DELETENode($name)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |



#### Example Usage

```php
$name = 'name';

$result = $coreV1->deleteProxyCoreV1DELETENode($name);

```


### <a name="update_proxy_core_v1_patch_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PATCHNode") updateProxyCoreV1PATCHNode

> *Tags:*  ``` Skips Authentication ``` 

> proxy PATCH requests to Node


```php
function updateProxyCoreV1PATCHNode($name)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |



#### Example Usage

```php
$name = 'name';

$result = $coreV1->updateProxyCoreV1PATCHNode($name);

```


### <a name="get_proxy_core_v1_get_node_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getProxyCoreV1GETNodeWithPath") getProxyCoreV1GETNodeWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy GET requests to Node


```php
function getProxyCoreV1GETNodeWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->getProxyCoreV1GETNodeWithPath($name, $path);

```


### <a name="update_proxy_core_v1_put_node_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PUTNodeWithPath") updateProxyCoreV1PUTNodeWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy PUT requests to Node


```php
function updateProxyCoreV1PUTNodeWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->updateProxyCoreV1PUTNodeWithPath($name, $path);

```


### <a name="create_proxy_core_v1_post_node_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createProxyCoreV1POSTNodeWithPath") createProxyCoreV1POSTNodeWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy POST requests to Node


```php
function createProxyCoreV1POSTNodeWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->createProxyCoreV1POSTNodeWithPath($name, $path);

```


### <a name="delete_proxy_core_v1_delete_node_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteProxyCoreV1DELETENodeWithPath") deleteProxyCoreV1DELETENodeWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy DELETE requests to Node


```php
function deleteProxyCoreV1DELETENodeWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->deleteProxyCoreV1DELETENodeWithPath($name, $path);

```


### <a name="update_proxy_core_v1_patch_node_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateProxyCoreV1PATCHNodeWithPath") updateProxyCoreV1PATCHNodeWithPath

> *Tags:*  ``` Skips Authentication ``` 

> proxy PATCH requests to Node


```php
function updateProxyCoreV1PATCHNodeWithPath(
        $name,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$path = 'path';

$result = $coreV1->updateProxyCoreV1PATCHNodeWithPath($name, $path);

```


### <a name="list_core_v1_replication_controller_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1ReplicationControllerForAllNamespaces") listCoreV1ReplicationControllerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ReplicationController


```php
function listCoreV1ReplicationControllerForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 179;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1ReplicationControllerForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_replication_controller_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1ReplicationControllerForAllNamespaces") createCoreV1ReplicationControllerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ReplicationController


```php
function createCoreV1ReplicationControllerForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ReplicationController();
$pretty = 'pretty';

$result = $coreV1->createCoreV1ReplicationControllerForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_resource_quota_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1ResourceQuotaForAllNamespaces") listCoreV1ResourceQuotaForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ResourceQuota


```php
function listCoreV1ResourceQuotaForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 15;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1ResourceQuotaForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_resource_quota_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1ResourceQuotaForAllNamespaces") createCoreV1ResourceQuotaForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ResourceQuota


```php
function createCoreV1ResourceQuotaForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ResourceQuota();
$pretty = 'pretty';

$result = $coreV1->createCoreV1ResourceQuotaForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_secret_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1SecretForAllNamespaces") listCoreV1SecretForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Secret


```php
function listCoreV1SecretForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 15;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1SecretForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_secret_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1SecretForAllNamespaces") createCoreV1SecretForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Secret


```php
function createCoreV1SecretForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Secret();
$pretty = 'pretty';

$result = $coreV1->createCoreV1SecretForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1SecurityContextConstraints") listCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind SecurityContextConstraints


```php
function listCoreV1SecurityContextConstraints(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 228;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->listCoreV1SecurityContextConstraints($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1SecurityContextConstraints") createCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> create SecurityContextConstraints


```php
function createCoreV1SecurityContextConstraints(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SecurityContextConstraints();
$pretty = 'pretty';

$result = $coreV1->createCoreV1SecurityContextConstraints($body, $pretty);

```


### <a name="delete_core_v1_collection_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1CollectionSecurityContextConstraints") deleteCoreV1CollectionSecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of SecurityContextConstraints


```php
function deleteCoreV1CollectionSecurityContextConstraints(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 228;
$watch = true;
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1CollectionSecurityContextConstraints($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getReadCoreV1SecurityContextConstraints") getReadCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> read the specified SecurityContextConstraints


```php
function getReadCoreV1SecurityContextConstraints(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the SecurityContextConstraints |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $coreV1->getReadCoreV1SecurityContextConstraints($name, $exact, $export, $pretty);

```


### <a name="update_replace_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.updateReplaceCoreV1SecurityContextConstraints") updateReplaceCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified SecurityContextConstraints


```php
function updateReplaceCoreV1SecurityContextConstraints(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the SecurityContextConstraints |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SecurityContextConstraints();
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->updateReplaceCoreV1SecurityContextConstraints($body, $name, $pretty);

```


### <a name="delete_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.deleteCoreV1SecurityContextConstraints") deleteCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> delete SecurityContextConstraints


```php
function deleteCoreV1SecurityContextConstraints(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the SecurityContextConstraints |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 65;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $coreV1->deleteCoreV1SecurityContextConstraints($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.patchCoreV1SecurityContextConstraints") patchCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified SecurityContextConstraints


```php
function patchCoreV1SecurityContextConstraints(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the SecurityContextConstraints |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $coreV1->patchCoreV1SecurityContextConstraints($body, $name, $pretty);

```


### <a name="list_core_v1_service_account_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1ServiceAccountForAllNamespaces") listCoreV1ServiceAccountForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ServiceAccount


```php
function listCoreV1ServiceAccountForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 65;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1ServiceAccountForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_service_account_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1ServiceAccountForAllNamespaces") createCoreV1ServiceAccountForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ServiceAccount


```php
function createCoreV1ServiceAccountForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ServiceAccount();
$pretty = 'pretty';

$result = $coreV1->createCoreV1ServiceAccountForAllNamespaces($body, $pretty);

```


### <a name="list_core_v1_service_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.listCoreV1ServiceForAllNamespaces") listCoreV1ServiceForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Service


```php
function listCoreV1ServiceForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 65;
$watch = false;
$pretty = 'pretty';

$result = $coreV1->listCoreV1ServiceForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_core_v1_service_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.createCoreV1ServiceForAllNamespaces") createCoreV1ServiceForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Service


```php
function createCoreV1ServiceForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Service();
$pretty = 'pretty';

$result = $coreV1->createCoreV1ServiceForAllNamespaces($body, $pretty);

```


### <a name="get_watch_core_v1_config_map_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1ConfigMapListForAllNamespaces") getWatchCoreV1ConfigMapListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ConfigMap


```php
function getWatchCoreV1ConfigMapListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 156;
$watch = true;

$result = $coreV1->getWatchCoreV1ConfigMapListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_endpoints_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1EndpointsListForAllNamespaces") getWatchCoreV1EndpointsListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Endpoints


```php
function getWatchCoreV1EndpointsListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 156;
$watch = true;

$result = $coreV1->getWatchCoreV1EndpointsListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_event_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1EventListForAllNamespaces") getWatchCoreV1EventListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Event


```php
function getWatchCoreV1EventListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 156;
$watch = true;

$result = $coreV1->getWatchCoreV1EventListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_limit_range_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1LimitRangeListForAllNamespaces") getWatchCoreV1LimitRangeListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of LimitRange


```php
function getWatchCoreV1LimitRangeListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 156;
$watch = true;

$result = $coreV1->getWatchCoreV1LimitRangeListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespace_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespaceList") getWatchCoreV1NamespaceList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Namespace


```php
function getWatchCoreV1NamespaceList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 115;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespaceList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_config_map_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedConfigMapList") getWatchCoreV1NamespacedConfigMapList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ConfigMap


```php
function getWatchCoreV1NamespacedConfigMapList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 115;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedConfigMapList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_config_map"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedConfigMap") getWatchCoreV1NamespacedConfigMap

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ConfigMap


```php
function getWatchCoreV1NamespacedConfigMap(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ConfigMap |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 115;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedConfigMap($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_endpoints_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedEndpointsList") getWatchCoreV1NamespacedEndpointsList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Endpoints


```php
function getWatchCoreV1NamespacedEndpointsList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 115;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedEndpointsList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_endpoints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedEndpoints") getWatchCoreV1NamespacedEndpoints

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Endpoints


```php
function getWatchCoreV1NamespacedEndpoints(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Endpoints |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 206;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedEndpoints($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_event_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedEventList") getWatchCoreV1NamespacedEventList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Event


```php
function getWatchCoreV1NamespacedEventList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 206;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedEventList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_event"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedEvent") getWatchCoreV1NamespacedEvent

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Event


```php
function getWatchCoreV1NamespacedEvent(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Event |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 206;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedEvent($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_limit_range_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedLimitRangeList") getWatchCoreV1NamespacedLimitRangeList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of LimitRange


```php
function getWatchCoreV1NamespacedLimitRangeList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedLimitRangeList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_limit_range"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedLimitRange") getWatchCoreV1NamespacedLimitRange

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind LimitRange


```php
function getWatchCoreV1NamespacedLimitRange(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the LimitRange |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedLimitRange($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_persistent_volume_claim_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedPersistentVolumeClaimList") getWatchCoreV1NamespacedPersistentVolumeClaimList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PersistentVolumeClaim


```php
function getWatchCoreV1NamespacedPersistentVolumeClaimList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedPersistentVolumeClaimList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_persistent_volume_claim"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedPersistentVolumeClaim") getWatchCoreV1NamespacedPersistentVolumeClaim

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PersistentVolumeClaim


```php
function getWatchCoreV1NamespacedPersistentVolumeClaim(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PersistentVolumeClaim |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedPersistentVolumeClaim($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_pod_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedPodList") getWatchCoreV1NamespacedPodList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Pod


```php
function getWatchCoreV1NamespacedPodList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 1;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedPodList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_pod"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedPod") getWatchCoreV1NamespacedPod

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Pod


```php
function getWatchCoreV1NamespacedPod(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Pod |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 1;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedPod($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_pod_template_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedPodTemplateList") getWatchCoreV1NamespacedPodTemplateList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodTemplate


```php
function getWatchCoreV1NamespacedPodTemplateList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 1;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedPodTemplateList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_pod_template"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedPodTemplate") getWatchCoreV1NamespacedPodTemplate

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PodTemplate


```php
function getWatchCoreV1NamespacedPodTemplate(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodTemplate |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 1;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedPodTemplate($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_replication_controller_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedReplicationControllerList") getWatchCoreV1NamespacedReplicationControllerList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ReplicationController


```php
function getWatchCoreV1NamespacedReplicationControllerList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 92;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedReplicationControllerList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_replication_controller"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedReplicationController") getWatchCoreV1NamespacedReplicationController

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ReplicationController


```php
function getWatchCoreV1NamespacedReplicationController(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ReplicationController |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 92;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedReplicationController($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_resource_quota_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedResourceQuotaList") getWatchCoreV1NamespacedResourceQuotaList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ResourceQuota


```php
function getWatchCoreV1NamespacedResourceQuotaList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 92;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedResourceQuotaList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedResourceQuota") getWatchCoreV1NamespacedResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ResourceQuota


```php
function getWatchCoreV1NamespacedResourceQuota(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 51;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedResourceQuota($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_secret_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedSecretList") getWatchCoreV1NamespacedSecretList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Secret


```php
function getWatchCoreV1NamespacedSecretList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 51;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedSecretList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_secret"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedSecret") getWatchCoreV1NamespacedSecret

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Secret


```php
function getWatchCoreV1NamespacedSecret(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Secret |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 51;
$watch = false;

$result = $coreV1->getWatchCoreV1NamespacedSecret($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_service_account_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedServiceAccountList") getWatchCoreV1NamespacedServiceAccountList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ServiceAccount


```php
function getWatchCoreV1NamespacedServiceAccountList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 142;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedServiceAccountList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_service_account"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedServiceAccount") getWatchCoreV1NamespacedServiceAccount

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ServiceAccount


```php
function getWatchCoreV1NamespacedServiceAccount(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ServiceAccount |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 142;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedServiceAccount($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_service_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedServiceList") getWatchCoreV1NamespacedServiceList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Service


```php
function getWatchCoreV1NamespacedServiceList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 142;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedServiceList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespaced_service"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NamespacedService") getWatchCoreV1NamespacedService

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Service


```php
function getWatchCoreV1NamespacedService(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Service |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 142;
$watch = true;

$result = $coreV1->getWatchCoreV1NamespacedService($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1Namespace") getWatchCoreV1Namespace

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Namespace


```php
function getWatchCoreV1Namespace(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Namespace |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 234;
$watch = true;

$result = $coreV1->getWatchCoreV1Namespace($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_node_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1NodeList") getWatchCoreV1NodeList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Node


```php
function getWatchCoreV1NodeList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 234;
$watch = true;

$result = $coreV1->getWatchCoreV1NodeList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_node"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1Node") getWatchCoreV1Node

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Node


```php
function getWatchCoreV1Node(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Node |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 234;
$watch = true;

$result = $coreV1->getWatchCoreV1Node($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_persistent_volume_claim_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1PersistentVolumeClaimListForAllNamespaces") getWatchCoreV1PersistentVolumeClaimListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PersistentVolumeClaim


```php
function getWatchCoreV1PersistentVolumeClaimListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 234;
$watch = true;

$result = $coreV1->getWatchCoreV1PersistentVolumeClaimListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_persistent_volume_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1PersistentVolumeList") getWatchCoreV1PersistentVolumeList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PersistentVolume


```php
function getWatchCoreV1PersistentVolumeList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 234;
$watch = true;

$result = $coreV1->getWatchCoreV1PersistentVolumeList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_persistent_volume"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1PersistentVolume") getWatchCoreV1PersistentVolume

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PersistentVolume


```php
function getWatchCoreV1PersistentVolume(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PersistentVolume |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 192;
$watch = true;

$result = $coreV1->getWatchCoreV1PersistentVolume($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_pod_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1PodListForAllNamespaces") getWatchCoreV1PodListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Pod


```php
function getWatchCoreV1PodListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 192;
$watch = true;

$result = $coreV1->getWatchCoreV1PodListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_pod_template_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1PodTemplateListForAllNamespaces") getWatchCoreV1PodTemplateListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodTemplate


```php
function getWatchCoreV1PodTemplateListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 192;
$watch = true;

$result = $coreV1->getWatchCoreV1PodTemplateListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_replication_controller_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1ReplicationControllerListForAllNamespaces") getWatchCoreV1ReplicationControllerListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ReplicationController


```php
function getWatchCoreV1ReplicationControllerListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 29;
$watch = false;

$result = $coreV1->getWatchCoreV1ReplicationControllerListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_resource_quota_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1ResourceQuotaListForAllNamespaces") getWatchCoreV1ResourceQuotaListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ResourceQuota


```php
function getWatchCoreV1ResourceQuotaListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 29;
$watch = false;

$result = $coreV1->getWatchCoreV1ResourceQuotaListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_secret_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1SecretListForAllNamespaces") getWatchCoreV1SecretListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Secret


```php
function getWatchCoreV1SecretListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 29;
$watch = false;

$result = $coreV1->getWatchCoreV1SecretListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_security_context_constraints_list"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1SecurityContextConstraintsList") getWatchCoreV1SecurityContextConstraintsList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of SecurityContextConstraints


```php
function getWatchCoreV1SecurityContextConstraintsList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 242;
$watch = true;

$result = $coreV1->getWatchCoreV1SecurityContextConstraintsList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_security_context_constraints"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1SecurityContextConstraints") getWatchCoreV1SecurityContextConstraints

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind SecurityContextConstraints


```php
function getWatchCoreV1SecurityContextConstraints(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the SecurityContextConstraints |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 242;
$watch = true;

$result = $coreV1->getWatchCoreV1SecurityContextConstraints($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_service_account_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1ServiceAccountListForAllNamespaces") getWatchCoreV1ServiceAccountListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ServiceAccount


```php
function getWatchCoreV1ServiceAccountListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 242;
$watch = true;

$result = $coreV1->getWatchCoreV1ServiceAccountListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_core_v1_service_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".CoreV1Controller.getWatchCoreV1ServiceListForAllNamespaces") getWatchCoreV1ServiceListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Service


```php
function getWatchCoreV1ServiceListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 242;
$watch = true;

$result = $coreV1->getWatchCoreV1ServiceListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="apis_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ApisController") ApisController

### Get singleton instance

The singleton instance of the ``` ApisController ``` class can be accessed from the API Client.

```php
$apis = $client->getApis();
```

### <a name="get_api_versions"></a>![Method: ](https://apidocs.io/img/method.png ".ApisController.getAPIVersions") getAPIVersions

> *Tags:*  ``` Skips Authentication ``` 

> get available API versions


```php
function getAPIVersions()
```

#### Example Usage

```php

$result = $apis->getAPIVersions();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="apps_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AppsOpenshiftIoController") AppsOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` AppsOpenshiftIoController ``` class can be accessed from the API Client.

```php
$appsOpenshiftIo = $client->getAppsOpenshiftIo();
```

### <a name="get_apps_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoController.getAppsOpenshiftIoAPIGroup") getAppsOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getAppsOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $appsOpenshiftIo->getAppsOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="apps_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AppsOpenshiftIoV1Controller") AppsOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` AppsOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$appsOpenshiftIoV1 = $client->getAppsOpenshiftIoV1();
```

### <a name="get_apps_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getAppsOpenshiftIoV1APIResources") getAppsOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAppsOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $appsOpenshiftIoV1->getAppsOpenshiftIoV1APIResources();

```


### <a name="list_apps_openshift_io_v1_deployment_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.listAppsOpenshiftIoV1DeploymentConfigForAllNamespaces") listAppsOpenshiftIoV1DeploymentConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind DeploymentConfig


```php
function listAppsOpenshiftIoV1DeploymentConfigForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 242;
$watch = true;
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->listAppsOpenshiftIoV1DeploymentConfigForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_apps_openshift_io_v1_deployment_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.createAppsOpenshiftIoV1DeploymentConfigForAllNamespaces") createAppsOpenshiftIoV1DeploymentConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a DeploymentConfig


```php
function createAppsOpenshiftIoV1DeploymentConfigForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->createAppsOpenshiftIoV1DeploymentConfigForAllNamespaces($body, $pretty);

```


### <a name="list_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.listAppsOpenshiftIoV1NamespacedDeploymentConfig") listAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind DeploymentConfig


```php
function listAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 78;
$watch = false;
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->listAppsOpenshiftIoV1NamespacedDeploymentConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.createAppsOpenshiftIoV1NamespacedDeploymentConfig") createAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> create a DeploymentConfig


```php
function createAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->createAppsOpenshiftIoV1NamespacedDeploymentConfig($body, $mnamespace, $pretty);

```


### <a name="delete_apps_openshift_io_v1_collection_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.deleteAppsOpenshiftIoV1CollectionNamespacedDeploymentConfig") deleteAppsOpenshiftIoV1CollectionNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of DeploymentConfig


```php
function deleteAppsOpenshiftIoV1CollectionNamespacedDeploymentConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 78;
$watch = false;
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->deleteAppsOpenshiftIoV1CollectionNamespacedDeploymentConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getReadAppsOpenshiftIoV1NamespacedDeploymentConfig") getReadAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> read the specified DeploymentConfig


```php
function getReadAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->getReadAppsOpenshiftIoV1NamespacedDeploymentConfig($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfig") updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified DeploymentConfig


```php
function updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfig($body, $name, $mnamespace, $pretty);

```


### <a name="delete_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.deleteAppsOpenshiftIoV1NamespacedDeploymentConfig") deleteAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete a DeploymentConfig


```php
function deleteAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 170;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->deleteAppsOpenshiftIoV1NamespacedDeploymentConfig($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.patchAppsOpenshiftIoV1NamespacedDeploymentConfig") patchAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified DeploymentConfig


```php
function patchAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->patchAppsOpenshiftIoV1NamespacedDeploymentConfig($body, $name, $mnamespace, $pretty);

```


### <a name="create_apps_openshift_io_v1_namespaced_deployment_request_instantiate"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.createAppsOpenshiftIoV1NamespacedDeploymentRequestInstantiate") createAppsOpenshiftIoV1NamespacedDeploymentRequestInstantiate

> *Tags:*  ``` Skips Authentication ``` 

> create instantiate of a DeploymentRequest


```php
function createAppsOpenshiftIoV1NamespacedDeploymentRequestInstantiate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentRequest |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentRequest();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->createAppsOpenshiftIoV1NamespacedDeploymentRequestInstantiate($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_apps_openshift_io_v1_namespaced_deployment_log_log"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getReadAppsOpenshiftIoV1NamespacedDeploymentLogLog") getReadAppsOpenshiftIoV1NamespacedDeploymentLogLog

> *Tags:*  ``` Skips Authentication ``` 

> read log of the specified DeploymentLog


```php
function getReadAppsOpenshiftIoV1NamespacedDeploymentLogLog(
        $name,
        $mnamespace,
        $container = null,
        $follow = null,
        $limitBytes = null,
        $nowait = null,
        $pretty = null,
        $previous = null,
        $sinceSeconds = null,
        $tailLines = null,
        $timestamps = null,
        $version = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentLog |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | The container for which to stream logs. Defaults to only container if there is one container in the pod. |
| follow |  ``` Optional ```  | Follow if true indicates that the build log should be streamed until the build terminates. |
| limitBytes |  ``` Optional ```  | If set, the number of bytes to read from the server before terminating the log output. This may not display a complete final line of logging, and may return slightly more or slightly less than the specified limit. |
| nowait |  ``` Optional ```  | NoWait if true causes the call to return immediately even if the deployment is not available yet. Otherwise the server will wait until the deployment has started. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| previous |  ``` Optional ```  | Return previous deployment logs. Defaults to false. |
| sinceSeconds |  ``` Optional ```  | A relative time in seconds before the current time from which to show logs. If this value precedes the time a pod was started, only logs since the pod start will be returned. If this value is in the future, no logs will be returned. Only one of sinceSeconds or sinceTime may be specified. |
| tailLines |  ``` Optional ```  | If set, the number of lines from the end of the logs to show. If not specified, logs are shown from the creation of the container or sinceSeconds or sinceTime |
| timestamps |  ``` Optional ```  | If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line of log output. Defaults to false. |
| version |  ``` Optional ```  | Version of the deployment for which to view logs. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$follow = true;
$limitBytes = 128;
$nowait = true;
$pretty = 'pretty';
$previous = true;
$sinceSeconds = 128;
$tailLines = 128;
$timestamps = true;
$version = 128;

$result = $appsOpenshiftIoV1->getReadAppsOpenshiftIoV1NamespacedDeploymentLogLog($name, $mnamespace, $container, $follow, $limitBytes, $nowait, $pretty, $previous, $sinceSeconds, $tailLines, $timestamps, $version);

```


### <a name="create_apps_openshift_io_v1_namespaced_deployment_config_rollback_rollback"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.createAppsOpenshiftIoV1NamespacedDeploymentConfigRollbackRollback") createAppsOpenshiftIoV1NamespacedDeploymentConfigRollbackRollback

> *Tags:*  ``` Skips Authentication ``` 

> create rollback of a DeploymentConfigRollback


```php
function createAppsOpenshiftIoV1NamespacedDeploymentConfigRollbackRollback(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfigRollback |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfigRollback();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->createAppsOpenshiftIoV1NamespacedDeploymentConfigRollbackRollback($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_apps_openshift_io_v1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getReadAppsOpenshiftIoV1NamespacedScaleScale") getReadAppsOpenshiftIoV1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadAppsOpenshiftIoV1NamespacedScaleScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->getReadAppsOpenshiftIoV1NamespacedScaleScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_apps_openshift_io_v1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.updateReplaceAppsOpenshiftIoV1NamespacedScaleScale") updateReplaceAppsOpenshiftIoV1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceAppsOpenshiftIoV1NamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->updateReplaceAppsOpenshiftIoV1NamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_apps_openshift_io_v1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.patchAppsOpenshiftIoV1NamespacedScaleScale") patchAppsOpenshiftIoV1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchAppsOpenshiftIoV1NamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->patchAppsOpenshiftIoV1NamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_apps_openshift_io_v1_namespaced_deployment_config_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getReadAppsOpenshiftIoV1NamespacedDeploymentConfigStatus") getReadAppsOpenshiftIoV1NamespacedDeploymentConfigStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified DeploymentConfig


```php
function getReadAppsOpenshiftIoV1NamespacedDeploymentConfigStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->getReadAppsOpenshiftIoV1NamespacedDeploymentConfigStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_apps_openshift_io_v1_namespaced_deployment_config_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfigStatus") updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfigStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified DeploymentConfig


```php
function updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfigStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->updateReplaceAppsOpenshiftIoV1NamespacedDeploymentConfigStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_apps_openshift_io_v1_namespaced_deployment_config_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.patchAppsOpenshiftIoV1NamespacedDeploymentConfigStatus") patchAppsOpenshiftIoV1NamespacedDeploymentConfigStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified DeploymentConfig


```php
function patchAppsOpenshiftIoV1NamespacedDeploymentConfigStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsOpenshiftIoV1->patchAppsOpenshiftIoV1NamespacedDeploymentConfigStatus($body, $name, $mnamespace, $pretty);

```


### <a name="get_watch_apps_openshift_io_v1_deployment_config_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getWatchAppsOpenshiftIoV1DeploymentConfigListForAllNamespaces") getWatchAppsOpenshiftIoV1DeploymentConfigListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of DeploymentConfig


```php
function getWatchAppsOpenshiftIoV1DeploymentConfigListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 220;
$watch = true;

$result = $appsOpenshiftIoV1->getWatchAppsOpenshiftIoV1DeploymentConfigListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_openshift_io_v1_namespaced_deployment_config_list"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getWatchAppsOpenshiftIoV1NamespacedDeploymentConfigList") getWatchAppsOpenshiftIoV1NamespacedDeploymentConfigList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of DeploymentConfig


```php
function getWatchAppsOpenshiftIoV1NamespacedDeploymentConfigList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 220;
$watch = true;

$result = $appsOpenshiftIoV1->getWatchAppsOpenshiftIoV1NamespacedDeploymentConfigList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_openshift_io_v1_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".AppsOpenshiftIoV1Controller.getWatchAppsOpenshiftIoV1NamespacedDeploymentConfig") getWatchAppsOpenshiftIoV1NamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind DeploymentConfig


```php
function getWatchAppsOpenshiftIoV1NamespacedDeploymentConfig(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 56;
$watch = false;

$result = $appsOpenshiftIoV1->getWatchAppsOpenshiftIoV1NamespacedDeploymentConfig($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="apps_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AppsController") AppsController

### Get singleton instance

The singleton instance of the ``` AppsController ``` class can be accessed from the API Client.

```php
$apps = $client->getApps();
```

### <a name="get_apps_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".AppsController.getAppsAPIGroup") getAppsAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getAppsAPIGroup()
```

#### Example Usage

```php

$result = $apps->getAppsAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="apps_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AppsV1beta1Controller") AppsV1beta1Controller

### Get singleton instance

The singleton instance of the ``` AppsV1beta1Controller ``` class can be accessed from the API Client.

```php
$appsV1beta1 = $client->getAppsV1beta1();
```

### <a name="get_apps_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getAppsV1beta1APIResources") getAppsV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAppsV1beta1APIResources()
```

#### Example Usage

```php

$result = $appsV1beta1->getAppsV1beta1APIResources();

```


### <a name="list_apps_v1beta1_deployment_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.listAppsV1beta1DeploymentForAllNamespaces") listAppsV1beta1DeploymentForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Deployment


```php
function listAppsV1beta1DeploymentForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 56;
$watch = false;
$pretty = 'pretty';

$result = $appsV1beta1->listAppsV1beta1DeploymentForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_apps_v1beta1_deployment_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.createAppsV1beta1DeploymentForAllNamespaces") createAppsV1beta1DeploymentForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Deployment


```php
function createAppsV1beta1DeploymentForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$pretty = 'pretty';

$result = $appsV1beta1->createAppsV1beta1DeploymentForAllNamespaces($body, $pretty);

```


### <a name="list_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.listAppsV1beta1NamespacedDeployment") listAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Deployment


```php
function listAppsV1beta1NamespacedDeployment(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 56;
$watch = false;
$pretty = 'pretty';

$result = $appsV1beta1->listAppsV1beta1NamespacedDeployment($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.createAppsV1beta1NamespacedDeployment") createAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> create a Deployment


```php
function createAppsV1beta1NamespacedDeployment(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->createAppsV1beta1NamespacedDeployment($body, $mnamespace, $pretty);

```


### <a name="delete_apps_v1beta1_collection_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.deleteAppsV1beta1CollectionNamespacedDeployment") deleteAppsV1beta1CollectionNamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Deployment


```php
function deleteAppsV1beta1CollectionNamespacedDeployment(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 14;
$watch = false;
$pretty = 'pretty';

$result = $appsV1beta1->deleteAppsV1beta1CollectionNamespacedDeployment($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getReadAppsV1beta1NamespacedDeployment") getReadAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Deployment


```php
function getReadAppsV1beta1NamespacedDeployment(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $appsV1beta1->getReadAppsV1beta1NamespacedDeployment($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.updateReplaceAppsV1beta1NamespacedDeployment") updateReplaceAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Deployment


```php
function updateReplaceAppsV1beta1NamespacedDeployment(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->updateReplaceAppsV1beta1NamespacedDeployment($body, $name, $mnamespace, $pretty);

```


### <a name="delete_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.deleteAppsV1beta1NamespacedDeployment") deleteAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> delete a Deployment


```php
function deleteAppsV1beta1NamespacedDeployment(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 14;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $appsV1beta1->deleteAppsV1beta1NamespacedDeployment($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.patchAppsV1beta1NamespacedDeployment") patchAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Deployment


```php
function patchAppsV1beta1NamespacedDeployment(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->patchAppsV1beta1NamespacedDeployment($body, $name, $mnamespace, $pretty);

```


### <a name="create_apps_v1beta1_namespaced_deployment_rollback_rollback"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.createAppsV1beta1NamespacedDeploymentRollbackRollback") createAppsV1beta1NamespacedDeploymentRollbackRollback

> *Tags:*  ``` Skips Authentication ``` 

> create rollback of a DeploymentRollback


```php
function createAppsV1beta1NamespacedDeploymentRollbackRollback(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentRollback |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1DeploymentRollback();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->createAppsV1beta1NamespacedDeploymentRollbackRollback($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_apps_v1beta1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getReadAppsV1beta1NamespacedScaleScale") getReadAppsV1beta1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadAppsV1beta1NamespacedScaleScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->getReadAppsV1beta1NamespacedScaleScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_apps_v1beta1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.updateReplaceAppsV1beta1NamespacedScaleScale") updateReplaceAppsV1beta1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceAppsV1beta1NamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->updateReplaceAppsV1beta1NamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_apps_v1beta1_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.patchAppsV1beta1NamespacedScaleScale") patchAppsV1beta1NamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchAppsV1beta1NamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->patchAppsV1beta1NamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_apps_v1beta1_namespaced_deployment_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getReadAppsV1beta1NamespacedDeploymentStatus") getReadAppsV1beta1NamespacedDeploymentStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Deployment


```php
function getReadAppsV1beta1NamespacedDeploymentStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->getReadAppsV1beta1NamespacedDeploymentStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_apps_v1beta1_namespaced_deployment_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.updateReplaceAppsV1beta1NamespacedDeploymentStatus") updateReplaceAppsV1beta1NamespacedDeploymentStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Deployment


```php
function updateReplaceAppsV1beta1NamespacedDeploymentStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->updateReplaceAppsV1beta1NamespacedDeploymentStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_apps_v1beta1_namespaced_deployment_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.patchAppsV1beta1NamespacedDeploymentStatus") patchAppsV1beta1NamespacedDeploymentStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Deployment


```php
function patchAppsV1beta1NamespacedDeploymentStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->patchAppsV1beta1NamespacedDeploymentStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.listAppsV1beta1NamespacedStatefulSet") listAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind StatefulSet


```php
function listAppsV1beta1NamespacedStatefulSet(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 64;
$watch = false;
$pretty = 'pretty';

$result = $appsV1beta1->listAppsV1beta1NamespacedStatefulSet($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.createAppsV1beta1NamespacedStatefulSet") createAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> create a StatefulSet


```php
function createAppsV1beta1NamespacedStatefulSet(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1StatefulSet();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->createAppsV1beta1NamespacedStatefulSet($body, $mnamespace, $pretty);

```


### <a name="delete_apps_v1beta1_collection_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.deleteAppsV1beta1CollectionNamespacedStatefulSet") deleteAppsV1beta1CollectionNamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of StatefulSet


```php
function deleteAppsV1beta1CollectionNamespacedStatefulSet(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 64;
$watch = false;
$pretty = 'pretty';

$result = $appsV1beta1->deleteAppsV1beta1CollectionNamespacedStatefulSet($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getReadAppsV1beta1NamespacedStatefulSet") getReadAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> read the specified StatefulSet


```php
function getReadAppsV1beta1NamespacedStatefulSet(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $appsV1beta1->getReadAppsV1beta1NamespacedStatefulSet($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.updateReplaceAppsV1beta1NamespacedStatefulSet") updateReplaceAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified StatefulSet


```php
function updateReplaceAppsV1beta1NamespacedStatefulSet(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1StatefulSet();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->updateReplaceAppsV1beta1NamespacedStatefulSet($body, $name, $mnamespace, $pretty);

```


### <a name="delete_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.deleteAppsV1beta1NamespacedStatefulSet") deleteAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> delete a StatefulSet


```php
function deleteAppsV1beta1NamespacedStatefulSet(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 156;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $appsV1beta1->deleteAppsV1beta1NamespacedStatefulSet($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.patchAppsV1beta1NamespacedStatefulSet") patchAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified StatefulSet


```php
function patchAppsV1beta1NamespacedStatefulSet(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->patchAppsV1beta1NamespacedStatefulSet($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_apps_v1beta1_namespaced_stateful_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getReadAppsV1beta1NamespacedStatefulSetStatus") getReadAppsV1beta1NamespacedStatefulSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified StatefulSet


```php
function getReadAppsV1beta1NamespacedStatefulSetStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->getReadAppsV1beta1NamespacedStatefulSetStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_apps_v1beta1_namespaced_stateful_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.updateReplaceAppsV1beta1NamespacedStatefulSetStatus") updateReplaceAppsV1beta1NamespacedStatefulSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified StatefulSet


```php
function updateReplaceAppsV1beta1NamespacedStatefulSetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1StatefulSet();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->updateReplaceAppsV1beta1NamespacedStatefulSetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_apps_v1beta1_namespaced_stateful_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.patchAppsV1beta1NamespacedStatefulSetStatus") patchAppsV1beta1NamespacedStatefulSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified StatefulSet


```php
function patchAppsV1beta1NamespacedStatefulSetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $appsV1beta1->patchAppsV1beta1NamespacedStatefulSetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_apps_v1beta1_stateful_set_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.listAppsV1beta1StatefulSetForAllNamespaces") listAppsV1beta1StatefulSetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind StatefulSet


```php
function listAppsV1beta1StatefulSetForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 247;
$watch = true;
$pretty = 'pretty';

$result = $appsV1beta1->listAppsV1beta1StatefulSetForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_apps_v1beta1_stateful_set_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.createAppsV1beta1StatefulSetForAllNamespaces") createAppsV1beta1StatefulSetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a StatefulSet


```php
function createAppsV1beta1StatefulSetForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1StatefulSet();
$pretty = 'pretty';

$result = $appsV1beta1->createAppsV1beta1StatefulSetForAllNamespaces($body, $pretty);

```


### <a name="get_watch_apps_v1beta1_deployment_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getWatchAppsV1beta1DeploymentListForAllNamespaces") getWatchAppsV1beta1DeploymentListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Deployment


```php
function getWatchAppsV1beta1DeploymentListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 247;
$watch = true;

$result = $appsV1beta1->getWatchAppsV1beta1DeploymentListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_v1beta1_namespaced_deployment_list"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getWatchAppsV1beta1NamespacedDeploymentList") getWatchAppsV1beta1NamespacedDeploymentList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Deployment


```php
function getWatchAppsV1beta1NamespacedDeploymentList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 247;
$watch = true;

$result = $appsV1beta1->getWatchAppsV1beta1NamespacedDeploymentList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getWatchAppsV1beta1NamespacedDeployment") getWatchAppsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Deployment


```php
function getWatchAppsV1beta1NamespacedDeployment(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 205;
$watch = true;

$result = $appsV1beta1->getWatchAppsV1beta1NamespacedDeployment($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_v1beta1_namespaced_stateful_set_list"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getWatchAppsV1beta1NamespacedStatefulSetList") getWatchAppsV1beta1NamespacedStatefulSetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of StatefulSet


```php
function getWatchAppsV1beta1NamespacedStatefulSetList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 205;
$watch = true;

$result = $appsV1beta1->getWatchAppsV1beta1NamespacedStatefulSetList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_v1beta1_namespaced_stateful_set"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getWatchAppsV1beta1NamespacedStatefulSet") getWatchAppsV1beta1NamespacedStatefulSet

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind StatefulSet


```php
function getWatchAppsV1beta1NamespacedStatefulSet(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StatefulSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 205;
$watch = true;

$result = $appsV1beta1->getWatchAppsV1beta1NamespacedStatefulSet($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_apps_v1beta1_stateful_set_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AppsV1beta1Controller.getWatchAppsV1beta1StatefulSetListForAllNamespaces") getWatchAppsV1beta1StatefulSetListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of StatefulSet


```php
function getWatchAppsV1beta1StatefulSetListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 42;
$watch = false;

$result = $appsV1beta1->getWatchAppsV1beta1StatefulSetListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authentication_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthenticationController") AuthenticationController

### Get singleton instance

The singleton instance of the ``` AuthenticationController ``` class can be accessed from the API Client.

```php
$authentication = $client->getAuthentication();
```

### <a name="get_authentication_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".AuthenticationController.getAuthenticationAPIGroup") getAuthenticationAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getAuthenticationAPIGroup()
```

#### Example Usage

```php

$result = $authentication->getAuthenticationAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authentication_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthenticationV1Controller") AuthenticationV1Controller

### Get singleton instance

The singleton instance of the ``` AuthenticationV1Controller ``` class can be accessed from the API Client.

```php
$authenticationV1 = $client->getAuthenticationV1();
```

### <a name="get_authentication_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AuthenticationV1Controller.getAuthenticationV1APIResources") getAuthenticationV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAuthenticationV1APIResources()
```

#### Example Usage

```php

$result = $authenticationV1->getAuthenticationV1APIResources();

```


### <a name="create_authentication_v1_token_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthenticationV1Controller.createAuthenticationV1TokenReview") createAuthenticationV1TokenReview

> *Tags:*  ``` Skips Authentication ``` 

> create a TokenReview


```php
function createAuthenticationV1TokenReview(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1TokenReview();
$pretty = 'pretty';

$result = $authenticationV1->createAuthenticationV1TokenReview($body, $pretty);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authentication_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthenticationV1beta1Controller") AuthenticationV1beta1Controller

### Get singleton instance

The singleton instance of the ``` AuthenticationV1beta1Controller ``` class can be accessed from the API Client.

```php
$authenticationV1beta1 = $client->getAuthenticationV1beta1();
```

### <a name="get_authentication_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AuthenticationV1beta1Controller.getAuthenticationV1beta1APIResources") getAuthenticationV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAuthenticationV1beta1APIResources()
```

#### Example Usage

```php

$result = $authenticationV1beta1->getAuthenticationV1beta1APIResources();

```


### <a name="create_authentication_v1beta1_token_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthenticationV1beta1Controller.createAuthenticationV1beta1TokenReview") createAuthenticationV1beta1TokenReview

> *Tags:*  ``` Skips Authentication ``` 

> create a TokenReview


```php
function createAuthenticationV1beta1TokenReview(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1TokenReview();
$pretty = 'pretty';

$result = $authenticationV1beta1->createAuthenticationV1beta1TokenReview($body, $pretty);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authorization_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthorizationController") AuthorizationController

### Get singleton instance

The singleton instance of the ``` AuthorizationController ``` class can be accessed from the API Client.

```php
$authorization = $client->getAuthorization();
```

### <a name="get_authorization_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationController.getAuthorizationAPIGroup") getAuthorizationAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getAuthorizationAPIGroup()
```

#### Example Usage

```php

$result = $authorization->getAuthorizationAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authorization_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthorizationV1Controller") AuthorizationV1Controller

### Get singleton instance

The singleton instance of the ``` AuthorizationV1Controller ``` class can be accessed from the API Client.

```php
$authorizationV1 = $client->getAuthorizationV1();
```

### <a name="get_authorization_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1Controller.getAuthorizationV1APIResources") getAuthorizationV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAuthorizationV1APIResources()
```

#### Example Usage

```php

$result = $authorizationV1->getAuthorizationV1APIResources();

```


### <a name="create_authorization_v1_local_subject_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1Controller.createAuthorizationV1LocalSubjectAccessReviewForAllNamespaces") createAuthorizationV1LocalSubjectAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createAuthorizationV1LocalSubjectAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationV1->createAuthorizationV1LocalSubjectAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_v1_namespaced_local_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1Controller.createAuthorizationV1NamespacedLocalSubjectAccessReview") createAuthorizationV1NamespacedLocalSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createAuthorizationV1NamespacedLocalSubjectAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationV1->createAuthorizationV1NamespacedLocalSubjectAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_authorization_v1_self_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1Controller.createAuthorizationV1SelfSubjectAccessReview") createAuthorizationV1SelfSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SelfSubjectAccessReview


```php
function createAuthorizationV1SelfSubjectAccessReview(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SelfSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationV1->createAuthorizationV1SelfSubjectAccessReview($body, $pretty);

```


### <a name="create_authorization_v1_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1Controller.createAuthorizationV1SubjectAccessReview") createAuthorizationV1SubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectAccessReview


```php
function createAuthorizationV1SubjectAccessReview(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationV1->createAuthorizationV1SubjectAccessReview($body, $pretty);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authorization_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthorizationV1beta1Controller") AuthorizationV1beta1Controller

### Get singleton instance

The singleton instance of the ``` AuthorizationV1beta1Controller ``` class can be accessed from the API Client.

```php
$authorizationV1beta1 = $client->getAuthorizationV1beta1();
```

### <a name="get_authorization_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1beta1Controller.getAuthorizationV1beta1APIResources") getAuthorizationV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAuthorizationV1beta1APIResources()
```

#### Example Usage

```php

$result = $authorizationV1beta1->getAuthorizationV1beta1APIResources();

```


### <a name="create_authorization_v1beta1_local_subject_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1beta1Controller.createAuthorizationV1beta1LocalSubjectAccessReviewForAllNamespaces") createAuthorizationV1beta1LocalSubjectAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createAuthorizationV1beta1LocalSubjectAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationV1beta1->createAuthorizationV1beta1LocalSubjectAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_v1beta1_namespaced_local_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1beta1Controller.createAuthorizationV1beta1NamespacedLocalSubjectAccessReview") createAuthorizationV1beta1NamespacedLocalSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createAuthorizationV1beta1NamespacedLocalSubjectAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1LocalSubjectAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationV1beta1->createAuthorizationV1beta1NamespacedLocalSubjectAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_authorization_v1beta1_self_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1beta1Controller.createAuthorizationV1beta1SelfSubjectAccessReview") createAuthorizationV1beta1SelfSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SelfSubjectAccessReview


```php
function createAuthorizationV1beta1SelfSubjectAccessReview(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SelfSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationV1beta1->createAuthorizationV1beta1SelfSubjectAccessReview($body, $pretty);

```


### <a name="create_authorization_v1beta1_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationV1beta1Controller.createAuthorizationV1beta1SubjectAccessReview") createAuthorizationV1beta1SubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectAccessReview


```php
function createAuthorizationV1beta1SubjectAccessReview(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationV1beta1->createAuthorizationV1beta1SubjectAccessReview($body, $pretty);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authorization_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthorizationOpenshiftIoController") AuthorizationOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` AuthorizationOpenshiftIoController ``` class can be accessed from the API Client.

```php
$authorizationOpenshiftIo = $client->getAuthorizationOpenshiftIo();
```

### <a name="get_authorization_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoController.getAuthorizationOpenshiftIoAPIGroup") getAuthorizationOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getAuthorizationOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $authorizationOpenshiftIo->getAuthorizationOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="authorization_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AuthorizationOpenshiftIoV1Controller") AuthorizationOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` AuthorizationOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$authorizationOpenshiftIoV1 = $client->getAuthorizationOpenshiftIoV1();
```

### <a name="get_authorization_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getAuthorizationOpenshiftIoV1APIResources") getAuthorizationOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAuthorizationOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $authorizationOpenshiftIoV1->getAuthorizationOpenshiftIoV1APIResources();

```


### <a name="list_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1ClusterPolicy") listAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterPolicy


```php
function listAuthorizationOpenshiftIoV1ClusterPolicy(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 133;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1ClusterPolicy($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1ClusterPolicy") createAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterPolicy


```php
function createAuthorizationOpenshiftIoV1ClusterPolicy(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1ClusterPolicy($body, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_collection_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1CollectionClusterPolicy") deleteAuthorizationOpenshiftIoV1CollectionClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterPolicy


```php
function deleteAuthorizationOpenshiftIoV1CollectionClusterPolicy(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 92;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1CollectionClusterPolicy($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1ClusterPolicy") getReadAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterPolicy


```php
function getReadAuthorizationOpenshiftIoV1ClusterPolicy(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicy |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1ClusterPolicy($name, $exact, $export, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1ClusterPolicy") updateReplaceAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterPolicy


```php
function updateReplaceAuthorizationOpenshiftIoV1ClusterPolicy(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicy |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1ClusterPolicy($body, $name, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1ClusterPolicy") deleteAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterPolicy


```php
function deleteAuthorizationOpenshiftIoV1ClusterPolicy(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicy |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 92;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1ClusterPolicy($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1ClusterPolicy") patchAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterPolicy


```php
function patchAuthorizationOpenshiftIoV1ClusterPolicy(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicy |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1ClusterPolicy($body, $name, $pretty);

```


### <a name="list_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1ClusterPolicyBinding") listAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterPolicyBinding


```php
function listAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 92;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1ClusterPolicyBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1ClusterPolicyBinding") createAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterPolicyBinding


```php
function createAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1ClusterPolicyBinding($body, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_collection_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1CollectionClusterPolicyBinding") deleteAuthorizationOpenshiftIoV1CollectionClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterPolicyBinding


```php
function deleteAuthorizationOpenshiftIoV1CollectionClusterPolicyBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 183;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1CollectionClusterPolicyBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1ClusterPolicyBinding") getReadAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterPolicyBinding


```php
function getReadAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1ClusterPolicyBinding($name, $exact, $export, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1ClusterPolicyBinding") updateReplaceAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterPolicyBinding


```php
function updateReplaceAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1ClusterPolicyBinding($body, $name, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1ClusterPolicyBinding") deleteAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterPolicyBinding


```php
function deleteAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 183;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1ClusterPolicyBinding($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1ClusterPolicyBinding") patchAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterPolicyBinding


```php
function patchAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1ClusterPolicyBinding($body, $name, $pretty);

```


### <a name="list_authorization_openshift_io_v1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1ClusterRoleBinding") listAuthorizationOpenshiftIoV1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ClusterRoleBinding


```php
function listAuthorizationOpenshiftIoV1ClusterRoleBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 183;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1ClusterRoleBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1ClusterRoleBinding") createAuthorizationOpenshiftIoV1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterRoleBinding


```php
function createAuthorizationOpenshiftIoV1ClusterRoleBinding(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1ClusterRoleBinding($body, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1ClusterRoleBinding") getReadAuthorizationOpenshiftIoV1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterRoleBinding


```php
function getReadAuthorizationOpenshiftIoV1ClusterRoleBinding(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1ClusterRoleBinding($name, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1ClusterRoleBinding") updateReplaceAuthorizationOpenshiftIoV1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterRoleBinding


```php
function updateReplaceAuthorizationOpenshiftIoV1ClusterRoleBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1ClusterRoleBinding($body, $name, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1ClusterRoleBinding") deleteAuthorizationOpenshiftIoV1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterRoleBinding


```php
function deleteAuthorizationOpenshiftIoV1ClusterRoleBinding(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 141;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1ClusterRoleBinding($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1ClusterRoleBinding") patchAuthorizationOpenshiftIoV1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterRoleBinding


```php
function patchAuthorizationOpenshiftIoV1ClusterRoleBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1ClusterRoleBinding($body, $name, $pretty);

```


### <a name="list_authorization_openshift_io_v1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1ClusterRole") listAuthorizationOpenshiftIoV1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ClusterRole


```php
function listAuthorizationOpenshiftIoV1ClusterRole(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 141;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1ClusterRole($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1ClusterRole") createAuthorizationOpenshiftIoV1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterRole


```php
function createAuthorizationOpenshiftIoV1ClusterRole(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1ClusterRole($body, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1ClusterRole") getReadAuthorizationOpenshiftIoV1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterRole


```php
function getReadAuthorizationOpenshiftIoV1ClusterRole(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1ClusterRole($name, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1ClusterRole") updateReplaceAuthorizationOpenshiftIoV1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterRole


```php
function updateReplaceAuthorizationOpenshiftIoV1ClusterRole(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1ClusterRole($body, $name, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1ClusterRole") deleteAuthorizationOpenshiftIoV1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterRole


```php
function deleteAuthorizationOpenshiftIoV1ClusterRole(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 233;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1ClusterRole($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1ClusterRole") patchAuthorizationOpenshiftIoV1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterRole


```php
function patchAuthorizationOpenshiftIoV1ClusterRole(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1ClusterRole($body, $name, $pretty);

```


### <a name="create_authorization_openshift_io_v1_local_resource_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1LocalResourceAccessReviewForAllNamespaces") createAuthorizationOpenshiftIoV1LocalResourceAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalResourceAccessReview


```php
function createAuthorizationOpenshiftIoV1LocalResourceAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1LocalResourceAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_openshift_io_v1_local_subject_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1LocalSubjectAccessReviewForAllNamespaces") createAuthorizationOpenshiftIoV1LocalSubjectAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createAuthorizationOpenshiftIoV1LocalSubjectAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1LocalSubjectAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_local_resource_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedLocalResourceAccessReview") createAuthorizationOpenshiftIoV1NamespacedLocalResourceAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalResourceAccessReview


```php
function createAuthorizationOpenshiftIoV1NamespacedLocalResourceAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedLocalResourceAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_local_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedLocalSubjectAccessReview") createAuthorizationOpenshiftIoV1NamespacedLocalSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createAuthorizationOpenshiftIoV1NamespacedLocalSubjectAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedLocalSubjectAccessReview($body, $mnamespace, $pretty);

```


### <a name="list_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1NamespacedPolicy") listAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Policy


```php
function listAuthorizationOpenshiftIoV1NamespacedPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 233;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1NamespacedPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedPolicy") createAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create a Policy


```php
function createAuthorizationOpenshiftIoV1NamespacedPolicy(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedPolicy($body, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_collection_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicy") deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Policy


```php
function deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 69;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1NamespacedPolicy") getReadAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Policy


```php
function getReadAuthorizationOpenshiftIoV1NamespacedPolicy(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1NamespacedPolicy($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicy") updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Policy


```php
function updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1NamespacedPolicy") deleteAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete a Policy


```php
function deleteAuthorizationOpenshiftIoV1NamespacedPolicy(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 69;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1NamespacedPolicy($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1NamespacedPolicy") patchAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Policy


```php
function patchAuthorizationOpenshiftIoV1NamespacedPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1NamespacedPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="list_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1NamespacedPolicyBinding") listAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PolicyBinding


```php
function listAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 28;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1NamespacedPolicyBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedPolicyBinding") createAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a PolicyBinding


```php
function createAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedPolicyBinding($body, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_collection_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicyBinding") deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PolicyBinding


```php
function deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicyBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 28;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1CollectionNamespacedPolicyBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1NamespacedPolicyBinding") getReadAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PolicyBinding


```php
function getReadAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1NamespacedPolicyBinding($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicyBinding") updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PolicyBinding


```php
function updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1NamespacedPolicyBinding($body, $name, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1NamespacedPolicyBinding") deleteAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a PolicyBinding


```php
function deleteAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 119;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1NamespacedPolicyBinding($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1NamespacedPolicyBinding") patchAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PolicyBinding


```php
function patchAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1NamespacedPolicyBinding($body, $name, $mnamespace, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_resource_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedResourceAccessReview") createAuthorizationOpenshiftIoV1NamespacedResourceAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a ResourceAccessReview


```php
function createAuthorizationOpenshiftIoV1NamespacedResourceAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedResourceAccessReview($body, $mnamespace, $pretty);

```


### <a name="list_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") listAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind RoleBindingRestriction


```php
function listAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 119;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") createAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBindingRestriction


```php
function createAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1RoleBindingRestriction();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($body, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_collection_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1CollectionNamespacedRoleBindingRestriction") deleteAuthorizationOpenshiftIoV1CollectionNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of RoleBindingRestriction


```php
function deleteAuthorizationOpenshiftIoV1CollectionNamespacedRoleBindingRestriction(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 211;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1CollectionNamespacedRoleBindingRestriction($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") getReadAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> read the specified RoleBindingRestriction


```php
function getReadAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified RoleBindingRestriction


```php
function updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1RoleBindingRestriction();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($body, $name, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") deleteAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> delete a RoleBindingRestriction


```php
function deleteAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 169;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") patchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified RoleBindingRestriction


```php
function patchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($body, $name, $mnamespace, $pretty);

```


### <a name="list_authorization_openshift_io_v1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1NamespacedRoleBinding") listAuthorizationOpenshiftIoV1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind RoleBinding


```php
function listAuthorizationOpenshiftIoV1NamespacedRoleBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 169;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1NamespacedRoleBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedRoleBinding") createAuthorizationOpenshiftIoV1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBinding


```php
function createAuthorizationOpenshiftIoV1NamespacedRoleBinding(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedRoleBinding($body, $mnamespace, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1NamespacedRoleBinding") getReadAuthorizationOpenshiftIoV1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified RoleBinding


```php
function getReadAuthorizationOpenshiftIoV1NamespacedRoleBinding(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1NamespacedRoleBinding($name, $mnamespace, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBinding") updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified RoleBinding


```php
function updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1NamespacedRoleBinding($body, $name, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1NamespacedRoleBinding") deleteAuthorizationOpenshiftIoV1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a RoleBinding


```php
function deleteAuthorizationOpenshiftIoV1NamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 5;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1NamespacedRoleBinding($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1NamespacedRoleBinding") patchAuthorizationOpenshiftIoV1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified RoleBinding


```php
function patchAuthorizationOpenshiftIoV1NamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1NamespacedRoleBinding($body, $name, $mnamespace, $pretty);

```


### <a name="list_authorization_openshift_io_v1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1NamespacedRole") listAuthorizationOpenshiftIoV1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind Role


```php
function listAuthorizationOpenshiftIoV1NamespacedRole(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 5;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1NamespacedRole($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedRole") createAuthorizationOpenshiftIoV1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> create a Role


```php
function createAuthorizationOpenshiftIoV1NamespacedRole(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedRole($body, $mnamespace, $pretty);

```


### <a name="get_read_authorization_openshift_io_v1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getReadAuthorizationOpenshiftIoV1NamespacedRole") getReadAuthorizationOpenshiftIoV1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Role


```php
function getReadAuthorizationOpenshiftIoV1NamespacedRole(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->getReadAuthorizationOpenshiftIoV1NamespacedRole($name, $mnamespace, $pretty);

```


### <a name="update_replace_authorization_openshift_io_v1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.updateReplaceAuthorizationOpenshiftIoV1NamespacedRole") updateReplaceAuthorizationOpenshiftIoV1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Role


```php
function updateReplaceAuthorizationOpenshiftIoV1NamespacedRole(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->updateReplaceAuthorizationOpenshiftIoV1NamespacedRole($body, $name, $mnamespace, $pretty);

```


### <a name="delete_authorization_openshift_io_v1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.deleteAuthorizationOpenshiftIoV1NamespacedRole") deleteAuthorizationOpenshiftIoV1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> delete a Role


```php
function deleteAuthorizationOpenshiftIoV1NamespacedRole(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 219;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->deleteAuthorizationOpenshiftIoV1NamespacedRole($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_authorization_openshift_io_v1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.patchAuthorizationOpenshiftIoV1NamespacedRole") patchAuthorizationOpenshiftIoV1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Role


```php
function patchAuthorizationOpenshiftIoV1NamespacedRole(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->patchAuthorizationOpenshiftIoV1NamespacedRole($body, $name, $mnamespace, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_self_subject_rules_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedSelfSubjectRulesReview") createAuthorizationOpenshiftIoV1NamespacedSelfSubjectRulesReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SelfSubjectRulesReview


```php
function createAuthorizationOpenshiftIoV1NamespacedSelfSubjectRulesReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SelfSubjectRulesReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedSelfSubjectRulesReview($body, $mnamespace, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedSubjectAccessReview") createAuthorizationOpenshiftIoV1NamespacedSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectAccessReview


```php
function createAuthorizationOpenshiftIoV1NamespacedSubjectAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedSubjectAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_authorization_openshift_io_v1_namespaced_subject_rules_review"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1NamespacedSubjectRulesReview") createAuthorizationOpenshiftIoV1NamespacedSubjectRulesReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectRulesReview


```php
function createAuthorizationOpenshiftIoV1NamespacedSubjectRulesReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SubjectRulesReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1NamespacedSubjectRulesReview($body, $mnamespace, $pretty);

```


### <a name="list_authorization_openshift_io_v1_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1PolicyForAllNamespaces") listAuthorizationOpenshiftIoV1PolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Policy


```php
function listAuthorizationOpenshiftIoV1PolicyForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 55;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1PolicyForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1PolicyForAllNamespaces") createAuthorizationOpenshiftIoV1PolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Policy


```php
function createAuthorizationOpenshiftIoV1PolicyForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1PolicyForAllNamespaces($body, $pretty);

```


### <a name="list_authorization_openshift_io_v1_policy_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces") listAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PolicyBinding


```php
function listAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 55;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_policy_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces") createAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PolicyBinding


```php
function createAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1PolicyBindingForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_openshift_io_v1_resource_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1ResourceAccessReviewForAllNamespaces") createAuthorizationOpenshiftIoV1ResourceAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ResourceAccessReview


```php
function createAuthorizationOpenshiftIoV1ResourceAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1ResourceAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="list_authorization_openshift_io_v1_role_binding_restriction_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces") listAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind RoleBindingRestriction


```php
function listAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 55;
$watch = false;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_role_binding_restriction_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces") createAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBindingRestriction


```php
function createAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1RoleBindingRestriction();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1RoleBindingRestrictionForAllNamespaces($body, $pretty);

```


### <a name="list_authorization_openshift_io_v1_role_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces") listAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind RoleBinding


```php
function listAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 147;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_role_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces") createAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBinding


```php
function createAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1RoleBindingForAllNamespaces($body, $pretty);

```


### <a name="list_authorization_openshift_io_v1_role_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.listAuthorizationOpenshiftIoV1RoleForAllNamespaces") listAuthorizationOpenshiftIoV1RoleForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind Role


```php
function listAuthorizationOpenshiftIoV1RoleForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 147;
$watch = true;
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->listAuthorizationOpenshiftIoV1RoleForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_authorization_openshift_io_v1_role_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1RoleForAllNamespaces") createAuthorizationOpenshiftIoV1RoleForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Role


```php
function createAuthorizationOpenshiftIoV1RoleForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1RoleForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_openshift_io_v1_self_subject_rules_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1SelfSubjectRulesReviewForAllNamespaces") createAuthorizationOpenshiftIoV1SelfSubjectRulesReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a SelfSubjectRulesReview


```php
function createAuthorizationOpenshiftIoV1SelfSubjectRulesReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SelfSubjectRulesReview();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1SelfSubjectRulesReviewForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_openshift_io_v1_subject_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1SubjectAccessReviewForAllNamespaces") createAuthorizationOpenshiftIoV1SubjectAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectAccessReview


```php
function createAuthorizationOpenshiftIoV1SubjectAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1SubjectAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_authorization_openshift_io_v1_subject_rules_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.createAuthorizationOpenshiftIoV1SubjectRulesReviewForAllNamespaces") createAuthorizationOpenshiftIoV1SubjectRulesReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectRulesReview


```php
function createAuthorizationOpenshiftIoV1SubjectRulesReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SubjectRulesReview();
$pretty = 'pretty';

$result = $authorizationOpenshiftIoV1->createAuthorizationOpenshiftIoV1SubjectRulesReviewForAllNamespaces($body, $pretty);

```


### <a name="get_watch_authorization_openshift_io_v1_cluster_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1ClusterPolicyList") getWatchAuthorizationOpenshiftIoV1ClusterPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterPolicy


```php
function getWatchAuthorizationOpenshiftIoV1ClusterPolicyList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 105;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1ClusterPolicyList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1ClusterPolicy") getWatchAuthorizationOpenshiftIoV1ClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterPolicy


```php
function getWatchAuthorizationOpenshiftIoV1ClusterPolicy(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicy |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 105;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1ClusterPolicy($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_cluster_policy_binding_list"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1ClusterPolicyBindingList") getWatchAuthorizationOpenshiftIoV1ClusterPolicyBindingList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterPolicyBinding


```php
function getWatchAuthorizationOpenshiftIoV1ClusterPolicyBindingList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 105;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1ClusterPolicyBindingList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1ClusterPolicyBinding") getWatchAuthorizationOpenshiftIoV1ClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterPolicyBinding


```php
function getWatchAuthorizationOpenshiftIoV1ClusterPolicyBinding(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 105;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1ClusterPolicyBinding($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_namespaced_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1NamespacedPolicyList") getWatchAuthorizationOpenshiftIoV1NamespacedPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Policy


```php
function getWatchAuthorizationOpenshiftIoV1NamespacedPolicyList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1NamespacedPolicyList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1NamespacedPolicy") getWatchAuthorizationOpenshiftIoV1NamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Policy


```php
function getWatchAuthorizationOpenshiftIoV1NamespacedPolicy(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1NamespacedPolicy($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_namespaced_policy_binding_list"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBindingList") getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBindingList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PolicyBinding


```php
function getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBindingList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBindingList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBinding") getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PolicyBinding


```php
function getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBinding(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1NamespacedPolicyBinding($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_namespaced_role_binding_restriction_list"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestrictionList") getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestrictionList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of RoleBindingRestriction


```php
function getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestrictionList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 33;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestrictionList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction") getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind RoleBindingRestriction


```php
function getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 33;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1NamespacedRoleBindingRestriction($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_policy_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1PolicyListForAllNamespaces") getWatchAuthorizationOpenshiftIoV1PolicyListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Policy


```php
function getWatchAuthorizationOpenshiftIoV1PolicyListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 33;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1PolicyListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_policy_binding_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1PolicyBindingListForAllNamespaces") getWatchAuthorizationOpenshiftIoV1PolicyBindingListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PolicyBinding


```php
function getWatchAuthorizationOpenshiftIoV1PolicyBindingListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 33;
$watch = false;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1PolicyBindingListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_authorization_openshift_io_v1_role_binding_restriction_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AuthorizationOpenshiftIoV1Controller.getWatchAuthorizationOpenshiftIoV1RoleBindingRestrictionListForAllNamespaces") getWatchAuthorizationOpenshiftIoV1RoleBindingRestrictionListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of RoleBindingRestriction


```php
function getWatchAuthorizationOpenshiftIoV1RoleBindingRestrictionListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 246;
$watch = true;

$result = $authorizationOpenshiftIoV1->getWatchAuthorizationOpenshiftIoV1RoleBindingRestrictionListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="autoscaling_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AutoscalingController") AutoscalingController

### Get singleton instance

The singleton instance of the ``` AutoscalingController ``` class can be accessed from the API Client.

```php
$autoscaling = $client->getAutoscaling();
```

### <a name="get_autoscaling_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingController.getAutoscalingAPIGroup") getAutoscalingAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getAutoscalingAPIGroup()
```

#### Example Usage

```php

$result = $autoscaling->getAutoscalingAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="autoscaling_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AutoscalingV1Controller") AutoscalingV1Controller

### Get singleton instance

The singleton instance of the ``` AutoscalingV1Controller ``` class can be accessed from the API Client.

```php
$autoscalingV1 = $client->getAutoscalingV1();
```

### <a name="get_autoscaling_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.getAutoscalingV1APIResources") getAutoscalingV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAutoscalingV1APIResources()
```

#### Example Usage

```php

$result = $autoscalingV1->getAutoscalingV1APIResources();

```


### <a name="list_autoscaling_v1_horizontal_pod_autoscaler_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.listAutoscalingV1HorizontalPodAutoscalerForAllNamespaces") listAutoscalingV1HorizontalPodAutoscalerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HorizontalPodAutoscaler


```php
function listAutoscalingV1HorizontalPodAutoscalerForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 246;
$watch = true;
$pretty = 'pretty';

$result = $autoscalingV1->listAutoscalingV1HorizontalPodAutoscalerForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_autoscaling_v1_horizontal_pod_autoscaler_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.createAutoscalingV1HorizontalPodAutoscalerForAllNamespaces") createAutoscalingV1HorizontalPodAutoscalerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a HorizontalPodAutoscaler


```php
function createAutoscalingV1HorizontalPodAutoscalerForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HorizontalPodAutoscaler();
$pretty = 'pretty';

$result = $autoscalingV1->createAutoscalingV1HorizontalPodAutoscalerForAllNamespaces($body, $pretty);

```


### <a name="list_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.listAutoscalingV1NamespacedHorizontalPodAutoscaler") listAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HorizontalPodAutoscaler


```php
function listAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 246;
$watch = true;
$pretty = 'pretty';

$result = $autoscalingV1->listAutoscalingV1NamespacedHorizontalPodAutoscaler($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.createAutoscalingV1NamespacedHorizontalPodAutoscaler") createAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> create a HorizontalPodAutoscaler


```php
function createAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HorizontalPodAutoscaler();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV1->createAutoscalingV1NamespacedHorizontalPodAutoscaler($body, $mnamespace, $pretty);

```


### <a name="delete_autoscaling_v1_collection_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscaler") deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of HorizontalPodAutoscaler


```php
function deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscaler(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 83;
$watch = false;
$pretty = 'pretty';

$result = $autoscalingV1->deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscaler($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.getReadAutoscalingV1NamespacedHorizontalPodAutoscaler") getReadAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> read the specified HorizontalPodAutoscaler


```php
function getReadAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $autoscalingV1->getReadAutoscalingV1NamespacedHorizontalPodAutoscaler($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscaler") updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified HorizontalPodAutoscaler


```php
function updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HorizontalPodAutoscaler();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV1->updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $pretty);

```


### <a name="delete_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.deleteAutoscalingV1NamespacedHorizontalPodAutoscaler") deleteAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> delete a HorizontalPodAutoscaler


```php
function deleteAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 83;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $autoscalingV1->deleteAutoscalingV1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.patchAutoscalingV1NamespacedHorizontalPodAutoscaler") patchAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified HorizontalPodAutoscaler


```php
function patchAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV1->patchAutoscalingV1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_autoscaling_v1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.getReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatus") getReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified HorizontalPodAutoscaler


```php
function getReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV1->getReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_autoscaling_v1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatus") updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified HorizontalPodAutoscaler


```php
function updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HorizontalPodAutoscaler();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV1->updateReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_autoscaling_v1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatus") patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified HorizontalPodAutoscaler


```php
function patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV1->patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="get_watch_autoscaling_v1_horizontal_pod_autoscaler_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.getWatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespaces") getWatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HorizontalPodAutoscaler


```php
function getWatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 41;
$watch = false;

$result = $autoscalingV1->getWatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_autoscaling_v1_namespaced_horizontal_pod_autoscaler_list"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.getWatchAutoscalingV1NamespacedHorizontalPodAutoscalerList") getWatchAutoscalingV1NamespacedHorizontalPodAutoscalerList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HorizontalPodAutoscaler


```php
function getWatchAutoscalingV1NamespacedHorizontalPodAutoscalerList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 41;
$watch = false;

$result = $autoscalingV1->getWatchAutoscalingV1NamespacedHorizontalPodAutoscalerList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_autoscaling_v1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV1Controller.getWatchAutoscalingV1NamespacedHorizontalPodAutoscaler") getWatchAutoscalingV1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind HorizontalPodAutoscaler


```php
function getWatchAutoscalingV1NamespacedHorizontalPodAutoscaler(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 41;
$watch = false;

$result = $autoscalingV1->getWatchAutoscalingV1NamespacedHorizontalPodAutoscaler($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="autoscaling_v2alpha1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".AutoscalingV2alpha1Controller") AutoscalingV2alpha1Controller

### Get singleton instance

The singleton instance of the ``` AutoscalingV2alpha1Controller ``` class can be accessed from the API Client.

```php
$autoscalingV2alpha1 = $client->getAutoscalingV2alpha1();
```

### <a name="get_autoscaling_v2alpha1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.getAutoscalingV2alpha1APIResources") getAutoscalingV2alpha1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAutoscalingV2alpha1APIResources()
```

#### Example Usage

```php

$result = $autoscalingV2alpha1->getAutoscalingV2alpha1APIResources();

```


### <a name="list_autoscaling_v2alpha1_horizontal_pod_autoscaler_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.listAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces") listAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HorizontalPodAutoscaler


```php
function listAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 133;
$watch = true;
$pretty = 'pretty';

$result = $autoscalingV2alpha1->listAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_autoscaling_v2alpha1_horizontal_pod_autoscaler_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.createAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces") createAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a HorizontalPodAutoscaler


```php
function createAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1HorizontalPodAutoscaler();
$pretty = 'pretty';

$result = $autoscalingV2alpha1->createAutoscalingV2alpha1HorizontalPodAutoscalerForAllNamespaces($body, $pretty);

```


### <a name="list_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.listAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") listAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HorizontalPodAutoscaler


```php
function listAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 133;
$watch = true;
$pretty = 'pretty';

$result = $autoscalingV2alpha1->listAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.createAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") createAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> create a HorizontalPodAutoscaler


```php
function createAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1HorizontalPodAutoscaler();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->createAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($body, $mnamespace, $pretty);

```


### <a name="delete_autoscaling_v2alpha1_collection_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.deleteAutoscalingV2alpha1CollectionNamespacedHorizontalPodAutoscaler") deleteAutoscalingV2alpha1CollectionNamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of HorizontalPodAutoscaler


```php
function deleteAutoscalingV2alpha1CollectionNamespacedHorizontalPodAutoscaler(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 224;
$watch = true;
$pretty = 'pretty';

$result = $autoscalingV2alpha1->deleteAutoscalingV2alpha1CollectionNamespacedHorizontalPodAutoscaler($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> read the specified HorizontalPodAutoscaler


```php
function getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $autoscalingV2alpha1->getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified HorizontalPodAutoscaler


```php
function updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1HorizontalPodAutoscaler();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $pretty);

```


### <a name="delete_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.deleteAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") deleteAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> delete a HorizontalPodAutoscaler


```php
function deleteAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 224;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->deleteAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified HorizontalPodAutoscaler


```php
function patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus") getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified HorizontalPodAutoscaler


```php
function getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->getReadAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus") updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified HorizontalPodAutoscaler


```php
function updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1HorizontalPodAutoscaler();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->updateReplaceAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus") patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified HorizontalPodAutoscaler


```php
function patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $autoscalingV2alpha1->patchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="get_watch_autoscaling_v2alpha1_horizontal_pod_autoscaler_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.getWatchAutoscalingV2alpha1HorizontalPodAutoscalerListForAllNamespaces") getWatchAutoscalingV2alpha1HorizontalPodAutoscalerListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HorizontalPodAutoscaler


```php
function getWatchAutoscalingV2alpha1HorizontalPodAutoscalerListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 182;
$watch = true;

$result = $autoscalingV2alpha1->getWatchAutoscalingV2alpha1HorizontalPodAutoscalerListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler_list"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerList") getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HorizontalPodAutoscaler


```php
function getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 182;
$watch = true;

$result = $autoscalingV2alpha1->getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscalerList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_autoscaling_v2alpha1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".AutoscalingV2alpha1Controller.getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler") getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind HorizontalPodAutoscaler


```php
function getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 19;
$watch = false;

$result = $autoscalingV2alpha1->getWatchAutoscalingV2alpha1NamespacedHorizontalPodAutoscaler($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="batch_controller"></a>![Class: ](https://apidocs.io/img/class.png ".BatchController") BatchController

### Get singleton instance

The singleton instance of the ``` BatchController ``` class can be accessed from the API Client.

```php
$batch = $client->getBatch();
```

### <a name="get_batch_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".BatchController.getBatchAPIGroup") getBatchAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getBatchAPIGroup()
```

#### Example Usage

```php

$result = $batch->getBatchAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="batch_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".BatchV1Controller") BatchV1Controller

### Get singleton instance

The singleton instance of the ``` BatchV1Controller ``` class can be accessed from the API Client.

```php
$batchV1 = $client->getBatchV1();
```

### <a name="get_batch_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.getBatchV1APIResources") getBatchV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getBatchV1APIResources()
```

#### Example Usage

```php

$result = $batchV1->getBatchV1APIResources();

```


### <a name="list_batch_v1_job_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.listBatchV1JobForAllNamespaces") listBatchV1JobForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Job


```php
function listBatchV1JobForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 19;
$watch = false;
$pretty = 'pretty';

$result = $batchV1->listBatchV1JobForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_batch_v1_job_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.createBatchV1JobForAllNamespaces") createBatchV1JobForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Job


```php
function createBatchV1JobForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Job();
$pretty = 'pretty';

$result = $batchV1->createBatchV1JobForAllNamespaces($body, $pretty);

```


### <a name="list_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.listBatchV1NamespacedJob") listBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Job


```php
function listBatchV1NamespacedJob(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 19;
$watch = false;
$pretty = 'pretty';

$result = $batchV1->listBatchV1NamespacedJob($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.createBatchV1NamespacedJob") createBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> create a Job


```php
function createBatchV1NamespacedJob(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Job();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV1->createBatchV1NamespacedJob($body, $mnamespace, $pretty);

```


### <a name="delete_batch_v1_collection_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.deleteBatchV1CollectionNamespacedJob") deleteBatchV1CollectionNamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Job


```php
function deleteBatchV1CollectionNamespacedJob(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 19;
$watch = false;
$pretty = 'pretty';

$result = $batchV1->deleteBatchV1CollectionNamespacedJob($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.getReadBatchV1NamespacedJob") getReadBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Job


```php
function getReadBatchV1NamespacedJob(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $batchV1->getReadBatchV1NamespacedJob($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.updateReplaceBatchV1NamespacedJob") updateReplaceBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Job


```php
function updateReplaceBatchV1NamespacedJob(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Job();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV1->updateReplaceBatchV1NamespacedJob($body, $name, $mnamespace, $pretty);

```


### <a name="delete_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.deleteBatchV1NamespacedJob") deleteBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> delete a Job


```php
function deleteBatchV1NamespacedJob(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 110;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $batchV1->deleteBatchV1NamespacedJob($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.patchBatchV1NamespacedJob") patchBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Job


```php
function patchBatchV1NamespacedJob(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV1->patchBatchV1NamespacedJob($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_batch_v1_namespaced_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.getReadBatchV1NamespacedJobStatus") getReadBatchV1NamespacedJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Job


```php
function getReadBatchV1NamespacedJobStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV1->getReadBatchV1NamespacedJobStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_batch_v1_namespaced_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.updateReplaceBatchV1NamespacedJobStatus") updateReplaceBatchV1NamespacedJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Job


```php
function updateReplaceBatchV1NamespacedJobStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Job();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV1->updateReplaceBatchV1NamespacedJobStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_batch_v1_namespaced_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.patchBatchV1NamespacedJobStatus") patchBatchV1NamespacedJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Job


```php
function patchBatchV1NamespacedJobStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV1->patchBatchV1NamespacedJobStatus($body, $name, $mnamespace, $pretty);

```


### <a name="get_watch_batch_v1_job_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.getWatchBatchV1JobListForAllNamespaces") getWatchBatchV1JobListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Job


```php
function getWatchBatchV1JobListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 69;
$watch = false;

$result = $batchV1->getWatchBatchV1JobListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v1_namespaced_job_list"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.getWatchBatchV1NamespacedJobList") getWatchBatchV1NamespacedJobList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Job


```php
function getWatchBatchV1NamespacedJobList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 69;
$watch = false;

$result = $batchV1->getWatchBatchV1NamespacedJobList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v1_namespaced_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV1Controller.getWatchBatchV1NamespacedJob") getWatchBatchV1NamespacedJob

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Job


```php
function getWatchBatchV1NamespacedJob(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Job |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 69;
$watch = false;

$result = $batchV1->getWatchBatchV1NamespacedJob($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="batch_v2alpha1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".BatchV2alpha1Controller") BatchV2alpha1Controller

### Get singleton instance

The singleton instance of the ``` BatchV2alpha1Controller ``` class can be accessed from the API Client.

```php
$batchV2alpha1 = $client->getBatchV2alpha1();
```

### <a name="get_batch_v2alpha1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getBatchV2alpha1APIResources") getBatchV2alpha1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getBatchV2alpha1APIResources()
```

#### Example Usage

```php

$result = $batchV2alpha1->getBatchV2alpha1APIResources();

```


### <a name="list_batch_v2alpha1_cron_job_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.listBatchV2alpha1CronJobForAllNamespaces") listBatchV2alpha1CronJobForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind CronJob


```php
function listBatchV2alpha1CronJobForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 160;
$watch = true;
$pretty = 'pretty';

$result = $batchV2alpha1->listBatchV2alpha1CronJobForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_batch_v2alpha1_cron_job_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.createBatchV2alpha1CronJobForAllNamespaces") createBatchV2alpha1CronJobForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a CronJob


```php
function createBatchV2alpha1CronJobForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$pretty = 'pretty';

$result = $batchV2alpha1->createBatchV2alpha1CronJobForAllNamespaces($body, $pretty);

```


### <a name="list_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.listBatchV2alpha1NamespacedCronJob") listBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind CronJob


```php
function listBatchV2alpha1NamespacedCronJob(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 160;
$watch = true;
$pretty = 'pretty';

$result = $batchV2alpha1->listBatchV2alpha1NamespacedCronJob($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.createBatchV2alpha1NamespacedCronJob") createBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> create a CronJob


```php
function createBatchV2alpha1NamespacedCronJob(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->createBatchV2alpha1NamespacedCronJob($body, $mnamespace, $pretty);

```


### <a name="delete_batch_v2alpha1_collection_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.deleteBatchV2alpha1CollectionNamespacedCronJob") deleteBatchV2alpha1CollectionNamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of CronJob


```php
function deleteBatchV2alpha1CollectionNamespacedCronJob(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 118;
$watch = false;
$pretty = 'pretty';

$result = $batchV2alpha1->deleteBatchV2alpha1CollectionNamespacedCronJob($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getReadBatchV2alpha1NamespacedCronJob") getReadBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> read the specified CronJob


```php
function getReadBatchV2alpha1NamespacedCronJob(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $batchV2alpha1->getReadBatchV2alpha1NamespacedCronJob($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.updateReplaceBatchV2alpha1NamespacedCronJob") updateReplaceBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified CronJob


```php
function updateReplaceBatchV2alpha1NamespacedCronJob(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->updateReplaceBatchV2alpha1NamespacedCronJob($body, $name, $mnamespace, $pretty);

```


### <a name="delete_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.deleteBatchV2alpha1NamespacedCronJob") deleteBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> delete a CronJob


```php
function deleteBatchV2alpha1NamespacedCronJob(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 118;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $batchV2alpha1->deleteBatchV2alpha1NamespacedCronJob($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.patchBatchV2alpha1NamespacedCronJob") patchBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified CronJob


```php
function patchBatchV2alpha1NamespacedCronJob(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->patchBatchV2alpha1NamespacedCronJob($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_batch_v2alpha1_namespaced_cron_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getReadBatchV2alpha1NamespacedCronJobStatus") getReadBatchV2alpha1NamespacedCronJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified CronJob


```php
function getReadBatchV2alpha1NamespacedCronJobStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->getReadBatchV2alpha1NamespacedCronJobStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_batch_v2alpha1_namespaced_cron_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.updateReplaceBatchV2alpha1NamespacedCronJobStatus") updateReplaceBatchV2alpha1NamespacedCronJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified CronJob


```php
function updateReplaceBatchV2alpha1NamespacedCronJobStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->updateReplaceBatchV2alpha1NamespacedCronJobStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_batch_v2alpha1_namespaced_cron_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.patchBatchV2alpha1NamespacedCronJobStatus") patchBatchV2alpha1NamespacedCronJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified CronJob


```php
function patchBatchV2alpha1NamespacedCronJobStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->patchBatchV2alpha1NamespacedCronJobStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.listBatchV2alpha1NamespacedScheduledJob") listBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ScheduledJob


```php
function listBatchV2alpha1NamespacedScheduledJob(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 210;
$watch = true;
$pretty = 'pretty';

$result = $batchV2alpha1->listBatchV2alpha1NamespacedScheduledJob($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.createBatchV2alpha1NamespacedScheduledJob") createBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> create a ScheduledJob


```php
function createBatchV2alpha1NamespacedScheduledJob(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->createBatchV2alpha1NamespacedScheduledJob($body, $mnamespace, $pretty);

```


### <a name="delete_batch_v2alpha1_collection_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.deleteBatchV2alpha1CollectionNamespacedScheduledJob") deleteBatchV2alpha1CollectionNamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ScheduledJob


```php
function deleteBatchV2alpha1CollectionNamespacedScheduledJob(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 210;
$watch = true;
$pretty = 'pretty';

$result = $batchV2alpha1->deleteBatchV2alpha1CollectionNamespacedScheduledJob($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getReadBatchV2alpha1NamespacedScheduledJob") getReadBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ScheduledJob


```php
function getReadBatchV2alpha1NamespacedScheduledJob(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $batchV2alpha1->getReadBatchV2alpha1NamespacedScheduledJob($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.updateReplaceBatchV2alpha1NamespacedScheduledJob") updateReplaceBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ScheduledJob


```php
function updateReplaceBatchV2alpha1NamespacedScheduledJob(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->updateReplaceBatchV2alpha1NamespacedScheduledJob($body, $name, $mnamespace, $pretty);

```


### <a name="delete_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.deleteBatchV2alpha1NamespacedScheduledJob") deleteBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> delete a ScheduledJob


```php
function deleteBatchV2alpha1NamespacedScheduledJob(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 46;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $batchV2alpha1->deleteBatchV2alpha1NamespacedScheduledJob($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.patchBatchV2alpha1NamespacedScheduledJob") patchBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ScheduledJob


```php
function patchBatchV2alpha1NamespacedScheduledJob(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->patchBatchV2alpha1NamespacedScheduledJob($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_batch_v2alpha1_namespaced_scheduled_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getReadBatchV2alpha1NamespacedScheduledJobStatus") getReadBatchV2alpha1NamespacedScheduledJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ScheduledJob


```php
function getReadBatchV2alpha1NamespacedScheduledJobStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->getReadBatchV2alpha1NamespacedScheduledJobStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_batch_v2alpha1_namespaced_scheduled_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.updateReplaceBatchV2alpha1NamespacedScheduledJobStatus") updateReplaceBatchV2alpha1NamespacedScheduledJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ScheduledJob


```php
function updateReplaceBatchV2alpha1NamespacedScheduledJobStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->updateReplaceBatchV2alpha1NamespacedScheduledJobStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_batch_v2alpha1_namespaced_scheduled_job_status"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.patchBatchV2alpha1NamespacedScheduledJobStatus") patchBatchV2alpha1NamespacedScheduledJobStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ScheduledJob


```php
function patchBatchV2alpha1NamespacedScheduledJobStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $batchV2alpha1->patchBatchV2alpha1NamespacedScheduledJobStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_batch_v2alpha1_scheduled_job_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.listBatchV2alpha1ScheduledJobForAllNamespaces") listBatchV2alpha1ScheduledJobForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ScheduledJob


```php
function listBatchV2alpha1ScheduledJobForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 5;
$watch = false;
$pretty = 'pretty';

$result = $batchV2alpha1->listBatchV2alpha1ScheduledJobForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_batch_v2alpha1_scheduled_job_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.createBatchV2alpha1ScheduledJobForAllNamespaces") createBatchV2alpha1ScheduledJobForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ScheduledJob


```php
function createBatchV2alpha1ScheduledJobForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V2alpha1CronJob();
$pretty = 'pretty';

$result = $batchV2alpha1->createBatchV2alpha1ScheduledJobForAllNamespaces($body, $pretty);

```


### <a name="get_watch_batch_v2alpha1_cron_job_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getWatchBatchV2alpha1CronJobListForAllNamespaces") getWatchBatchV2alpha1CronJobListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of CronJob


```php
function getWatchBatchV2alpha1CronJobListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 5;
$watch = false;

$result = $batchV2alpha1->getWatchBatchV2alpha1CronJobListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v2alpha1_namespaced_cron_job_list"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getWatchBatchV2alpha1NamespacedCronJobList") getWatchBatchV2alpha1NamespacedCronJobList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of CronJob


```php
function getWatchBatchV2alpha1NamespacedCronJobList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 5;
$watch = false;

$result = $batchV2alpha1->getWatchBatchV2alpha1NamespacedCronJobList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v2alpha1_namespaced_cron_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getWatchBatchV2alpha1NamespacedCronJob") getWatchBatchV2alpha1NamespacedCronJob

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind CronJob


```php
function getWatchBatchV2alpha1NamespacedCronJob(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the CronJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 96;
$watch = false;

$result = $batchV2alpha1->getWatchBatchV2alpha1NamespacedCronJob($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v2alpha1_namespaced_scheduled_job_list"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getWatchBatchV2alpha1NamespacedScheduledJobList") getWatchBatchV2alpha1NamespacedScheduledJobList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ScheduledJob


```php
function getWatchBatchV2alpha1NamespacedScheduledJobList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 96;
$watch = false;

$result = $batchV2alpha1->getWatchBatchV2alpha1NamespacedScheduledJobList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v2alpha1_namespaced_scheduled_job"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getWatchBatchV2alpha1NamespacedScheduledJob") getWatchBatchV2alpha1NamespacedScheduledJob

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ScheduledJob


```php
function getWatchBatchV2alpha1NamespacedScheduledJob(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ScheduledJob |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 96;
$watch = false;

$result = $batchV2alpha1->getWatchBatchV2alpha1NamespacedScheduledJob($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_batch_v2alpha1_scheduled_job_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BatchV2alpha1Controller.getWatchBatchV2alpha1ScheduledJobListForAllNamespaces") getWatchBatchV2alpha1ScheduledJobListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ScheduledJob


```php
function getWatchBatchV2alpha1ScheduledJobListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 188;
$watch = true;

$result = $batchV2alpha1->getWatchBatchV2alpha1ScheduledJobListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="build_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".BuildOpenshiftIoController") BuildOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` BuildOpenshiftIoController ``` class can be accessed from the API Client.

```php
$buildOpenshiftIo = $client->getBuildOpenshiftIo();
```

### <a name="get_build_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoController.getBuildOpenshiftIoAPIGroup") getBuildOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getBuildOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $buildOpenshiftIo->getBuildOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="build_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".BuildOpenshiftIoV1Controller") BuildOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` BuildOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$buildOpenshiftIoV1 = $client->getBuildOpenshiftIoV1();
```

### <a name="get_build_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getBuildOpenshiftIoV1APIResources") getBuildOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getBuildOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $buildOpenshiftIoV1->getBuildOpenshiftIoV1APIResources();

```


### <a name="list_build_openshift_io_v1_build_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.listBuildOpenshiftIoV1BuildConfigForAllNamespaces") listBuildOpenshiftIoV1BuildConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind BuildConfig


```php
function listBuildOpenshiftIoV1BuildConfigForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 188;
$watch = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->listBuildOpenshiftIoV1BuildConfigForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_build_openshift_io_v1_build_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createBuildOpenshiftIoV1BuildConfigForAllNamespaces") createBuildOpenshiftIoV1BuildConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a BuildConfig


```php
function createBuildOpenshiftIoV1BuildConfigForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildConfig();
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->createBuildOpenshiftIoV1BuildConfigForAllNamespaces($body, $pretty);

```


### <a name="list_build_openshift_io_v1_build_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.listBuildOpenshiftIoV1BuildForAllNamespaces") listBuildOpenshiftIoV1BuildForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Build


```php
function listBuildOpenshiftIoV1BuildForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 188;
$watch = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->listBuildOpenshiftIoV1BuildForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_build_openshift_io_v1_build_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createBuildOpenshiftIoV1BuildForAllNamespaces") createBuildOpenshiftIoV1BuildForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Build


```php
function createBuildOpenshiftIoV1BuildForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->createBuildOpenshiftIoV1BuildForAllNamespaces($body, $pretty);

```


### <a name="list_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.listBuildOpenshiftIoV1NamespacedBuildConfig") listBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind BuildConfig


```php
function listBuildOpenshiftIoV1NamespacedBuildConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 146;
$watch = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->listBuildOpenshiftIoV1NamespacedBuildConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createBuildOpenshiftIoV1NamespacedBuildConfig") createBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> create a BuildConfig


```php
function createBuildOpenshiftIoV1NamespacedBuildConfig(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildConfig();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->createBuildOpenshiftIoV1NamespacedBuildConfig($body, $mnamespace, $pretty);

```


### <a name="delete_build_openshift_io_v1_collection_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.deleteBuildOpenshiftIoV1CollectionNamespacedBuildConfig") deleteBuildOpenshiftIoV1CollectionNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of BuildConfig


```php
function deleteBuildOpenshiftIoV1CollectionNamespacedBuildConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 146;
$watch = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->deleteBuildOpenshiftIoV1CollectionNamespacedBuildConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getReadBuildOpenshiftIoV1NamespacedBuildConfig") getReadBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> read the specified BuildConfig


```php
function getReadBuildOpenshiftIoV1NamespacedBuildConfig(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->getReadBuildOpenshiftIoV1NamespacedBuildConfig($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.updateReplaceBuildOpenshiftIoV1NamespacedBuildConfig") updateReplaceBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified BuildConfig


```php
function updateReplaceBuildOpenshiftIoV1NamespacedBuildConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildConfig();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->updateReplaceBuildOpenshiftIoV1NamespacedBuildConfig($body, $name, $mnamespace, $pretty);

```


### <a name="delete_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.deleteBuildOpenshiftIoV1NamespacedBuildConfig") deleteBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete a BuildConfig


```php
function deleteBuildOpenshiftIoV1NamespacedBuildConfig(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 237;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->deleteBuildOpenshiftIoV1NamespacedBuildConfig($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.patchBuildOpenshiftIoV1NamespacedBuildConfig") patchBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified BuildConfig


```php
function patchBuildOpenshiftIoV1NamespacedBuildConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->patchBuildOpenshiftIoV1NamespacedBuildConfig($body, $name, $mnamespace, $pretty);

```


### <a name="create_build_openshift_io_v1_namespaced_build_request_instantiate"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createBuildOpenshiftIoV1NamespacedBuildRequestInstantiate") createBuildOpenshiftIoV1NamespacedBuildRequestInstantiate

> *Tags:*  ``` Skips Authentication ``` 

> create instantiate of a BuildRequest


```php
function createBuildOpenshiftIoV1NamespacedBuildRequestInstantiate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildRequest |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildRequest();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->createBuildOpenshiftIoV1NamespacedBuildRequestInstantiate($body, $name, $mnamespace, $pretty);

```


### <a name="create_connect_build_openshift_io_v1_post_namespaced_binary_build_request_options_instantiatebinary"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createConnectBuildOpenshiftIoV1PostNamespacedBinaryBuildRequestOptionsInstantiatebinary") createConnectBuildOpenshiftIoV1PostNamespacedBinaryBuildRequestOptionsInstantiatebinary

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to instantiatebinary of BinaryBuildRequestOptions


```php
function createConnectBuildOpenshiftIoV1PostNamespacedBinaryBuildRequestOptionsInstantiatebinary(
        $name,
        $mnamespace,
        $asFile = null,
        $revisionAuthorEmail = null,
        $revisionAuthorName = null,
        $revisionCommit = null,
        $revisionCommitterEmail = null,
        $revisionCommitterName = null,
        $revisionMessage = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BinaryBuildRequestOptions |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| asFile |  ``` Optional ```  | asFile determines if the binary should be created as a file within the source rather than extracted as an archive |
| revisionAuthorEmail |  ``` Optional ```  | revision.authorEmail of the source control user |
| revisionAuthorName |  ``` Optional ```  | revision.authorName of the source control user |
| revisionCommit |  ``` Optional ```  | revision.commit is the value identifying a specific commit |
| revisionCommitterEmail |  ``` Optional ```  | revision.committerEmail of the source control user |
| revisionCommitterName |  ``` Optional ```  | revision.committerName of the source control user |
| revisionMessage |  ``` Optional ```  | revision.message is the description of a specific commit |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$asFile = 'asFile';
$revisionAuthorEmail = 'revision.authorEmail';
$revisionAuthorName = 'revision.authorName';
$revisionCommit = 'revision.commit';
$revisionCommitterEmail = 'revision.committerEmail';
$revisionCommitterName = 'revision.committerName';
$revisionMessage = 'revision.message';

$result = $buildOpenshiftIoV1->createConnectBuildOpenshiftIoV1PostNamespacedBinaryBuildRequestOptionsInstantiatebinary($name, $mnamespace, $asFile, $revisionAuthorEmail, $revisionAuthorName, $revisionCommit, $revisionCommitterEmail, $revisionCommitterName, $revisionMessage);

```


### <a name="create_connect_build_openshift_io_v1_post_namespaced_build_webhooks"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooks") createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooks

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to webhooks of Build


```php
function createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooks(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to pod. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $buildOpenshiftIoV1->createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooks($name, $mnamespace, $path);

```


### <a name="create_connect_build_openshift_io_v1_post_namespaced_build_webhooks_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooksWithPath") createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooksWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to webhooks of Build


```php
function createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooksWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $buildOpenshiftIoV1->createConnectBuildOpenshiftIoV1PostNamespacedBuildWebhooksWithPath($name, $mnamespace, $path);

```


### <a name="list_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.listBuildOpenshiftIoV1NamespacedBuild") listBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Build


```php
function listBuildOpenshiftIoV1NamespacedBuild(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->listBuildOpenshiftIoV1NamespacedBuild($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createBuildOpenshiftIoV1NamespacedBuild") createBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> create a Build


```php
function createBuildOpenshiftIoV1NamespacedBuild(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->createBuildOpenshiftIoV1NamespacedBuild($body, $mnamespace, $pretty);

```


### <a name="delete_build_openshift_io_v1_collection_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.deleteBuildOpenshiftIoV1CollectionNamespacedBuild") deleteBuildOpenshiftIoV1CollectionNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Build


```php
function deleteBuildOpenshiftIoV1CollectionNamespacedBuild(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->deleteBuildOpenshiftIoV1CollectionNamespacedBuild($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getReadBuildOpenshiftIoV1NamespacedBuild") getReadBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Build


```php
function getReadBuildOpenshiftIoV1NamespacedBuild(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->getReadBuildOpenshiftIoV1NamespacedBuild($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.updateReplaceBuildOpenshiftIoV1NamespacedBuild") updateReplaceBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Build


```php
function updateReplaceBuildOpenshiftIoV1NamespacedBuild(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->updateReplaceBuildOpenshiftIoV1NamespacedBuild($body, $name, $mnamespace, $pretty);

```


### <a name="delete_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.deleteBuildOpenshiftIoV1NamespacedBuild") deleteBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> delete a Build


```php
function deleteBuildOpenshiftIoV1NamespacedBuild(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 196;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->deleteBuildOpenshiftIoV1NamespacedBuild($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.patchBuildOpenshiftIoV1NamespacedBuild") patchBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Build


```php
function patchBuildOpenshiftIoV1NamespacedBuild(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->patchBuildOpenshiftIoV1NamespacedBuild($body, $name, $mnamespace, $pretty);

```


### <a name="create_build_openshift_io_v1_namespaced_build_request_clone"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.createBuildOpenshiftIoV1NamespacedBuildRequestClone") createBuildOpenshiftIoV1NamespacedBuildRequestClone

> *Tags:*  ``` Skips Authentication ``` 

> create clone of a BuildRequest


```php
function createBuildOpenshiftIoV1NamespacedBuildRequestClone(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildRequest |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildRequest();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->createBuildOpenshiftIoV1NamespacedBuildRequestClone($body, $name, $mnamespace, $pretty);

```


### <a name="update_replace_build_openshift_io_v1_namespaced_build_details"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.updateReplaceBuildOpenshiftIoV1NamespacedBuildDetails") updateReplaceBuildOpenshiftIoV1NamespacedBuildDetails

> *Tags:*  ``` Skips Authentication ``` 

> replace details of the specified Build


```php
function updateReplaceBuildOpenshiftIoV1NamespacedBuildDetails(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $buildOpenshiftIoV1->updateReplaceBuildOpenshiftIoV1NamespacedBuildDetails($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_build_openshift_io_v1_namespaced_build_log_log"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getReadBuildOpenshiftIoV1NamespacedBuildLogLog") getReadBuildOpenshiftIoV1NamespacedBuildLogLog

> *Tags:*  ``` Skips Authentication ``` 

> read log of the specified BuildLog


```php
function getReadBuildOpenshiftIoV1NamespacedBuildLogLog(
        $name,
        $mnamespace,
        $container = null,
        $follow = null,
        $limitBytes = null,
        $nowait = null,
        $pretty = null,
        $previous = null,
        $sinceSeconds = null,
        $tailLines = null,
        $timestamps = null,
        $version = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BuildLog |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | cointainer for which to stream logs. Defaults to only container if there is one container in the pod. |
| follow |  ``` Optional ```  | follow if true indicates that the build log should be streamed until the build terminates. |
| limitBytes |  ``` Optional ```  | limitBytes, If set, is the number of bytes to read from the server before terminating the log output. This may not display a complete final line of logging, and may return slightly more or slightly less than the specified limit. |
| nowait |  ``` Optional ```  | noWait if true causes the call to return immediately even if the build is not available yet. Otherwise the server will wait until the build has started. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| previous |  ``` Optional ```  | previous returns previous build logs. Defaults to false. |
| sinceSeconds |  ``` Optional ```  | sinceSeconds is a relative time in seconds before the current time from which to show logs. If this value precedes the time a pod was started, only logs since the pod start will be returned. If this value is in the future, no logs will be returned. Only one of sinceSeconds or sinceTime may be specified. |
| tailLines |  ``` Optional ```  | tailLines, If set, is the number of lines from the end of the logs to show. If not specified, logs are shown from the creation of the container or sinceSeconds or sinceTime |
| timestamps |  ``` Optional ```  | timestamps, If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line of log output. Defaults to false. |
| version |  ``` Optional ```  | version of the build for which to view logs. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$follow = false;
$limitBytes = 32;
$nowait = false;
$pretty = 'pretty';
$previous = false;
$sinceSeconds = 32;
$tailLines = 32;
$timestamps = false;
$version = 32;

$result = $buildOpenshiftIoV1->getReadBuildOpenshiftIoV1NamespacedBuildLogLog($name, $mnamespace, $container, $follow, $limitBytes, $nowait, $pretty, $previous, $sinceSeconds, $tailLines, $timestamps, $version);

```


### <a name="get_watch_build_openshift_io_v1_build_config_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getWatchBuildOpenshiftIoV1BuildConfigListForAllNamespaces") getWatchBuildOpenshiftIoV1BuildConfigListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of BuildConfig


```php
function getWatchBuildOpenshiftIoV1BuildConfigListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 32;
$watch = false;

$result = $buildOpenshiftIoV1->getWatchBuildOpenshiftIoV1BuildConfigListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_build_openshift_io_v1_build_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getWatchBuildOpenshiftIoV1BuildListForAllNamespaces") getWatchBuildOpenshiftIoV1BuildListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Build


```php
function getWatchBuildOpenshiftIoV1BuildListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 124;
$watch = false;

$result = $buildOpenshiftIoV1->getWatchBuildOpenshiftIoV1BuildListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_build_openshift_io_v1_namespaced_build_config_list"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getWatchBuildOpenshiftIoV1NamespacedBuildConfigList") getWatchBuildOpenshiftIoV1NamespacedBuildConfigList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of BuildConfig


```php
function getWatchBuildOpenshiftIoV1NamespacedBuildConfigList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 124;
$watch = false;

$result = $buildOpenshiftIoV1->getWatchBuildOpenshiftIoV1NamespacedBuildConfigList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_build_openshift_io_v1_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getWatchBuildOpenshiftIoV1NamespacedBuildConfig") getWatchBuildOpenshiftIoV1NamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind BuildConfig


```php
function getWatchBuildOpenshiftIoV1NamespacedBuildConfig(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 124;
$watch = false;

$result = $buildOpenshiftIoV1->getWatchBuildOpenshiftIoV1NamespacedBuildConfig($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_build_openshift_io_v1_namespaced_build_list"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getWatchBuildOpenshiftIoV1NamespacedBuildList") getWatchBuildOpenshiftIoV1NamespacedBuildList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Build


```php
function getWatchBuildOpenshiftIoV1NamespacedBuildList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 124;
$watch = false;

$result = $buildOpenshiftIoV1->getWatchBuildOpenshiftIoV1NamespacedBuildList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_build_openshift_io_v1_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".BuildOpenshiftIoV1Controller.getWatchBuildOpenshiftIoV1NamespacedBuild") getWatchBuildOpenshiftIoV1NamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Build


```php
function getWatchBuildOpenshiftIoV1NamespacedBuild(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 82;
$watch = false;

$result = $buildOpenshiftIoV1->getWatchBuildOpenshiftIoV1NamespacedBuild($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="certificates_controller"></a>![Class: ](https://apidocs.io/img/class.png ".CertificatesController") CertificatesController

### Get singleton instance

The singleton instance of the ``` CertificatesController ``` class can be accessed from the API Client.

```php
$certificates = $client->getCertificates();
```

### <a name="get_certificates_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesController.getCertificatesAPIGroup") getCertificatesAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getCertificatesAPIGroup()
```

#### Example Usage

```php

$result = $certificates->getCertificatesAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="certificates_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".CertificatesV1beta1Controller") CertificatesV1beta1Controller

### Get singleton instance

The singleton instance of the ``` CertificatesV1beta1Controller ``` class can be accessed from the API Client.

```php
$certificatesV1beta1 = $client->getCertificatesV1beta1();
```

### <a name="get_certificates_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.getCertificatesV1beta1APIResources") getCertificatesV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getCertificatesV1beta1APIResources()
```

#### Example Usage

```php

$result = $certificatesV1beta1->getCertificatesV1beta1APIResources();

```


### <a name="list_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.listCertificatesV1beta1CertificateSigningRequest") listCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind CertificateSigningRequest


```php
function listCertificatesV1beta1CertificateSigningRequest(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 82;
$watch = false;
$pretty = 'pretty';

$result = $certificatesV1beta1->listCertificatesV1beta1CertificateSigningRequest($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.createCertificatesV1beta1CertificateSigningRequest") createCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> create a CertificateSigningRequest


```php
function createCertificatesV1beta1CertificateSigningRequest(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1CertificateSigningRequest();
$pretty = 'pretty';

$result = $certificatesV1beta1->createCertificatesV1beta1CertificateSigningRequest($body, $pretty);

```


### <a name="delete_certificates_v1beta1_collection_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.deleteCertificatesV1beta1CollectionCertificateSigningRequest") deleteCertificatesV1beta1CollectionCertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of CertificateSigningRequest


```php
function deleteCertificatesV1beta1CollectionCertificateSigningRequest(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 82;
$watch = false;
$pretty = 'pretty';

$result = $certificatesV1beta1->deleteCertificatesV1beta1CollectionCertificateSigningRequest($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.getReadCertificatesV1beta1CertificateSigningRequest") getReadCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> read the specified CertificateSigningRequest


```php
function getReadCertificatesV1beta1CertificateSigningRequest(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $certificatesV1beta1->getReadCertificatesV1beta1CertificateSigningRequest($name, $exact, $export, $pretty);

```


### <a name="update_replace_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.updateReplaceCertificatesV1beta1CertificateSigningRequest") updateReplaceCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified CertificateSigningRequest


```php
function updateReplaceCertificatesV1beta1CertificateSigningRequest(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1CertificateSigningRequest();
$name = 'name';
$pretty = 'pretty';

$result = $certificatesV1beta1->updateReplaceCertificatesV1beta1CertificateSigningRequest($body, $name, $pretty);

```


### <a name="delete_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.deleteCertificatesV1beta1CertificateSigningRequest") deleteCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> delete a CertificateSigningRequest


```php
function deleteCertificatesV1beta1CertificateSigningRequest(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 173;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $certificatesV1beta1->deleteCertificatesV1beta1CertificateSigningRequest($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.patchCertificatesV1beta1CertificateSigningRequest") patchCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified CertificateSigningRequest


```php
function patchCertificatesV1beta1CertificateSigningRequest(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $certificatesV1beta1->patchCertificatesV1beta1CertificateSigningRequest($body, $name, $pretty);

```


### <a name="update_replace_certificates_v1beta1_certificate_signing_request_approval"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.updateReplaceCertificatesV1beta1CertificateSigningRequestApproval") updateReplaceCertificatesV1beta1CertificateSigningRequestApproval

> *Tags:*  ``` Skips Authentication ``` 

> replace approval of the specified CertificateSigningRequest


```php
function updateReplaceCertificatesV1beta1CertificateSigningRequestApproval(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1CertificateSigningRequest();
$name = 'name';
$pretty = 'pretty';

$result = $certificatesV1beta1->updateReplaceCertificatesV1beta1CertificateSigningRequestApproval($body, $name, $pretty);

```


### <a name="update_replace_certificates_v1beta1_certificate_signing_request_status"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.updateReplaceCertificatesV1beta1CertificateSigningRequestStatus") updateReplaceCertificatesV1beta1CertificateSigningRequestStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified CertificateSigningRequest


```php
function updateReplaceCertificatesV1beta1CertificateSigningRequestStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1CertificateSigningRequest();
$name = 'name';
$pretty = 'pretty';

$result = $certificatesV1beta1->updateReplaceCertificatesV1beta1CertificateSigningRequestStatus($body, $name, $pretty);

```


### <a name="get_watch_certificates_v1beta1_certificate_signing_request_list"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.getWatchCertificatesV1beta1CertificateSigningRequestList") getWatchCertificatesV1beta1CertificateSigningRequestList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of CertificateSigningRequest


```php
function getWatchCertificatesV1beta1CertificateSigningRequestList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 10;
$watch = false;

$result = $certificatesV1beta1->getWatchCertificatesV1beta1CertificateSigningRequestList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_certificates_v1beta1_certificate_signing_request"></a>![Method: ](https://apidocs.io/img/method.png ".CertificatesV1beta1Controller.getWatchCertificatesV1beta1CertificateSigningRequest") getWatchCertificatesV1beta1CertificateSigningRequest

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind CertificateSigningRequest


```php
function getWatchCertificatesV1beta1CertificateSigningRequest(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the CertificateSigningRequest |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 10;
$watch = false;

$result = $certificatesV1beta1->getWatchCertificatesV1beta1CertificateSigningRequest($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="extensions_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ExtensionsController") ExtensionsController

### Get singleton instance

The singleton instance of the ``` ExtensionsController ``` class can be accessed from the API Client.

```php
$extensions = $client->getExtensions();
```

### <a name="get_extensions_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsController.getExtensionsAPIGroup") getExtensionsAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getExtensionsAPIGroup()
```

#### Example Usage

```php

$result = $extensions->getExtensionsAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="extensions_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ExtensionsV1beta1Controller") ExtensionsV1beta1Controller

### Get singleton instance

The singleton instance of the ``` ExtensionsV1beta1Controller ``` class can be accessed from the API Client.

```php
$extensionsV1beta1 = $client->getExtensionsV1beta1();
```

### <a name="get_extensions_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getExtensionsV1beta1APIResources") getExtensionsV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getExtensionsV1beta1APIResources()
```

#### Example Usage

```php

$result = $extensionsV1beta1->getExtensionsV1beta1APIResources();

```


### <a name="list_extensions_v1beta1_daemon_set_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1DaemonSetForAllNamespaces") listExtensionsV1beta1DaemonSetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind DaemonSet


```php
function listExtensionsV1beta1DaemonSetForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 10;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1DaemonSetForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_daemon_set_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1DaemonSetForAllNamespaces") createExtensionsV1beta1DaemonSetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a DaemonSet


```php
function createExtensionsV1beta1DaemonSetForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1DaemonSet();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1DaemonSetForAllNamespaces($body, $pretty);

```


### <a name="list_extensions_v1beta1_deployment_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1DeploymentForAllNamespaces") listExtensionsV1beta1DeploymentForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Deployment


```php
function listExtensionsV1beta1DeploymentForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 223;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1DeploymentForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_deployment_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1DeploymentForAllNamespaces") createExtensionsV1beta1DeploymentForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Deployment


```php
function createExtensionsV1beta1DeploymentForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1DeploymentForAllNamespaces($body, $pretty);

```


### <a name="list_extensions_v1beta1_horizontal_pod_autoscaler_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces") listExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HorizontalPodAutoscaler


```php
function listExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 223;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_horizontal_pod_autoscaler_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces") createExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a HorizontalPodAutoscaler


```php
function createExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1HorizontalPodAutoscaler();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1HorizontalPodAutoscalerForAllNamespaces($body, $pretty);

```


### <a name="list_extensions_v1beta1_ingress_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1IngressForAllNamespaces") listExtensionsV1beta1IngressForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Ingress


```php
function listExtensionsV1beta1IngressForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 223;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1IngressForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_ingress_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1IngressForAllNamespaces") createExtensionsV1beta1IngressForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an Ingress


```php
function createExtensionsV1beta1IngressForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Ingress();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1IngressForAllNamespaces($body, $pretty);

```


### <a name="list_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NamespacedDaemonSet") listExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind DaemonSet


```php
function listExtensionsV1beta1NamespacedDaemonSet(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 60;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NamespacedDaemonSet($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedDaemonSet") createExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> create a DaemonSet


```php
function createExtensionsV1beta1NamespacedDaemonSet(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1DaemonSet();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedDaemonSet($body, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionNamespacedDaemonSet") deleteExtensionsV1beta1CollectionNamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of DaemonSet


```php
function deleteExtensionsV1beta1CollectionNamespacedDaemonSet(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 60;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionNamespacedDaemonSet($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedDaemonSet") getReadExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> read the specified DaemonSet


```php
function getReadExtensionsV1beta1NamespacedDaemonSet(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedDaemonSet($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedDaemonSet") updateReplaceExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified DaemonSet


```php
function updateReplaceExtensionsV1beta1NamespacedDaemonSet(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1DaemonSet();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedDaemonSet($body, $name, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1NamespacedDaemonSet") deleteExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> delete a DaemonSet


```php
function deleteExtensionsV1beta1NamespacedDaemonSet(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 18;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1NamespacedDaemonSet($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedDaemonSet") patchExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified DaemonSet


```php
function patchExtensionsV1beta1NamespacedDaemonSet(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedDaemonSet($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_daemon_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedDaemonSetStatus") getReadExtensionsV1beta1NamespacedDaemonSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified DaemonSet


```php
function getReadExtensionsV1beta1NamespacedDaemonSetStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedDaemonSetStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_daemon_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedDaemonSetStatus") updateReplaceExtensionsV1beta1NamespacedDaemonSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified DaemonSet


```php
function updateReplaceExtensionsV1beta1NamespacedDaemonSetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1DaemonSet();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedDaemonSetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_daemon_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedDaemonSetStatus") patchExtensionsV1beta1NamespacedDaemonSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified DaemonSet


```php
function patchExtensionsV1beta1NamespacedDaemonSetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedDaemonSetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NamespacedDeployment") listExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Deployment


```php
function listExtensionsV1beta1NamespacedDeployment(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 18;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NamespacedDeployment($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedDeployment") createExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> create a Deployment


```php
function createExtensionsV1beta1NamespacedDeployment(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedDeployment($body, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionNamespacedDeployment") deleteExtensionsV1beta1CollectionNamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Deployment


```php
function deleteExtensionsV1beta1CollectionNamespacedDeployment(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 109;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionNamespacedDeployment($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedDeployment") getReadExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Deployment


```php
function getReadExtensionsV1beta1NamespacedDeployment(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedDeployment($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedDeployment") updateReplaceExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Deployment


```php
function updateReplaceExtensionsV1beta1NamespacedDeployment(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedDeployment($body, $name, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1NamespacedDeployment") deleteExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> delete a Deployment


```php
function deleteExtensionsV1beta1NamespacedDeployment(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 109;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1NamespacedDeployment($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedDeployment") patchExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Deployment


```php
function patchExtensionsV1beta1NamespacedDeployment(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedDeployment($body, $name, $mnamespace, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_deployment_rollback_rollback"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedDeploymentRollbackRollback") createExtensionsV1beta1NamespacedDeploymentRollbackRollback

> *Tags:*  ``` Skips Authentication ``` 

> create rollback of a DeploymentRollback


```php
function createExtensionsV1beta1NamespacedDeploymentRollbackRollback(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentRollback |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1DeploymentRollback();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedDeploymentRollbackRollback($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_deployments_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedDeploymentsScale") getReadExtensionsV1beta1NamespacedDeploymentsScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadExtensionsV1beta1NamespacedDeploymentsScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedDeploymentsScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_deployments_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedDeploymentsScale") updateReplaceExtensionsV1beta1NamespacedDeploymentsScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceExtensionsV1beta1NamespacedDeploymentsScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedDeploymentsScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_deployments_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedDeploymentsScale") patchExtensionsV1beta1NamespacedDeploymentsScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchExtensionsV1beta1NamespacedDeploymentsScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedDeploymentsScale($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_deployment_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedDeploymentStatus") getReadExtensionsV1beta1NamespacedDeploymentStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Deployment


```php
function getReadExtensionsV1beta1NamespacedDeploymentStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedDeploymentStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_deployment_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedDeploymentStatus") updateReplaceExtensionsV1beta1NamespacedDeploymentStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Deployment


```php
function updateReplaceExtensionsV1beta1NamespacedDeploymentStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Deployment();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedDeploymentStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_deployment_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedDeploymentStatus") patchExtensionsV1beta1NamespacedDeploymentStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Deployment


```php
function patchExtensionsV1beta1NamespacedDeploymentStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedDeploymentStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NamespacedHorizontalPodAutoscaler") listExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HorizontalPodAutoscaler


```php
function listExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 159;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NamespacedHorizontalPodAutoscaler($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedHorizontalPodAutoscaler") createExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> create a HorizontalPodAutoscaler


```php
function createExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1HorizontalPodAutoscaler();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedHorizontalPodAutoscaler($body, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionNamespacedHorizontalPodAutoscaler") deleteExtensionsV1beta1CollectionNamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of HorizontalPodAutoscaler


```php
function deleteExtensionsV1beta1CollectionNamespacedHorizontalPodAutoscaler(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 159;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionNamespacedHorizontalPodAutoscaler($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedHorizontalPodAutoscaler") getReadExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> read the specified HorizontalPodAutoscaler


```php
function getReadExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedHorizontalPodAutoscaler($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscaler") updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified HorizontalPodAutoscaler


```php
function updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1HorizontalPodAutoscaler();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1NamespacedHorizontalPodAutoscaler") deleteExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> delete a HorizontalPodAutoscaler


```php
function deleteExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 251;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedHorizontalPodAutoscaler") patchExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified HorizontalPodAutoscaler


```php
function patchExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedHorizontalPodAutoscaler($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus") getReadExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified HorizontalPodAutoscaler


```php
function getReadExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus") updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified HorizontalPodAutoscaler


```php
function updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1HorizontalPodAutoscaler();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_horizontal_pod_autoscaler_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus") patchExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified HorizontalPodAutoscaler


```php
function patchExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedHorizontalPodAutoscalerStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NamespacedIngress") listExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Ingress


```php
function listExtensionsV1beta1NamespacedIngress(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 87;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NamespacedIngress($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedIngress") createExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> create an Ingress


```php
function createExtensionsV1beta1NamespacedIngress(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Ingress();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedIngress($body, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionNamespacedIngress") deleteExtensionsV1beta1CollectionNamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Ingress


```php
function deleteExtensionsV1beta1CollectionNamespacedIngress(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 45;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionNamespacedIngress($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedIngress") getReadExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Ingress


```php
function getReadExtensionsV1beta1NamespacedIngress(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedIngress($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedIngress") updateReplaceExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Ingress


```php
function updateReplaceExtensionsV1beta1NamespacedIngress(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Ingress();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedIngress($body, $name, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1NamespacedIngress") deleteExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> delete an Ingress


```php
function deleteExtensionsV1beta1NamespacedIngress(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 45;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1NamespacedIngress($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedIngress") patchExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Ingress


```php
function patchExtensionsV1beta1NamespacedIngress(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedIngress($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_ingress_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedIngressStatus") getReadExtensionsV1beta1NamespacedIngressStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Ingress


```php
function getReadExtensionsV1beta1NamespacedIngressStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedIngressStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_ingress_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedIngressStatus") updateReplaceExtensionsV1beta1NamespacedIngressStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Ingress


```php
function updateReplaceExtensionsV1beta1NamespacedIngressStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Ingress();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedIngressStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_ingress_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedIngressStatus") patchExtensionsV1beta1NamespacedIngressStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Ingress


```php
function patchExtensionsV1beta1NamespacedIngressStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedIngressStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NamespacedNetworkPolicy") listExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind NetworkPolicy


```php
function listExtensionsV1beta1NamespacedNetworkPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 137;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NamespacedNetworkPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedNetworkPolicy") createExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create a NetworkPolicy


```php
function createExtensionsV1beta1NamespacedNetworkPolicy(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1NetworkPolicy();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedNetworkPolicy($body, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionNamespacedNetworkPolicy") deleteExtensionsV1beta1CollectionNamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of NetworkPolicy


```php
function deleteExtensionsV1beta1CollectionNamespacedNetworkPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 137;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionNamespacedNetworkPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedNetworkPolicy") getReadExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified NetworkPolicy


```php
function getReadExtensionsV1beta1NamespacedNetworkPolicy(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the NetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedNetworkPolicy($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedNetworkPolicy") updateReplaceExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified NetworkPolicy


```php
function updateReplaceExtensionsV1beta1NamespacedNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1NetworkPolicy();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedNetworkPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1NamespacedNetworkPolicy") deleteExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete a NetworkPolicy


```php
function deleteExtensionsV1beta1NamespacedNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 95;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1NamespacedNetworkPolicy($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedNetworkPolicy") patchExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified NetworkPolicy


```php
function patchExtensionsV1beta1NamespacedNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedNetworkPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="list_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NamespacedReplicaSet") listExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ReplicaSet


```php
function listExtensionsV1beta1NamespacedReplicaSet(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 187;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NamespacedReplicaSet($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NamespacedReplicaSet") createExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> create a ReplicaSet


```php
function createExtensionsV1beta1NamespacedReplicaSet(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ReplicaSet();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NamespacedReplicaSet($body, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionNamespacedReplicaSet") deleteExtensionsV1beta1CollectionNamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ReplicaSet


```php
function deleteExtensionsV1beta1CollectionNamespacedReplicaSet(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 187;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionNamespacedReplicaSet($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedReplicaSet") getReadExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ReplicaSet


```php
function getReadExtensionsV1beta1NamespacedReplicaSet(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedReplicaSet($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedReplicaSet") updateReplaceExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ReplicaSet


```php
function updateReplaceExtensionsV1beta1NamespacedReplicaSet(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ReplicaSet();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedReplicaSet($body, $name, $mnamespace, $pretty);

```


### <a name="delete_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1NamespacedReplicaSet") deleteExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> delete a ReplicaSet


```php
function deleteExtensionsV1beta1NamespacedReplicaSet(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 23;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1NamespacedReplicaSet($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedReplicaSet") patchExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ReplicaSet


```php
function patchExtensionsV1beta1NamespacedReplicaSet(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedReplicaSet($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_replicasets_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedReplicasetsScale") getReadExtensionsV1beta1NamespacedReplicasetsScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadExtensionsV1beta1NamespacedReplicasetsScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedReplicasetsScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_replicasets_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedReplicasetsScale") updateReplaceExtensionsV1beta1NamespacedReplicasetsScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceExtensionsV1beta1NamespacedReplicasetsScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedReplicasetsScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_replicasets_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedReplicasetsScale") patchExtensionsV1beta1NamespacedReplicasetsScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchExtensionsV1beta1NamespacedReplicasetsScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedReplicasetsScale($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_replica_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedReplicaSetStatus") getReadExtensionsV1beta1NamespacedReplicaSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ReplicaSet


```php
function getReadExtensionsV1beta1NamespacedReplicaSetStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedReplicaSetStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_replica_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedReplicaSetStatus") updateReplaceExtensionsV1beta1NamespacedReplicaSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ReplicaSet


```php
function updateReplaceExtensionsV1beta1NamespacedReplicaSetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ReplicaSet();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedReplicaSetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_replica_set_status"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedReplicaSetStatus") patchExtensionsV1beta1NamespacedReplicaSetStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ReplicaSet


```php
function patchExtensionsV1beta1NamespacedReplicaSetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedReplicaSetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_extensions_v1beta1_namespaced_replicationcontrollers_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1NamespacedReplicationcontrollersScale") getReadExtensionsV1beta1NamespacedReplicationcontrollersScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadExtensionsV1beta1NamespacedReplicationcontrollersScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1NamespacedReplicationcontrollersScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_extensions_v1beta1_namespaced_replicationcontrollers_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1NamespacedReplicationcontrollersScale") updateReplaceExtensionsV1beta1NamespacedReplicationcontrollersScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceExtensionsV1beta1NamespacedReplicationcontrollersScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1NamespacedReplicationcontrollersScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_extensions_v1beta1_namespaced_replicationcontrollers_scale"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1NamespacedReplicationcontrollersScale") patchExtensionsV1beta1NamespacedReplicationcontrollersScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchExtensionsV1beta1NamespacedReplicationcontrollersScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1NamespacedReplicationcontrollersScale($body, $name, $mnamespace, $pretty);

```


### <a name="list_extensions_v1beta1_network_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1NetworkPolicyForAllNamespaces") listExtensionsV1beta1NetworkPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind NetworkPolicy


```php
function listExtensionsV1beta1NetworkPolicyForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 237;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1NetworkPolicyForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_network_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1NetworkPolicyForAllNamespaces") createExtensionsV1beta1NetworkPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a NetworkPolicy


```php
function createExtensionsV1beta1NetworkPolicyForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1NetworkPolicy();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1NetworkPolicyForAllNamespaces($body, $pretty);

```


### <a name="list_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1PodSecurityPolicy") listExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodSecurityPolicy


```php
function listExtensionsV1beta1PodSecurityPolicy(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 73;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1PodSecurityPolicy($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1PodSecurityPolicy") createExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicy


```php
function createExtensionsV1beta1PodSecurityPolicy(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1PodSecurityPolicy();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1PodSecurityPolicy($body, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionPodSecurityPolicy") deleteExtensionsV1beta1CollectionPodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PodSecurityPolicy


```php
function deleteExtensionsV1beta1CollectionPodSecurityPolicy(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 73;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionPodSecurityPolicy($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1PodSecurityPolicy") getReadExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PodSecurityPolicy


```php
function getReadExtensionsV1beta1PodSecurityPolicy(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodSecurityPolicy |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1PodSecurityPolicy($name, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1PodSecurityPolicy") updateReplaceExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PodSecurityPolicy


```php
function updateReplaceExtensionsV1beta1PodSecurityPolicy(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodSecurityPolicy |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1PodSecurityPolicy();
$name = 'name';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1PodSecurityPolicy($body, $name, $pretty);

```


### <a name="delete_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1PodSecurityPolicy") deleteExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete a PodSecurityPolicy


```php
function deleteExtensionsV1beta1PodSecurityPolicy(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodSecurityPolicy |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 165;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1PodSecurityPolicy($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1PodSecurityPolicy") patchExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PodSecurityPolicy


```php
function patchExtensionsV1beta1PodSecurityPolicy(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodSecurityPolicy |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1PodSecurityPolicy($body, $name, $pretty);

```


### <a name="list_extensions_v1beta1_replica_set_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1ReplicaSetForAllNamespaces") listExtensionsV1beta1ReplicaSetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ReplicaSet


```php
function listExtensionsV1beta1ReplicaSetForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 165;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1ReplicaSetForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_replica_set_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1ReplicaSetForAllNamespaces") createExtensionsV1beta1ReplicaSetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ReplicaSet


```php
function createExtensionsV1beta1ReplicaSetForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ReplicaSet();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1ReplicaSetForAllNamespaces($body, $pretty);

```


### <a name="list_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.listExtensionsV1beta1ThirdPartyResource") listExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ThirdPartyResource


```php
function listExtensionsV1beta1ThirdPartyResource(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 165;
$watch = true;
$pretty = 'pretty';

$result = $extensionsV1beta1->listExtensionsV1beta1ThirdPartyResource($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.createExtensionsV1beta1ThirdPartyResource") createExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> create a ThirdPartyResource


```php
function createExtensionsV1beta1ThirdPartyResource(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ThirdPartyResource();
$pretty = 'pretty';

$result = $extensionsV1beta1->createExtensionsV1beta1ThirdPartyResource($body, $pretty);

```


### <a name="delete_extensions_v1beta1_collection_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1CollectionThirdPartyResource") deleteExtensionsV1beta1CollectionThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ThirdPartyResource


```php
function deleteExtensionsV1beta1CollectionThirdPartyResource(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 123;
$watch = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1CollectionThirdPartyResource($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getReadExtensionsV1beta1ThirdPartyResource") getReadExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ThirdPartyResource


```php
function getReadExtensionsV1beta1ThirdPartyResource(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ThirdPartyResource |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $extensionsV1beta1->getReadExtensionsV1beta1ThirdPartyResource($name, $exact, $export, $pretty);

```


### <a name="update_replace_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.updateReplaceExtensionsV1beta1ThirdPartyResource") updateReplaceExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ThirdPartyResource


```php
function updateReplaceExtensionsV1beta1ThirdPartyResource(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ThirdPartyResource |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ThirdPartyResource();
$name = 'name';
$pretty = 'pretty';

$result = $extensionsV1beta1->updateReplaceExtensionsV1beta1ThirdPartyResource($body, $name, $pretty);

```


### <a name="delete_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.deleteExtensionsV1beta1ThirdPartyResource") deleteExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> delete a ThirdPartyResource


```php
function deleteExtensionsV1beta1ThirdPartyResource(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ThirdPartyResource |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 123;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $extensionsV1beta1->deleteExtensionsV1beta1ThirdPartyResource($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.patchExtensionsV1beta1ThirdPartyResource") patchExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ThirdPartyResource


```php
function patchExtensionsV1beta1ThirdPartyResource(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ThirdPartyResource |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $extensionsV1beta1->patchExtensionsV1beta1ThirdPartyResource($body, $name, $pretty);

```


### <a name="get_watch_extensions_v1beta1_daemon_set_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1DaemonSetListForAllNamespaces") getWatchExtensionsV1beta1DaemonSetListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of DaemonSet


```php
function getWatchExtensionsV1beta1DaemonSetListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 214;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1DaemonSetListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_deployment_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1DeploymentListForAllNamespaces") getWatchExtensionsV1beta1DeploymentListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Deployment


```php
function getWatchExtensionsV1beta1DeploymentListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 214;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1DeploymentListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_horizontal_pod_autoscaler_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1HorizontalPodAutoscalerListForAllNamespaces") getWatchExtensionsV1beta1HorizontalPodAutoscalerListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HorizontalPodAutoscaler


```php
function getWatchExtensionsV1beta1HorizontalPodAutoscalerListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 214;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1HorizontalPodAutoscalerListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_ingress_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1IngressListForAllNamespaces") getWatchExtensionsV1beta1IngressListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Ingress


```php
function getWatchExtensionsV1beta1IngressListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 214;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1IngressListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_daemon_set_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedDaemonSetList") getWatchExtensionsV1beta1NamespacedDaemonSetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of DaemonSet


```php
function getWatchExtensionsV1beta1NamespacedDaemonSetList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 214;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedDaemonSetList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_daemon_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedDaemonSet") getWatchExtensionsV1beta1NamespacedDaemonSet

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind DaemonSet


```php
function getWatchExtensionsV1beta1NamespacedDaemonSet(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DaemonSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 173;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedDaemonSet($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_deployment_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedDeploymentList") getWatchExtensionsV1beta1NamespacedDeploymentList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Deployment


```php
function getWatchExtensionsV1beta1NamespacedDeploymentList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 173;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedDeploymentList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_deployment"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedDeployment") getWatchExtensionsV1beta1NamespacedDeployment

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Deployment


```php
function getWatchExtensionsV1beta1NamespacedDeployment(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Deployment |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 173;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedDeployment($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_horizontal_pod_autoscaler_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscalerList") getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscalerList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HorizontalPodAutoscaler


```php
function getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscalerList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 9;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscalerList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_horizontal_pod_autoscaler"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscaler") getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscaler

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind HorizontalPodAutoscaler


```php
function getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscaler(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HorizontalPodAutoscaler |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 9;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedHorizontalPodAutoscaler($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_ingress_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedIngressList") getWatchExtensionsV1beta1NamespacedIngressList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Ingress


```php
function getWatchExtensionsV1beta1NamespacedIngressList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 9;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedIngressList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_ingress"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedIngress") getWatchExtensionsV1beta1NamespacedIngress

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Ingress


```php
function getWatchExtensionsV1beta1NamespacedIngress(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Ingress |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 9;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedIngress($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_network_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedNetworkPolicyList") getWatchExtensionsV1beta1NamespacedNetworkPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of NetworkPolicy


```php
function getWatchExtensionsV1beta1NamespacedNetworkPolicyList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 101;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedNetworkPolicyList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedNetworkPolicy") getWatchExtensionsV1beta1NamespacedNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind NetworkPolicy


```php
function getWatchExtensionsV1beta1NamespacedNetworkPolicy(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the NetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 101;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedNetworkPolicy($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_replica_set_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedReplicaSetList") getWatchExtensionsV1beta1NamespacedReplicaSetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ReplicaSet


```php
function getWatchExtensionsV1beta1NamespacedReplicaSetList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 101;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedReplicaSetList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_namespaced_replica_set"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NamespacedReplicaSet") getWatchExtensionsV1beta1NamespacedReplicaSet

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ReplicaSet


```php
function getWatchExtensionsV1beta1NamespacedReplicaSet(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ReplicaSet |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 101;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NamespacedReplicaSet($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_network_policy_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1NetworkPolicyListForAllNamespaces") getWatchExtensionsV1beta1NetworkPolicyListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of NetworkPolicy


```php
function getWatchExtensionsV1beta1NetworkPolicyListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 59;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1NetworkPolicyListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_pod_security_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1PodSecurityPolicyList") getWatchExtensionsV1beta1PodSecurityPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodSecurityPolicy


```php
function getWatchExtensionsV1beta1PodSecurityPolicyList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 59;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1PodSecurityPolicyList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_pod_security_policy"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1PodSecurityPolicy") getWatchExtensionsV1beta1PodSecurityPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PodSecurityPolicy


```php
function getWatchExtensionsV1beta1PodSecurityPolicy(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodSecurityPolicy |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 59;
$watch = false;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1PodSecurityPolicy($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_replica_set_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1ReplicaSetListForAllNamespaces") getWatchExtensionsV1beta1ReplicaSetListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ReplicaSet


```php
function getWatchExtensionsV1beta1ReplicaSetListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 150;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1ReplicaSetListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_third_party_resource_list"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1ThirdPartyResourceList") getWatchExtensionsV1beta1ThirdPartyResourceList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ThirdPartyResource


```php
function getWatchExtensionsV1beta1ThirdPartyResourceList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 150;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1ThirdPartyResourceList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_extensions_v1beta1_third_party_resource"></a>![Method: ](https://apidocs.io/img/method.png ".ExtensionsV1beta1Controller.getWatchExtensionsV1beta1ThirdPartyResource") getWatchExtensionsV1beta1ThirdPartyResource

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ThirdPartyResource


```php
function getWatchExtensionsV1beta1ThirdPartyResource(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ThirdPartyResource |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 150;
$watch = true;

$result = $extensionsV1beta1->getWatchExtensionsV1beta1ThirdPartyResource($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="image_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ImageOpenshiftIoController") ImageOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` ImageOpenshiftIoController ``` class can be accessed from the API Client.

```php
$imageOpenshiftIo = $client->getImageOpenshiftIo();
```

### <a name="get_image_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoController.getImageOpenshiftIoAPIGroup") getImageOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getImageOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $imageOpenshiftIo->getImageOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="image_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ImageOpenshiftIoV1Controller") ImageOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` ImageOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$imageOpenshiftIoV1 = $client->getImageOpenshiftIoV1();
```

### <a name="get_image_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getImageOpenshiftIoV1APIResources") getImageOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getImageOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $imageOpenshiftIoV1->getImageOpenshiftIoV1APIResources();

```


### <a name="list_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.listImageOpenshiftIoV1Image") listImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Image


```php
function listImageOpenshiftIoV1Image(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 150;
$watch = true;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->listImageOpenshiftIoV1Image($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1Image") createImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> create an Image


```php
function createImageOpenshiftIoV1Image(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Image();
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1Image($body, $pretty);

```


### <a name="delete_image_openshift_io_v1_collection_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.deleteImageOpenshiftIoV1CollectionImage") deleteImageOpenshiftIoV1CollectionImage

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Image


```php
function deleteImageOpenshiftIoV1CollectionImage(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 242;
$watch = true;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->deleteImageOpenshiftIoV1CollectionImage($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getReadImageOpenshiftIoV1Image") getReadImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Image


```php
function getReadImageOpenshiftIoV1Image(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Image |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->getReadImageOpenshiftIoV1Image($name, $exact, $export, $pretty);

```


### <a name="update_replace_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.updateReplaceImageOpenshiftIoV1Image") updateReplaceImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Image


```php
function updateReplaceImageOpenshiftIoV1Image(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Image |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Image();
$name = 'name';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->updateReplaceImageOpenshiftIoV1Image($body, $name, $pretty);

```


### <a name="delete_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.deleteImageOpenshiftIoV1Image") deleteImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> delete an Image


```php
function deleteImageOpenshiftIoV1Image(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Image |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 242;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->deleteImageOpenshiftIoV1Image($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.patchImageOpenshiftIoV1Image") patchImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Image


```php
function patchImageOpenshiftIoV1Image(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Image |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->patchImageOpenshiftIoV1Image($body, $name, $pretty);

```


### <a name="create_image_openshift_io_v1_image_signature"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1ImageSignature") createImageOpenshiftIoV1ImageSignature

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageSignature


```php
function createImageOpenshiftIoV1ImageSignature(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageSignature();
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1ImageSignature($body, $pretty);

```


### <a name="delete_image_openshift_io_v1_image_signature"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.deleteImageOpenshiftIoV1ImageSignature") deleteImageOpenshiftIoV1ImageSignature

> *Tags:*  ``` Skips Authentication ``` 

> delete an ImageSignature


```php
function deleteImageOpenshiftIoV1ImageSignature(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageSignature |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->deleteImageOpenshiftIoV1ImageSignature($name, $pretty);

```


### <a name="create_image_openshift_io_v1_image_stream_import_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1ImageStreamImportForAllNamespaces") createImageOpenshiftIoV1ImageStreamImportForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamImport


```php
function createImageOpenshiftIoV1ImageStreamImportForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamImport();
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1ImageStreamImportForAllNamespaces($body, $pretty);

```


### <a name="create_image_openshift_io_v1_image_stream_mapping_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1ImageStreamMappingForAllNamespaces") createImageOpenshiftIoV1ImageStreamMappingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamMapping


```php
function createImageOpenshiftIoV1ImageStreamMappingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamMapping();
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1ImageStreamMappingForAllNamespaces($body, $pretty);

```


### <a name="list_image_openshift_io_v1_image_stream_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.listImageOpenshiftIoV1ImageStreamForAllNamespaces") listImageOpenshiftIoV1ImageStreamForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ImageStream


```php
function listImageOpenshiftIoV1ImageStreamForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 200;
$watch = true;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->listImageOpenshiftIoV1ImageStreamForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_openshift_io_v1_image_stream_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1ImageStreamForAllNamespaces") createImageOpenshiftIoV1ImageStreamForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStream


```php
function createImageOpenshiftIoV1ImageStreamForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1ImageStreamForAllNamespaces($body, $pretty);

```


### <a name="list_image_openshift_io_v1_image_stream_tag_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.listImageOpenshiftIoV1ImageStreamTagForAllNamespaces") listImageOpenshiftIoV1ImageStreamTagForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ImageStreamTag


```php
function listImageOpenshiftIoV1ImageStreamTagForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 200;
$watch = true;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->listImageOpenshiftIoV1ImageStreamTagForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_openshift_io_v1_image_stream_tag_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1ImageStreamTagForAllNamespaces") createImageOpenshiftIoV1ImageStreamTagForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamTag


```php
function createImageOpenshiftIoV1ImageStreamTagForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamTag();
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1ImageStreamTagForAllNamespaces($body, $pretty);

```


### <a name="get_read_image_openshift_io_v1_namespaced_image_stream_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getReadImageOpenshiftIoV1NamespacedImageStreamImage") getReadImageOpenshiftIoV1NamespacedImageStreamImage

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ImageStreamImage


```php
function getReadImageOpenshiftIoV1NamespacedImageStreamImage(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStreamImage |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->getReadImageOpenshiftIoV1NamespacedImageStreamImage($name, $mnamespace, $pretty);

```


### <a name="create_image_openshift_io_v1_namespaced_image_stream_import"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1NamespacedImageStreamImport") createImageOpenshiftIoV1NamespacedImageStreamImport

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamImport


```php
function createImageOpenshiftIoV1NamespacedImageStreamImport(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamImport();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1NamespacedImageStreamImport($body, $mnamespace, $pretty);

```


### <a name="create_image_openshift_io_v1_namespaced_image_stream_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1NamespacedImageStreamMapping") createImageOpenshiftIoV1NamespacedImageStreamMapping

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamMapping


```php
function createImageOpenshiftIoV1NamespacedImageStreamMapping(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamMapping();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1NamespacedImageStreamMapping($body, $mnamespace, $pretty);

```


### <a name="list_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.listImageOpenshiftIoV1NamespacedImageStream") listImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ImageStream


```php
function listImageOpenshiftIoV1NamespacedImageStream(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 37;
$watch = false;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->listImageOpenshiftIoV1NamespacedImageStream($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1NamespacedImageStream") createImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStream


```php
function createImageOpenshiftIoV1NamespacedImageStream(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1NamespacedImageStream($body, $mnamespace, $pretty);

```


### <a name="delete_image_openshift_io_v1_collection_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.deleteImageOpenshiftIoV1CollectionNamespacedImageStream") deleteImageOpenshiftIoV1CollectionNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ImageStream


```php
function deleteImageOpenshiftIoV1CollectionNamespacedImageStream(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 37;
$watch = false;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->deleteImageOpenshiftIoV1CollectionNamespacedImageStream($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getReadImageOpenshiftIoV1NamespacedImageStream") getReadImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ImageStream


```php
function getReadImageOpenshiftIoV1NamespacedImageStream(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->getReadImageOpenshiftIoV1NamespacedImageStream($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.updateReplaceImageOpenshiftIoV1NamespacedImageStream") updateReplaceImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ImageStream


```php
function updateReplaceImageOpenshiftIoV1NamespacedImageStream(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->updateReplaceImageOpenshiftIoV1NamespacedImageStream($body, $name, $mnamespace, $pretty);

```


### <a name="delete_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.deleteImageOpenshiftIoV1NamespacedImageStream") deleteImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> delete an ImageStream


```php
function deleteImageOpenshiftIoV1NamespacedImageStream(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 250;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->deleteImageOpenshiftIoV1NamespacedImageStream($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.patchImageOpenshiftIoV1NamespacedImageStream") patchImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ImageStream


```php
function patchImageOpenshiftIoV1NamespacedImageStream(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->patchImageOpenshiftIoV1NamespacedImageStream($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_image_openshift_io_v1_namespaced_secret_list_secrets"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getReadImageOpenshiftIoV1NamespacedSecretListSecrets") getReadImageOpenshiftIoV1NamespacedSecretListSecrets

> *Tags:*  ``` Skips Authentication ``` 

> read secrets of the specified SecretList


```php
function getReadImageOpenshiftIoV1NamespacedSecretListSecrets(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the SecretList |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 250;
$watch = true;

$result = $imageOpenshiftIoV1->getReadImageOpenshiftIoV1NamespacedSecretListSecrets($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_read_image_openshift_io_v1_namespaced_image_stream_status"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getReadImageOpenshiftIoV1NamespacedImageStreamStatus") getReadImageOpenshiftIoV1NamespacedImageStreamStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ImageStream


```php
function getReadImageOpenshiftIoV1NamespacedImageStreamStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->getReadImageOpenshiftIoV1NamespacedImageStreamStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_image_openshift_io_v1_namespaced_image_stream_status"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.updateReplaceImageOpenshiftIoV1NamespacedImageStreamStatus") updateReplaceImageOpenshiftIoV1NamespacedImageStreamStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ImageStream


```php
function updateReplaceImageOpenshiftIoV1NamespacedImageStreamStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->updateReplaceImageOpenshiftIoV1NamespacedImageStreamStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_image_openshift_io_v1_namespaced_image_stream_status"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.patchImageOpenshiftIoV1NamespacedImageStreamStatus") patchImageOpenshiftIoV1NamespacedImageStreamStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ImageStream


```php
function patchImageOpenshiftIoV1NamespacedImageStreamStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->patchImageOpenshiftIoV1NamespacedImageStreamStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_image_openshift_io_v1_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.listImageOpenshiftIoV1NamespacedImageStreamTag") listImageOpenshiftIoV1NamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ImageStreamTag


```php
function listImageOpenshiftIoV1NamespacedImageStreamTag(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 86;
$watch = false;
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->listImageOpenshiftIoV1NamespacedImageStreamTag($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_openshift_io_v1_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.createImageOpenshiftIoV1NamespacedImageStreamTag") createImageOpenshiftIoV1NamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamTag


```php
function createImageOpenshiftIoV1NamespacedImageStreamTag(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamTag();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->createImageOpenshiftIoV1NamespacedImageStreamTag($body, $mnamespace, $pretty);

```


### <a name="get_read_image_openshift_io_v1_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getReadImageOpenshiftIoV1NamespacedImageStreamTag") getReadImageOpenshiftIoV1NamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ImageStreamTag


```php
function getReadImageOpenshiftIoV1NamespacedImageStreamTag(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->getReadImageOpenshiftIoV1NamespacedImageStreamTag($name, $mnamespace, $pretty);

```


### <a name="update_replace_image_openshift_io_v1_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.updateReplaceImageOpenshiftIoV1NamespacedImageStreamTag") updateReplaceImageOpenshiftIoV1NamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ImageStreamTag


```php
function updateReplaceImageOpenshiftIoV1NamespacedImageStreamTag(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamTag();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->updateReplaceImageOpenshiftIoV1NamespacedImageStreamTag($body, $name, $mnamespace, $pretty);

```


### <a name="delete_image_openshift_io_v1_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.deleteImageOpenshiftIoV1NamespacedImageStreamTag") deleteImageOpenshiftIoV1NamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> delete an ImageStreamTag


```php
function deleteImageOpenshiftIoV1NamespacedImageStreamTag(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->deleteImageOpenshiftIoV1NamespacedImageStreamTag($name, $mnamespace, $pretty);

```


### <a name="patch_image_openshift_io_v1_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.patchImageOpenshiftIoV1NamespacedImageStreamTag") patchImageOpenshiftIoV1NamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ImageStreamTag


```php
function patchImageOpenshiftIoV1NamespacedImageStreamTag(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $imageOpenshiftIoV1->patchImageOpenshiftIoV1NamespacedImageStreamTag($body, $name, $mnamespace, $pretty);

```


### <a name="get_watch_image_openshift_io_v1_image_list"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getWatchImageOpenshiftIoV1ImageList") getWatchImageOpenshiftIoV1ImageList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Image


```php
function getWatchImageOpenshiftIoV1ImageList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 178;
$watch = true;

$result = $imageOpenshiftIoV1->getWatchImageOpenshiftIoV1ImageList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image_openshift_io_v1_image"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getWatchImageOpenshiftIoV1Image") getWatchImageOpenshiftIoV1Image

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Image


```php
function getWatchImageOpenshiftIoV1Image(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Image |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 178;
$watch = true;

$result = $imageOpenshiftIoV1->getWatchImageOpenshiftIoV1Image($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image_openshift_io_v1_image_stream_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getWatchImageOpenshiftIoV1ImageStreamListForAllNamespaces") getWatchImageOpenshiftIoV1ImageStreamListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ImageStream


```php
function getWatchImageOpenshiftIoV1ImageStreamListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 178;
$watch = true;

$result = $imageOpenshiftIoV1->getWatchImageOpenshiftIoV1ImageStreamListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image_openshift_io_v1_namespaced_image_stream_list"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getWatchImageOpenshiftIoV1NamespacedImageStreamList") getWatchImageOpenshiftIoV1NamespacedImageStreamList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ImageStream


```php
function getWatchImageOpenshiftIoV1NamespacedImageStreamList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 178;
$watch = true;

$result = $imageOpenshiftIoV1->getWatchImageOpenshiftIoV1NamespacedImageStreamList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image_openshift_io_v1_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".ImageOpenshiftIoV1Controller.getWatchImageOpenshiftIoV1NamespacedImageStream") getWatchImageOpenshiftIoV1NamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ImageStream


```php
function getWatchImageOpenshiftIoV1NamespacedImageStream(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 136;
$watch = true;

$result = $imageOpenshiftIoV1->getWatchImageOpenshiftIoV1NamespacedImageStream($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="network_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".NetworkOpenshiftIoController") NetworkOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` NetworkOpenshiftIoController ``` class can be accessed from the API Client.

```php
$networkOpenshiftIo = $client->getNetworkOpenshiftIo();
```

### <a name="get_network_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoController.getNetworkOpenshiftIoAPIGroup") getNetworkOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getNetworkOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $networkOpenshiftIo->getNetworkOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="network_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".NetworkOpenshiftIoV1Controller") NetworkOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` NetworkOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$networkOpenshiftIoV1 = $client->getNetworkOpenshiftIoV1();
```

### <a name="get_network_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getNetworkOpenshiftIoV1APIResources") getNetworkOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getNetworkOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $networkOpenshiftIoV1->getNetworkOpenshiftIoV1APIResources();

```


### <a name="list_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.listNetworkOpenshiftIoV1ClusterNetwork") listNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterNetwork


```php
function listNetworkOpenshiftIoV1ClusterNetwork(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 136;
$watch = true;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->listNetworkOpenshiftIoV1ClusterNetwork($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.createNetworkOpenshiftIoV1ClusterNetwork") createNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterNetwork


```php
function createNetworkOpenshiftIoV1ClusterNetwork(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterNetwork();
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->createNetworkOpenshiftIoV1ClusterNetwork($body, $pretty);

```


### <a name="delete_network_openshift_io_v1_collection_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1CollectionClusterNetwork") deleteNetworkOpenshiftIoV1CollectionClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterNetwork


```php
function deleteNetworkOpenshiftIoV1CollectionClusterNetwork(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 136;
$watch = true;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1CollectionClusterNetwork($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getReadNetworkOpenshiftIoV1ClusterNetwork") getReadNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterNetwork


```php
function getReadNetworkOpenshiftIoV1ClusterNetwork(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterNetwork |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->getReadNetworkOpenshiftIoV1ClusterNetwork($name, $exact, $export, $pretty);

```


### <a name="update_replace_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.updateReplaceNetworkOpenshiftIoV1ClusterNetwork") updateReplaceNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterNetwork


```php
function updateReplaceNetworkOpenshiftIoV1ClusterNetwork(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterNetwork |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterNetwork();
$name = 'name';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->updateReplaceNetworkOpenshiftIoV1ClusterNetwork($body, $name, $pretty);

```


### <a name="delete_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1ClusterNetwork") deleteNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterNetwork


```php
function deleteNetworkOpenshiftIoV1ClusterNetwork(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterNetwork |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 228;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1ClusterNetwork($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.patchNetworkOpenshiftIoV1ClusterNetwork") patchNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterNetwork


```php
function patchNetworkOpenshiftIoV1ClusterNetwork(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterNetwork |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->patchNetworkOpenshiftIoV1ClusterNetwork($body, $name, $pretty);

```


### <a name="list_network_openshift_io_v1_egress_network_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.listNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces") listNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind EgressNetworkPolicy


```php
function listNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 228;
$watch = true;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->listNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_network_openshift_io_v1_egress_network_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.createNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces") createNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an EgressNetworkPolicy


```php
function createNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1EgressNetworkPolicy();
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->createNetworkOpenshiftIoV1EgressNetworkPolicyForAllNamespaces($body, $pretty);

```


### <a name="list_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.listNetworkOpenshiftIoV1HostSubnet") listNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HostSubnet


```php
function listNetworkOpenshiftIoV1HostSubnet(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 228;
$watch = true;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->listNetworkOpenshiftIoV1HostSubnet($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.createNetworkOpenshiftIoV1HostSubnet") createNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> create a HostSubnet


```php
function createNetworkOpenshiftIoV1HostSubnet(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HostSubnet();
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->createNetworkOpenshiftIoV1HostSubnet($body, $pretty);

```


### <a name="delete_network_openshift_io_v1_collection_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1CollectionHostSubnet") deleteNetworkOpenshiftIoV1CollectionHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of HostSubnet


```php
function deleteNetworkOpenshiftIoV1CollectionHostSubnet(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 64;
$watch = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1CollectionHostSubnet($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getReadNetworkOpenshiftIoV1HostSubnet") getReadNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> read the specified HostSubnet


```php
function getReadNetworkOpenshiftIoV1HostSubnet(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HostSubnet |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->getReadNetworkOpenshiftIoV1HostSubnet($name, $exact, $export, $pretty);

```


### <a name="update_replace_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.updateReplaceNetworkOpenshiftIoV1HostSubnet") updateReplaceNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified HostSubnet


```php
function updateReplaceNetworkOpenshiftIoV1HostSubnet(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HostSubnet |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HostSubnet();
$name = 'name';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->updateReplaceNetworkOpenshiftIoV1HostSubnet($body, $name, $pretty);

```


### <a name="delete_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1HostSubnet") deleteNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> delete a HostSubnet


```php
function deleteNetworkOpenshiftIoV1HostSubnet(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HostSubnet |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 64;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1HostSubnet($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.patchNetworkOpenshiftIoV1HostSubnet") patchNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified HostSubnet


```php
function patchNetworkOpenshiftIoV1HostSubnet(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HostSubnet |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->patchNetworkOpenshiftIoV1HostSubnet($body, $name, $pretty);

```


### <a name="list_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.listNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") listNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind EgressNetworkPolicy


```php
function listNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 64;
$watch = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->listNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.createNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") createNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create an EgressNetworkPolicy


```php
function createNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1EgressNetworkPolicy();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->createNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($body, $mnamespace, $pretty);

```


### <a name="delete_network_openshift_io_v1_collection_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1CollectionNamespacedEgressNetworkPolicy") deleteNetworkOpenshiftIoV1CollectionNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of EgressNetworkPolicy


```php
function deleteNetworkOpenshiftIoV1CollectionNamespacedEgressNetworkPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 22;
$watch = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1CollectionNamespacedEgressNetworkPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getReadNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") getReadNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified EgressNetworkPolicy


```php
function getReadNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->getReadNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.updateReplaceNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") updateReplaceNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified EgressNetworkPolicy


```php
function updateReplaceNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1EgressNetworkPolicy();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->updateReplaceNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="delete_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") deleteNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete an EgressNetworkPolicy


```php
function deleteNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 22;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.patchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") patchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified EgressNetworkPolicy


```php
function patchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->patchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="list_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.listNetworkOpenshiftIoV1NetNamespace") listNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind NetNamespace


```php
function listNetworkOpenshiftIoV1NetNamespace(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 114;
$watch = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->listNetworkOpenshiftIoV1NetNamespace($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.createNetworkOpenshiftIoV1NetNamespace") createNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> create a NetNamespace


```php
function createNetworkOpenshiftIoV1NetNamespace(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1NetNamespace();
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->createNetworkOpenshiftIoV1NetNamespace($body, $pretty);

```


### <a name="delete_network_openshift_io_v1_collection_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1CollectionNetNamespace") deleteNetworkOpenshiftIoV1CollectionNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of NetNamespace


```php
function deleteNetworkOpenshiftIoV1CollectionNetNamespace(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 114;
$watch = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1CollectionNetNamespace($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getReadNetworkOpenshiftIoV1NetNamespace") getReadNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> read the specified NetNamespace


```php
function getReadNetworkOpenshiftIoV1NetNamespace(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the NetNamespace |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->getReadNetworkOpenshiftIoV1NetNamespace($name, $exact, $export, $pretty);

```


### <a name="update_replace_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.updateReplaceNetworkOpenshiftIoV1NetNamespace") updateReplaceNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified NetNamespace


```php
function updateReplaceNetworkOpenshiftIoV1NetNamespace(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetNamespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1NetNamespace();
$name = 'name';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->updateReplaceNetworkOpenshiftIoV1NetNamespace($body, $name, $pretty);

```


### <a name="delete_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.deleteNetworkOpenshiftIoV1NetNamespace") deleteNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> delete a NetNamespace


```php
function deleteNetworkOpenshiftIoV1NetNamespace(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetNamespace |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 72;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->deleteNetworkOpenshiftIoV1NetNamespace($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.patchNetworkOpenshiftIoV1NetNamespace") patchNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified NetNamespace


```php
function patchNetworkOpenshiftIoV1NetNamespace(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetNamespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $networkOpenshiftIoV1->patchNetworkOpenshiftIoV1NetNamespace($body, $name, $pretty);

```


### <a name="get_watch_network_openshift_io_v1_cluster_network_list"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1ClusterNetworkList") getWatchNetworkOpenshiftIoV1ClusterNetworkList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterNetwork


```php
function getWatchNetworkOpenshiftIoV1ClusterNetworkList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 72;
$watch = false;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1ClusterNetworkList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1ClusterNetwork") getWatchNetworkOpenshiftIoV1ClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterNetwork


```php
function getWatchNetworkOpenshiftIoV1ClusterNetwork(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterNetwork |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 72;
$watch = false;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1ClusterNetwork($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_egress_network_policy_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1EgressNetworkPolicyListForAllNamespaces") getWatchNetworkOpenshiftIoV1EgressNetworkPolicyListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of EgressNetworkPolicy


```php
function getWatchNetworkOpenshiftIoV1EgressNetworkPolicyListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 72;
$watch = false;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1EgressNetworkPolicyListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_host_subnet_list"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1HostSubnetList") getWatchNetworkOpenshiftIoV1HostSubnetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HostSubnet


```php
function getWatchNetworkOpenshiftIoV1HostSubnetList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1HostSubnetList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1HostSubnet") getWatchNetworkOpenshiftIoV1HostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind HostSubnet


```php
function getWatchNetworkOpenshiftIoV1HostSubnet(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HostSubnet |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1HostSubnet($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_namespaced_egress_network_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicyList") getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of EgressNetworkPolicy


```php
function getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicyList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicyList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy") getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind EgressNetworkPolicy


```php
function getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 164;
$watch = true;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1NamespacedEgressNetworkPolicy($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_net_namespace_list"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1NetNamespaceList") getWatchNetworkOpenshiftIoV1NetNamespaceList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of NetNamespace


```php
function getWatchNetworkOpenshiftIoV1NetNamespaceList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 0;
$watch = false;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1NetNamespaceList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_network_openshift_io_v1_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".NetworkOpenshiftIoV1Controller.getWatchNetworkOpenshiftIoV1NetNamespace") getWatchNetworkOpenshiftIoV1NetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind NetNamespace


```php
function getWatchNetworkOpenshiftIoV1NetNamespace(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the NetNamespace |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 0;
$watch = false;

$result = $networkOpenshiftIoV1->getWatchNetworkOpenshiftIoV1NetNamespace($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="oauth_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".OauthOpenshiftIoController") OauthOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` OauthOpenshiftIoController ``` class can be accessed from the API Client.

```php
$oauthOpenshiftIo = $client->getOauthOpenshiftIo();
```

### <a name="get_oauth_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoController.getOauthOpenshiftIoAPIGroup") getOauthOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getOauthOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $oauthOpenshiftIo->getOauthOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="oauth_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".OauthOpenshiftIoV1Controller") OauthOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` OauthOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$oauthOpenshiftIoV1 = $client->getOauthOpenshiftIoV1();
```

### <a name="get_oauth_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getOauthOpenshiftIoV1APIResources") getOauthOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getOauthOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $oauthOpenshiftIoV1->getOauthOpenshiftIoV1APIResources();

```


### <a name="list_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.listOauthOpenshiftIoV1OAuthAccessToken") listOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthAccessToken


```php
function listOauthOpenshiftIoV1OAuthAccessToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 213;
$watch = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->listOauthOpenshiftIoV1OAuthAccessToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.createOauthOpenshiftIoV1OAuthAccessToken") createOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthAccessToken


```php
function createOauthOpenshiftIoV1OAuthAccessToken(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAccessToken();
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->createOauthOpenshiftIoV1OAuthAccessToken($body, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_collection_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1CollectionOAuthAccessToken") deleteOauthOpenshiftIoV1CollectionOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthAccessToken


```php
function deleteOauthOpenshiftIoV1CollectionOAuthAccessToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 213;
$watch = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1CollectionOAuthAccessToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getReadOauthOpenshiftIoV1OAuthAccessToken") getReadOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthAccessToken


```php
function getReadOauthOpenshiftIoV1OAuthAccessToken(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAccessToken |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->getReadOauthOpenshiftIoV1OAuthAccessToken($name, $exact, $export, $pretty);

```


### <a name="update_replace_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.updateReplaceOauthOpenshiftIoV1OAuthAccessToken") updateReplaceOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthAccessToken


```php
function updateReplaceOauthOpenshiftIoV1OAuthAccessToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAccessToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAccessToken();
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->updateReplaceOauthOpenshiftIoV1OAuthAccessToken($body, $name, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1OAuthAccessToken") deleteOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthAccessToken


```php
function deleteOauthOpenshiftIoV1OAuthAccessToken(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAccessToken |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 213;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1OAuthAccessToken($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.patchOauthOpenshiftIoV1OAuthAccessToken") patchOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthAccessToken


```php
function patchOauthOpenshiftIoV1OAuthAccessToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAccessToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->patchOauthOpenshiftIoV1OAuthAccessToken($body, $name, $pretty);

```


### <a name="list_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.listOauthOpenshiftIoV1OAuthAuthorizeToken") listOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthAuthorizeToken


```php
function listOauthOpenshiftIoV1OAuthAuthorizeToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 50;
$watch = false;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->listOauthOpenshiftIoV1OAuthAuthorizeToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.createOauthOpenshiftIoV1OAuthAuthorizeToken") createOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthAuthorizeToken


```php
function createOauthOpenshiftIoV1OAuthAuthorizeToken(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAuthorizeToken();
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->createOauthOpenshiftIoV1OAuthAuthorizeToken($body, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_collection_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1CollectionOAuthAuthorizeToken") deleteOauthOpenshiftIoV1CollectionOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthAuthorizeToken


```php
function deleteOauthOpenshiftIoV1CollectionOAuthAuthorizeToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 50;
$watch = false;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1CollectionOAuthAuthorizeToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getReadOauthOpenshiftIoV1OAuthAuthorizeToken") getReadOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthAuthorizeToken


```php
function getReadOauthOpenshiftIoV1OAuthAuthorizeToken(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->getReadOauthOpenshiftIoV1OAuthAuthorizeToken($name, $exact, $export, $pretty);

```


### <a name="update_replace_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.updateReplaceOauthOpenshiftIoV1OAuthAuthorizeToken") updateReplaceOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthAuthorizeToken


```php
function updateReplaceOauthOpenshiftIoV1OAuthAuthorizeToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAuthorizeToken();
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->updateReplaceOauthOpenshiftIoV1OAuthAuthorizeToken($body, $name, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1OAuthAuthorizeToken") deleteOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthAuthorizeToken


```php
function deleteOauthOpenshiftIoV1OAuthAuthorizeToken(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 141;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1OAuthAuthorizeToken($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.patchOauthOpenshiftIoV1OAuthAuthorizeToken") patchOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthAuthorizeToken


```php
function patchOauthOpenshiftIoV1OAuthAuthorizeToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->patchOauthOpenshiftIoV1OAuthAuthorizeToken($body, $name, $pretty);

```


### <a name="list_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.listOauthOpenshiftIoV1OAuthClientAuthorization") listOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthClientAuthorization


```php
function listOauthOpenshiftIoV1OAuthClientAuthorization(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 141;
$watch = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->listOauthOpenshiftIoV1OAuthClientAuthorization($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.createOauthOpenshiftIoV1OAuthClientAuthorization") createOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthClientAuthorization


```php
function createOauthOpenshiftIoV1OAuthClientAuthorization(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClientAuthorization();
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->createOauthOpenshiftIoV1OAuthClientAuthorization($body, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_collection_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1CollectionOAuthClientAuthorization") deleteOauthOpenshiftIoV1CollectionOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthClientAuthorization


```php
function deleteOauthOpenshiftIoV1CollectionOAuthClientAuthorization(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 141;
$watch = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1CollectionOAuthClientAuthorization($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getReadOauthOpenshiftIoV1OAuthClientAuthorization") getReadOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthClientAuthorization


```php
function getReadOauthOpenshiftIoV1OAuthClientAuthorization(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->getReadOauthOpenshiftIoV1OAuthClientAuthorization($name, $exact, $export, $pretty);

```


### <a name="update_replace_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.updateReplaceOauthOpenshiftIoV1OAuthClientAuthorization") updateReplaceOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthClientAuthorization


```php
function updateReplaceOauthOpenshiftIoV1OAuthClientAuthorization(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClientAuthorization();
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->updateReplaceOauthOpenshiftIoV1OAuthClientAuthorization($body, $name, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1OAuthClientAuthorization") deleteOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthClientAuthorization


```php
function deleteOauthOpenshiftIoV1OAuthClientAuthorization(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 100;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1OAuthClientAuthorization($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.patchOauthOpenshiftIoV1OAuthClientAuthorization") patchOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthClientAuthorization


```php
function patchOauthOpenshiftIoV1OAuthClientAuthorization(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->patchOauthOpenshiftIoV1OAuthClientAuthorization($body, $name, $pretty);

```


### <a name="list_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.listOauthOpenshiftIoV1OAuthClient") listOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthClient


```php
function listOauthOpenshiftIoV1OAuthClient(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 100;
$watch = false;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->listOauthOpenshiftIoV1OAuthClient($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.createOauthOpenshiftIoV1OAuthClient") createOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthClient


```php
function createOauthOpenshiftIoV1OAuthClient(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClient();
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->createOauthOpenshiftIoV1OAuthClient($body, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_collection_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1CollectionOAuthClient") deleteOauthOpenshiftIoV1CollectionOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthClient


```php
function deleteOauthOpenshiftIoV1CollectionOAuthClient(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 100;
$watch = false;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1CollectionOAuthClient($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getReadOauthOpenshiftIoV1OAuthClient") getReadOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthClient


```php
function getReadOauthOpenshiftIoV1OAuthClient(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClient |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->getReadOauthOpenshiftIoV1OAuthClient($name, $exact, $export, $pretty);

```


### <a name="update_replace_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.updateReplaceOauthOpenshiftIoV1OAuthClient") updateReplaceOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthClient


```php
function updateReplaceOauthOpenshiftIoV1OAuthClient(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClient |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClient();
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->updateReplaceOauthOpenshiftIoV1OAuthClient($body, $name, $pretty);

```


### <a name="delete_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.deleteOauthOpenshiftIoV1OAuthClient") deleteOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthClient


```php
function deleteOauthOpenshiftIoV1OAuthClient(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClient |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 191;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->deleteOauthOpenshiftIoV1OAuthClient($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.patchOauthOpenshiftIoV1OAuthClient") patchOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthClient


```php
function patchOauthOpenshiftIoV1OAuthClient(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClient |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oauthOpenshiftIoV1->patchOauthOpenshiftIoV1OAuthClient($body, $name, $pretty);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_access_token_list"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthAccessTokenList") getWatchOauthOpenshiftIoV1OAuthAccessTokenList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthAccessToken


```php
function getWatchOauthOpenshiftIoV1OAuthAccessTokenList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 191;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthAccessTokenList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthAccessToken") getWatchOauthOpenshiftIoV1OAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthAccessToken


```php
function getWatchOauthOpenshiftIoV1OAuthAccessToken(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAccessToken |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 191;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthAccessToken($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_authorize_token_list"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthAuthorizeTokenList") getWatchOauthOpenshiftIoV1OAuthAuthorizeTokenList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthAuthorizeToken


```php
function getWatchOauthOpenshiftIoV1OAuthAuthorizeTokenList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthAuthorizeTokenList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthAuthorizeToken") getWatchOauthOpenshiftIoV1OAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthAuthorizeToken


```php
function getWatchOauthOpenshiftIoV1OAuthAuthorizeToken(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthAuthorizeToken($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_client_authorization_list"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthClientAuthorizationList") getWatchOauthOpenshiftIoV1OAuthClientAuthorizationList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthClientAuthorization


```php
function getWatchOauthOpenshiftIoV1OAuthClientAuthorizationList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthClientAuthorizationList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthClientAuthorization") getWatchOauthOpenshiftIoV1OAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthClientAuthorization


```php
function getWatchOauthOpenshiftIoV1OAuthClientAuthorization(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthClientAuthorization($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_client_list"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthClientList") getWatchOauthOpenshiftIoV1OAuthClientList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthClient


```php
function getWatchOauthOpenshiftIoV1OAuthClientList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 241;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthClientList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_oauth_openshift_io_v1_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OauthOpenshiftIoV1Controller.getWatchOauthOpenshiftIoV1OAuthClient") getWatchOauthOpenshiftIoV1OAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthClient


```php
function getWatchOauthOpenshiftIoV1OAuthClient(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClient |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 241;
$watch = true;

$result = $oauthOpenshiftIoV1->getWatchOauthOpenshiftIoV1OAuthClient($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="policy_controller"></a>![Class: ](https://apidocs.io/img/class.png ".PolicyController") PolicyController

### Get singleton instance

The singleton instance of the ``` PolicyController ``` class can be accessed from the API Client.

```php
$policy = $client->getPolicy();
```

### <a name="get_policy_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyController.getPolicyAPIGroup") getPolicyAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getPolicyAPIGroup()
```

#### Example Usage

```php

$result = $policy->getPolicyAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="policy_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".PolicyV1beta1Controller") PolicyV1beta1Controller

### Get singleton instance

The singleton instance of the ``` PolicyV1beta1Controller ``` class can be accessed from the API Client.

```php
$policyV1beta1 = $client->getPolicyV1beta1();
```

### <a name="get_policy_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.getPolicyV1beta1APIResources") getPolicyV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getPolicyV1beta1APIResources()
```

#### Example Usage

```php

$result = $policyV1beta1->getPolicyV1beta1APIResources();

```


### <a name="list_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.listPolicyV1beta1NamespacedPodDisruptionBudget") listPolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodDisruptionBudget


```php
function listPolicyV1beta1NamespacedPodDisruptionBudget(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 241;
$watch = true;
$pretty = 'pretty';

$result = $policyV1beta1->listPolicyV1beta1NamespacedPodDisruptionBudget($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.createPolicyV1beta1NamespacedPodDisruptionBudget") createPolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> create a PodDisruptionBudget


```php
function createPolicyV1beta1NamespacedPodDisruptionBudget(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1PodDisruptionBudget();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $policyV1beta1->createPolicyV1beta1NamespacedPodDisruptionBudget($body, $mnamespace, $pretty);

```


### <a name="delete_policy_v1beta1_collection_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.deletePolicyV1beta1CollectionNamespacedPodDisruptionBudget") deletePolicyV1beta1CollectionNamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PodDisruptionBudget


```php
function deletePolicyV1beta1CollectionNamespacedPodDisruptionBudget(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 77;
$watch = false;
$pretty = 'pretty';

$result = $policyV1beta1->deletePolicyV1beta1CollectionNamespacedPodDisruptionBudget($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.getReadPolicyV1beta1NamespacedPodDisruptionBudget") getReadPolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PodDisruptionBudget


```php
function getReadPolicyV1beta1NamespacedPodDisruptionBudget(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $policyV1beta1->getReadPolicyV1beta1NamespacedPodDisruptionBudget($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.updateReplacePolicyV1beta1NamespacedPodDisruptionBudget") updateReplacePolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PodDisruptionBudget


```php
function updateReplacePolicyV1beta1NamespacedPodDisruptionBudget(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1PodDisruptionBudget();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $policyV1beta1->updateReplacePolicyV1beta1NamespacedPodDisruptionBudget($body, $name, $mnamespace, $pretty);

```


### <a name="delete_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.deletePolicyV1beta1NamespacedPodDisruptionBudget") deletePolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> delete a PodDisruptionBudget


```php
function deletePolicyV1beta1NamespacedPodDisruptionBudget(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 77;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $policyV1beta1->deletePolicyV1beta1NamespacedPodDisruptionBudget($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.patchPolicyV1beta1NamespacedPodDisruptionBudget") patchPolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PodDisruptionBudget


```php
function patchPolicyV1beta1NamespacedPodDisruptionBudget(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $policyV1beta1->patchPolicyV1beta1NamespacedPodDisruptionBudget($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_policy_v1beta1_namespaced_pod_disruption_budget_status"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.getReadPolicyV1beta1NamespacedPodDisruptionBudgetStatus") getReadPolicyV1beta1NamespacedPodDisruptionBudgetStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified PodDisruptionBudget


```php
function getReadPolicyV1beta1NamespacedPodDisruptionBudgetStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $policyV1beta1->getReadPolicyV1beta1NamespacedPodDisruptionBudgetStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_policy_v1beta1_namespaced_pod_disruption_budget_status"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.updateReplacePolicyV1beta1NamespacedPodDisruptionBudgetStatus") updateReplacePolicyV1beta1NamespacedPodDisruptionBudgetStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified PodDisruptionBudget


```php
function updateReplacePolicyV1beta1NamespacedPodDisruptionBudgetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1PodDisruptionBudget();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $policyV1beta1->updateReplacePolicyV1beta1NamespacedPodDisruptionBudgetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_policy_v1beta1_namespaced_pod_disruption_budget_status"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.patchPolicyV1beta1NamespacedPodDisruptionBudgetStatus") patchPolicyV1beta1NamespacedPodDisruptionBudgetStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified PodDisruptionBudget


```php
function patchPolicyV1beta1NamespacedPodDisruptionBudgetStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $policyV1beta1->patchPolicyV1beta1NamespacedPodDisruptionBudgetStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_policy_v1beta1_pod_disruption_budget_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.listPolicyV1beta1PodDisruptionBudgetForAllNamespaces") listPolicyV1beta1PodDisruptionBudgetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodDisruptionBudget


```php
function listPolicyV1beta1PodDisruptionBudgetForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 36;
$watch = false;
$pretty = 'pretty';

$result = $policyV1beta1->listPolicyV1beta1PodDisruptionBudgetForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_policy_v1beta1_pod_disruption_budget_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.createPolicyV1beta1PodDisruptionBudgetForAllNamespaces") createPolicyV1beta1PodDisruptionBudgetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodDisruptionBudget


```php
function createPolicyV1beta1PodDisruptionBudgetForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1PodDisruptionBudget();
$pretty = 'pretty';

$result = $policyV1beta1->createPolicyV1beta1PodDisruptionBudgetForAllNamespaces($body, $pretty);

```


### <a name="get_watch_policy_v1beta1_namespaced_pod_disruption_budget_list"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.getWatchPolicyV1beta1NamespacedPodDisruptionBudgetList") getWatchPolicyV1beta1NamespacedPodDisruptionBudgetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodDisruptionBudget


```php
function getWatchPolicyV1beta1NamespacedPodDisruptionBudgetList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 127;
$watch = true;

$result = $policyV1beta1->getWatchPolicyV1beta1NamespacedPodDisruptionBudgetList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_policy_v1beta1_namespaced_pod_disruption_budget"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.getWatchPolicyV1beta1NamespacedPodDisruptionBudget") getWatchPolicyV1beta1NamespacedPodDisruptionBudget

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PodDisruptionBudget


```php
function getWatchPolicyV1beta1NamespacedPodDisruptionBudget(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodDisruptionBudget |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 127;
$watch = true;

$result = $policyV1beta1->getWatchPolicyV1beta1NamespacedPodDisruptionBudget($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_policy_v1beta1_pod_disruption_budget_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".PolicyV1beta1Controller.getWatchPolicyV1beta1PodDisruptionBudgetListForAllNamespaces") getWatchPolicyV1beta1PodDisruptionBudgetListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodDisruptionBudget


```php
function getWatchPolicyV1beta1PodDisruptionBudgetListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 127;
$watch = true;

$result = $policyV1beta1->getWatchPolicyV1beta1PodDisruptionBudgetListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="project_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ProjectOpenshiftIoController") ProjectOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` ProjectOpenshiftIoController ``` class can be accessed from the API Client.

```php
$projectOpenshiftIo = $client->getProjectOpenshiftIo();
```

### <a name="get_project_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoController.getProjectOpenshiftIoAPIGroup") getProjectOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getProjectOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $projectOpenshiftIo->getProjectOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="project_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".ProjectOpenshiftIoV1Controller") ProjectOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` ProjectOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$projectOpenshiftIoV1 = $client->getProjectOpenshiftIoV1();
```

### <a name="get_project_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.getProjectOpenshiftIoV1APIResources") getProjectOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getProjectOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $projectOpenshiftIoV1->getProjectOpenshiftIoV1APIResources();

```


### <a name="list_project_openshift_io_v1_project_request"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.listProjectOpenshiftIoV1ProjectRequest") listProjectOpenshiftIoV1ProjectRequest

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ProjectRequest


```php
function listProjectOpenshiftIoV1ProjectRequest(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 127;
$watch = true;
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->listProjectOpenshiftIoV1ProjectRequest($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_project_openshift_io_v1_project_request"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.createProjectOpenshiftIoV1ProjectRequest") createProjectOpenshiftIoV1ProjectRequest

> *Tags:*  ``` Skips Authentication ``` 

> create a ProjectRequest


```php
function createProjectOpenshiftIoV1ProjectRequest(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ProjectRequest();
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->createProjectOpenshiftIoV1ProjectRequest($body, $pretty);

```


### <a name="list_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.listProjectOpenshiftIoV1Project") listProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Project


```php
function listProjectOpenshiftIoV1Project(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 219;
$watch = true;
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->listProjectOpenshiftIoV1Project($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.createProjectOpenshiftIoV1Project") createProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> create a Project


```php
function createProjectOpenshiftIoV1Project(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->createProjectOpenshiftIoV1Project($body, $pretty);

```


### <a name="get_read_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.getReadProjectOpenshiftIoV1Project") getReadProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Project


```php
function getReadProjectOpenshiftIoV1Project(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->getReadProjectOpenshiftIoV1Project($name, $pretty);

```


### <a name="update_replace_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.updateReplaceProjectOpenshiftIoV1Project") updateReplaceProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Project


```php
function updateReplaceProjectOpenshiftIoV1Project(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$name = 'name';
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->updateReplaceProjectOpenshiftIoV1Project($body, $name, $pretty);

```


### <a name="delete_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.deleteProjectOpenshiftIoV1Project") deleteProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> delete a Project


```php
function deleteProjectOpenshiftIoV1Project(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->deleteProjectOpenshiftIoV1Project($name, $pretty);

```


### <a name="patch_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.patchProjectOpenshiftIoV1Project") patchProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Project


```php
function patchProjectOpenshiftIoV1Project(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $projectOpenshiftIoV1->patchProjectOpenshiftIoV1Project($body, $name, $pretty);

```


### <a name="get_watch_project_openshift_io_v1_project_list"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.getWatchProjectOpenshiftIoV1ProjectList") getWatchProjectOpenshiftIoV1ProjectList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Project


```php
function getWatchProjectOpenshiftIoV1ProjectList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 219;
$watch = true;

$result = $projectOpenshiftIoV1->getWatchProjectOpenshiftIoV1ProjectList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_project_openshift_io_v1_project"></a>![Method: ](https://apidocs.io/img/method.png ".ProjectOpenshiftIoV1Controller.getWatchProjectOpenshiftIoV1Project") getWatchProjectOpenshiftIoV1Project

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Project


```php
function getWatchProjectOpenshiftIoV1Project(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Project |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 177;
$watch = true;

$result = $projectOpenshiftIoV1->getWatchProjectOpenshiftIoV1Project($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="quota_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".QuotaOpenshiftIoController") QuotaOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` QuotaOpenshiftIoController ``` class can be accessed from the API Client.

```php
$quotaOpenshiftIo = $client->getQuotaOpenshiftIo();
```

### <a name="get_quota_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoController.getQuotaOpenshiftIoAPIGroup") getQuotaOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getQuotaOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $quotaOpenshiftIo->getQuotaOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="quota_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".QuotaOpenshiftIoV1Controller") QuotaOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` QuotaOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$quotaOpenshiftIoV1 = $client->getQuotaOpenshiftIoV1();
```

### <a name="get_quota_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.getQuotaOpenshiftIoV1APIResources") getQuotaOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getQuotaOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $quotaOpenshiftIoV1->getQuotaOpenshiftIoV1APIResources();

```


### <a name="list_quota_openshift_io_v1_applied_cluster_resource_quota_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.listQuotaOpenshiftIoV1AppliedClusterResourceQuotaForAllNamespaces") listQuotaOpenshiftIoV1AppliedClusterResourceQuotaForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind AppliedClusterResourceQuota


```php
function listQuotaOpenshiftIoV1AppliedClusterResourceQuotaForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 177;
$watch = true;

$result = $quotaOpenshiftIoV1->listQuotaOpenshiftIoV1AppliedClusterResourceQuotaForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="list_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.listQuotaOpenshiftIoV1ClusterResourceQuota") listQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterResourceQuota


```php
function listQuotaOpenshiftIoV1ClusterResourceQuota(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 177;
$watch = true;
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->listQuotaOpenshiftIoV1ClusterResourceQuota($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.createQuotaOpenshiftIoV1ClusterResourceQuota") createQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterResourceQuota


```php
function createQuotaOpenshiftIoV1ClusterResourceQuota(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1AppliedClusterResourceQuota();
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->createQuotaOpenshiftIoV1ClusterResourceQuota($body, $pretty);

```


### <a name="delete_quota_openshift_io_v1_collection_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.deleteQuotaOpenshiftIoV1CollectionClusterResourceQuota") deleteQuotaOpenshiftIoV1CollectionClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterResourceQuota


```php
function deleteQuotaOpenshiftIoV1CollectionClusterResourceQuota(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 177;
$watch = true;
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->deleteQuotaOpenshiftIoV1CollectionClusterResourceQuota($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.getReadQuotaOpenshiftIoV1ClusterResourceQuota") getReadQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterResourceQuota


```php
function getReadQuotaOpenshiftIoV1ClusterResourceQuota(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->getReadQuotaOpenshiftIoV1ClusterResourceQuota($name, $exact, $export, $pretty);

```


### <a name="update_replace_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.updateReplaceQuotaOpenshiftIoV1ClusterResourceQuota") updateReplaceQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterResourceQuota


```php
function updateReplaceQuotaOpenshiftIoV1ClusterResourceQuota(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1AppliedClusterResourceQuota();
$name = 'name';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->updateReplaceQuotaOpenshiftIoV1ClusterResourceQuota($body, $name, $pretty);

```


### <a name="delete_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.deleteQuotaOpenshiftIoV1ClusterResourceQuota") deleteQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterResourceQuota


```php
function deleteQuotaOpenshiftIoV1ClusterResourceQuota(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 14;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->deleteQuotaOpenshiftIoV1ClusterResourceQuota($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.patchQuotaOpenshiftIoV1ClusterResourceQuota") patchQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterResourceQuota


```php
function patchQuotaOpenshiftIoV1ClusterResourceQuota(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->patchQuotaOpenshiftIoV1ClusterResourceQuota($body, $name, $pretty);

```


### <a name="get_read_quota_openshift_io_v1_cluster_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.getReadQuotaOpenshiftIoV1ClusterResourceQuotaStatus") getReadQuotaOpenshiftIoV1ClusterResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ClusterResourceQuota


```php
function getReadQuotaOpenshiftIoV1ClusterResourceQuotaStatus(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->getReadQuotaOpenshiftIoV1ClusterResourceQuotaStatus($name, $pretty);

```


### <a name="update_replace_quota_openshift_io_v1_cluster_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.updateReplaceQuotaOpenshiftIoV1ClusterResourceQuotaStatus") updateReplaceQuotaOpenshiftIoV1ClusterResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ClusterResourceQuota


```php
function updateReplaceQuotaOpenshiftIoV1ClusterResourceQuotaStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1AppliedClusterResourceQuota();
$name = 'name';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->updateReplaceQuotaOpenshiftIoV1ClusterResourceQuotaStatus($body, $name, $pretty);

```


### <a name="patch_quota_openshift_io_v1_cluster_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.patchQuotaOpenshiftIoV1ClusterResourceQuotaStatus") patchQuotaOpenshiftIoV1ClusterResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ClusterResourceQuota


```php
function patchQuotaOpenshiftIoV1ClusterResourceQuotaStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->patchQuotaOpenshiftIoV1ClusterResourceQuotaStatus($body, $name, $pretty);

```


### <a name="list_quota_openshift_io_v1_namespaced_applied_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.listQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota") listQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind AppliedClusterResourceQuota


```php
function listQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 227;
$watch = true;

$result = $quotaOpenshiftIoV1->listQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_read_quota_openshift_io_v1_namespaced_applied_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.getReadQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota") getReadQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> read the specified AppliedClusterResourceQuota


```php
function getReadQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the AppliedClusterResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $quotaOpenshiftIoV1->getReadQuotaOpenshiftIoV1NamespacedAppliedClusterResourceQuota($name, $mnamespace, $pretty);

```


### <a name="get_watch_quota_openshift_io_v1_cluster_resource_quota_list"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.getWatchQuotaOpenshiftIoV1ClusterResourceQuotaList") getWatchQuotaOpenshiftIoV1ClusterResourceQuotaList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterResourceQuota


```php
function getWatchQuotaOpenshiftIoV1ClusterResourceQuotaList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 227;
$watch = true;

$result = $quotaOpenshiftIoV1->getWatchQuotaOpenshiftIoV1ClusterResourceQuotaList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_quota_openshift_io_v1_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".QuotaOpenshiftIoV1Controller.getWatchQuotaOpenshiftIoV1ClusterResourceQuota") getWatchQuotaOpenshiftIoV1ClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterResourceQuota


```php
function getWatchQuotaOpenshiftIoV1ClusterResourceQuota(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 227;
$watch = true;

$result = $quotaOpenshiftIoV1->getWatchQuotaOpenshiftIoV1ClusterResourceQuota($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="rbac_authorization_controller"></a>![Class: ](https://apidocs.io/img/class.png ".RbacAuthorizationController") RbacAuthorizationController

### Get singleton instance

The singleton instance of the ``` RbacAuthorizationController ``` class can be accessed from the API Client.

```php
$rbacAuthorization = $client->getRbacAuthorization();
```

### <a name="get_rbac_authorization_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationController.getRbacAuthorizationAPIGroup") getRbacAuthorizationAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getRbacAuthorizationAPIGroup()
```

#### Example Usage

```php

$result = $rbacAuthorization->getRbacAuthorizationAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="rbac_authorization_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".RbacAuthorizationV1beta1Controller") RbacAuthorizationV1beta1Controller

### Get singleton instance

The singleton instance of the ``` RbacAuthorizationV1beta1Controller ``` class can be accessed from the API Client.

```php
$rbacAuthorizationV1beta1 = $client->getRbacAuthorizationV1beta1();
```

### <a name="get_rbac_authorization_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getRbacAuthorizationV1beta1APIResources") getRbacAuthorizationV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getRbacAuthorizationV1beta1APIResources()
```

#### Example Usage

```php

$result = $rbacAuthorizationV1beta1->getRbacAuthorizationV1beta1APIResources();

```


### <a name="list_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.listRbacAuthorizationV1beta1ClusterRoleBinding") listRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterRoleBinding


```php
function listRbacAuthorizationV1beta1ClusterRoleBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 227;
$watch = true;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->listRbacAuthorizationV1beta1ClusterRoleBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.createRbacAuthorizationV1beta1ClusterRoleBinding") createRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterRoleBinding


```php
function createRbacAuthorizationV1beta1ClusterRoleBinding(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRoleBinding();
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->createRbacAuthorizationV1beta1ClusterRoleBinding($body, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_collection_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1CollectionClusterRoleBinding") deleteRbacAuthorizationV1beta1CollectionClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterRoleBinding


```php
function deleteRbacAuthorizationV1beta1CollectionClusterRoleBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 63;
$watch = false;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1CollectionClusterRoleBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getReadRbacAuthorizationV1beta1ClusterRoleBinding") getReadRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterRoleBinding


```php
function getReadRbacAuthorizationV1beta1ClusterRoleBinding(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->getReadRbacAuthorizationV1beta1ClusterRoleBinding($name, $pretty);

```


### <a name="update_replace_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.updateReplaceRbacAuthorizationV1beta1ClusterRoleBinding") updateReplaceRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterRoleBinding


```php
function updateReplaceRbacAuthorizationV1beta1ClusterRoleBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRoleBinding();
$name = 'name';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->updateReplaceRbacAuthorizationV1beta1ClusterRoleBinding($body, $name, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1ClusterRoleBinding") deleteRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterRoleBinding


```php
function deleteRbacAuthorizationV1beta1ClusterRoleBinding(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 63;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1ClusterRoleBinding($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.patchRbacAuthorizationV1beta1ClusterRoleBinding") patchRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterRoleBinding


```php
function patchRbacAuthorizationV1beta1ClusterRoleBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->patchRbacAuthorizationV1beta1ClusterRoleBinding($body, $name, $pretty);

```


### <a name="list_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.listRbacAuthorizationV1beta1ClusterRole") listRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterRole


```php
function listRbacAuthorizationV1beta1ClusterRole(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 155;
$watch = true;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->listRbacAuthorizationV1beta1ClusterRole($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.createRbacAuthorizationV1beta1ClusterRole") createRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterRole


```php
function createRbacAuthorizationV1beta1ClusterRole(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRole();
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->createRbacAuthorizationV1beta1ClusterRole($body, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_collection_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1CollectionClusterRole") deleteRbacAuthorizationV1beta1CollectionClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterRole


```php
function deleteRbacAuthorizationV1beta1CollectionClusterRole(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 155;
$watch = true;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1CollectionClusterRole($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getReadRbacAuthorizationV1beta1ClusterRole") getReadRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterRole


```php
function getReadRbacAuthorizationV1beta1ClusterRole(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->getReadRbacAuthorizationV1beta1ClusterRole($name, $pretty);

```


### <a name="update_replace_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.updateReplaceRbacAuthorizationV1beta1ClusterRole") updateReplaceRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterRole


```php
function updateReplaceRbacAuthorizationV1beta1ClusterRole(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRole();
$name = 'name';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->updateReplaceRbacAuthorizationV1beta1ClusterRole($body, $name, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1ClusterRole") deleteRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterRole


```php
function deleteRbacAuthorizationV1beta1ClusterRole(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 155;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1ClusterRole($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.patchRbacAuthorizationV1beta1ClusterRole") patchRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterRole


```php
function patchRbacAuthorizationV1beta1ClusterRole(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->patchRbacAuthorizationV1beta1ClusterRole($body, $name, $pretty);

```


### <a name="list_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.listRbacAuthorizationV1beta1NamespacedRoleBinding") listRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind RoleBinding


```php
function listRbacAuthorizationV1beta1NamespacedRoleBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 113;
$watch = false;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->listRbacAuthorizationV1beta1NamespacedRoleBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.createRbacAuthorizationV1beta1NamespacedRoleBinding") createRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBinding


```php
function createRbacAuthorizationV1beta1NamespacedRoleBinding(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRoleBinding();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->createRbacAuthorizationV1beta1NamespacedRoleBinding($body, $mnamespace, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_collection_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1CollectionNamespacedRoleBinding") deleteRbacAuthorizationV1beta1CollectionNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of RoleBinding


```php
function deleteRbacAuthorizationV1beta1CollectionNamespacedRoleBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 113;
$watch = false;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1CollectionNamespacedRoleBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getReadRbacAuthorizationV1beta1NamespacedRoleBinding") getReadRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified RoleBinding


```php
function getReadRbacAuthorizationV1beta1NamespacedRoleBinding(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->getReadRbacAuthorizationV1beta1NamespacedRoleBinding($name, $mnamespace, $pretty);

```


### <a name="update_replace_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.updateReplaceRbacAuthorizationV1beta1NamespacedRoleBinding") updateReplaceRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified RoleBinding


```php
function updateReplaceRbacAuthorizationV1beta1NamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRoleBinding();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->updateReplaceRbacAuthorizationV1beta1NamespacedRoleBinding($body, $name, $mnamespace, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1NamespacedRoleBinding") deleteRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a RoleBinding


```php
function deleteRbacAuthorizationV1beta1NamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 205;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1NamespacedRoleBinding($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.patchRbacAuthorizationV1beta1NamespacedRoleBinding") patchRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified RoleBinding


```php
function patchRbacAuthorizationV1beta1NamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->patchRbacAuthorizationV1beta1NamespacedRoleBinding($body, $name, $mnamespace, $pretty);

```


### <a name="list_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.listRbacAuthorizationV1beta1NamespacedRole") listRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Role


```php
function listRbacAuthorizationV1beta1NamespacedRole(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 205;
$watch = true;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->listRbacAuthorizationV1beta1NamespacedRole($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.createRbacAuthorizationV1beta1NamespacedRole") createRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> create a Role


```php
function createRbacAuthorizationV1beta1NamespacedRole(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRole();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->createRbacAuthorizationV1beta1NamespacedRole($body, $mnamespace, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_collection_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1CollectionNamespacedRole") deleteRbacAuthorizationV1beta1CollectionNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Role


```php
function deleteRbacAuthorizationV1beta1CollectionNamespacedRole(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 41;
$watch = false;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1CollectionNamespacedRole($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getReadRbacAuthorizationV1beta1NamespacedRole") getReadRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Role


```php
function getReadRbacAuthorizationV1beta1NamespacedRole(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->getReadRbacAuthorizationV1beta1NamespacedRole($name, $mnamespace, $pretty);

```


### <a name="update_replace_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.updateReplaceRbacAuthorizationV1beta1NamespacedRole") updateReplaceRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Role


```php
function updateReplaceRbacAuthorizationV1beta1NamespacedRole(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRole();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->updateReplaceRbacAuthorizationV1beta1NamespacedRole($body, $name, $mnamespace, $pretty);

```


### <a name="delete_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.deleteRbacAuthorizationV1beta1NamespacedRole") deleteRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> delete a Role


```php
function deleteRbacAuthorizationV1beta1NamespacedRole(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 41;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->deleteRbacAuthorizationV1beta1NamespacedRole($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.patchRbacAuthorizationV1beta1NamespacedRole") patchRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Role


```php
function patchRbacAuthorizationV1beta1NamespacedRole(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->patchRbacAuthorizationV1beta1NamespacedRole($body, $name, $mnamespace, $pretty);

```


### <a name="list_rbac_authorization_v1beta1_role_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.listRbacAuthorizationV1beta1RoleBindingForAllNamespaces") listRbacAuthorizationV1beta1RoleBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind RoleBinding


```php
function listRbacAuthorizationV1beta1RoleBindingForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 41;
$watch = false;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->listRbacAuthorizationV1beta1RoleBindingForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_rbac_authorization_v1beta1_role_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.createRbacAuthorizationV1beta1RoleBindingForAllNamespaces") createRbacAuthorizationV1beta1RoleBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBinding


```php
function createRbacAuthorizationV1beta1RoleBindingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRoleBinding();
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->createRbacAuthorizationV1beta1RoleBindingForAllNamespaces($body, $pretty);

```


### <a name="list_rbac_authorization_v1beta1_role_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.listRbacAuthorizationV1beta1RoleForAllNamespaces") listRbacAuthorizationV1beta1RoleForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Role


```php
function listRbacAuthorizationV1beta1RoleForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 254;
$watch = true;
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->listRbacAuthorizationV1beta1RoleForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_rbac_authorization_v1beta1_role_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.createRbacAuthorizationV1beta1RoleForAllNamespaces") createRbacAuthorizationV1beta1RoleForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Role


```php
function createRbacAuthorizationV1beta1RoleForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1ClusterRole();
$pretty = 'pretty';

$result = $rbacAuthorizationV1beta1->createRbacAuthorizationV1beta1RoleForAllNamespaces($body, $pretty);

```


### <a name="get_watch_rbac_authorization_v1beta1_cluster_role_binding_list"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1ClusterRoleBindingList") getWatchRbacAuthorizationV1beta1ClusterRoleBindingList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterRoleBinding


```php
function getWatchRbacAuthorizationV1beta1ClusterRoleBindingList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 254;
$watch = true;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1ClusterRoleBindingList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1ClusterRoleBinding") getWatchRbacAuthorizationV1beta1ClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterRoleBinding


```php
function getWatchRbacAuthorizationV1beta1ClusterRoleBinding(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 254;
$watch = true;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1ClusterRoleBinding($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_cluster_role_list"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1ClusterRoleList") getWatchRbacAuthorizationV1beta1ClusterRoleList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterRole


```php
function getWatchRbacAuthorizationV1beta1ClusterRoleList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 91;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1ClusterRoleList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1ClusterRole") getWatchRbacAuthorizationV1beta1ClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterRole


```php
function getWatchRbacAuthorizationV1beta1ClusterRole(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRole |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 91;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1ClusterRole($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_namespaced_role_binding_list"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1NamespacedRoleBindingList") getWatchRbacAuthorizationV1beta1NamespacedRoleBindingList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of RoleBinding


```php
function getWatchRbacAuthorizationV1beta1NamespacedRoleBindingList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 91;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1NamespacedRoleBindingList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1NamespacedRoleBinding") getWatchRbacAuthorizationV1beta1NamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind RoleBinding


```php
function getWatchRbacAuthorizationV1beta1NamespacedRoleBinding(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 91;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1NamespacedRoleBinding($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_namespaced_role_list"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1NamespacedRoleList") getWatchRbacAuthorizationV1beta1NamespacedRoleList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Role


```php
function getWatchRbacAuthorizationV1beta1NamespacedRoleList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1NamespacedRoleList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1NamespacedRole") getWatchRbacAuthorizationV1beta1NamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Role


```php
function getWatchRbacAuthorizationV1beta1NamespacedRole(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1NamespacedRole($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_role_binding_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1RoleBindingListForAllNamespaces") getWatchRbacAuthorizationV1beta1RoleBindingListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of RoleBinding


```php
function getWatchRbacAuthorizationV1beta1RoleBindingListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1RoleBindingListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_rbac_authorization_v1beta1_role_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RbacAuthorizationV1beta1Controller.getWatchRbacAuthorizationV1beta1RoleListForAllNamespaces") getWatchRbacAuthorizationV1beta1RoleListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Role


```php
function getWatchRbacAuthorizationV1beta1RoleListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $rbacAuthorizationV1beta1->getWatchRbacAuthorizationV1beta1RoleListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="route_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".RouteOpenshiftIoController") RouteOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` RouteOpenshiftIoController ``` class can be accessed from the API Client.

```php
$routeOpenshiftIo = $client->getRouteOpenshiftIo();
```

### <a name="get_route_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoController.getRouteOpenshiftIoAPIGroup") getRouteOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getRouteOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $routeOpenshiftIo->getRouteOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="route_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".RouteOpenshiftIoV1Controller") RouteOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` RouteOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$routeOpenshiftIoV1 = $client->getRouteOpenshiftIoV1();
```

### <a name="get_route_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.getRouteOpenshiftIoV1APIResources") getRouteOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getRouteOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $routeOpenshiftIoV1->getRouteOpenshiftIoV1APIResources();

```


### <a name="list_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.listRouteOpenshiftIoV1NamespacedRoute") listRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Route


```php
function listRouteOpenshiftIoV1NamespacedRoute(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 141;
$watch = true;
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->listRouteOpenshiftIoV1NamespacedRoute($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.createRouteOpenshiftIoV1NamespacedRoute") createRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> create a Route


```php
function createRouteOpenshiftIoV1NamespacedRoute(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->createRouteOpenshiftIoV1NamespacedRoute($body, $mnamespace, $pretty);

```


### <a name="delete_route_openshift_io_v1_collection_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.deleteRouteOpenshiftIoV1CollectionNamespacedRoute") deleteRouteOpenshiftIoV1CollectionNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Route


```php
function deleteRouteOpenshiftIoV1CollectionNamespacedRoute(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 141;
$watch = true;
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->deleteRouteOpenshiftIoV1CollectionNamespacedRoute($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.getReadRouteOpenshiftIoV1NamespacedRoute") getReadRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Route


```php
function getReadRouteOpenshiftIoV1NamespacedRoute(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->getReadRouteOpenshiftIoV1NamespacedRoute($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.updateReplaceRouteOpenshiftIoV1NamespacedRoute") updateReplaceRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Route


```php
function updateReplaceRouteOpenshiftIoV1NamespacedRoute(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->updateReplaceRouteOpenshiftIoV1NamespacedRoute($body, $name, $mnamespace, $pretty);

```


### <a name="delete_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.deleteRouteOpenshiftIoV1NamespacedRoute") deleteRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> delete a Route


```php
function deleteRouteOpenshiftIoV1NamespacedRoute(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 232;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->deleteRouteOpenshiftIoV1NamespacedRoute($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.patchRouteOpenshiftIoV1NamespacedRoute") patchRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Route


```php
function patchRouteOpenshiftIoV1NamespacedRoute(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->patchRouteOpenshiftIoV1NamespacedRoute($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_route_openshift_io_v1_namespaced_route_status"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.getReadRouteOpenshiftIoV1NamespacedRouteStatus") getReadRouteOpenshiftIoV1NamespacedRouteStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Route


```php
function getReadRouteOpenshiftIoV1NamespacedRouteStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->getReadRouteOpenshiftIoV1NamespacedRouteStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_route_openshift_io_v1_namespaced_route_status"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.updateReplaceRouteOpenshiftIoV1NamespacedRouteStatus") updateReplaceRouteOpenshiftIoV1NamespacedRouteStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Route


```php
function updateReplaceRouteOpenshiftIoV1NamespacedRouteStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->updateReplaceRouteOpenshiftIoV1NamespacedRouteStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_route_openshift_io_v1_namespaced_route_status"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.patchRouteOpenshiftIoV1NamespacedRouteStatus") patchRouteOpenshiftIoV1NamespacedRouteStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Route


```php
function patchRouteOpenshiftIoV1NamespacedRouteStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->patchRouteOpenshiftIoV1NamespacedRouteStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_route_openshift_io_v1_route_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.listRouteOpenshiftIoV1RouteForAllNamespaces") listRouteOpenshiftIoV1RouteForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Route


```php
function listRouteOpenshiftIoV1RouteForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 232;
$watch = true;
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->listRouteOpenshiftIoV1RouteForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_route_openshift_io_v1_route_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.createRouteOpenshiftIoV1RouteForAllNamespaces") createRouteOpenshiftIoV1RouteForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Route


```php
function createRouteOpenshiftIoV1RouteForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$pretty = 'pretty';

$result = $routeOpenshiftIoV1->createRouteOpenshiftIoV1RouteForAllNamespaces($body, $pretty);

```


### <a name="get_watch_route_openshift_io_v1_namespaced_route_list"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.getWatchRouteOpenshiftIoV1NamespacedRouteList") getWatchRouteOpenshiftIoV1NamespacedRouteList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Route


```php
function getWatchRouteOpenshiftIoV1NamespacedRouteList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 190;
$watch = true;

$result = $routeOpenshiftIoV1->getWatchRouteOpenshiftIoV1NamespacedRouteList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_route_openshift_io_v1_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.getWatchRouteOpenshiftIoV1NamespacedRoute") getWatchRouteOpenshiftIoV1NamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Route


```php
function getWatchRouteOpenshiftIoV1NamespacedRoute(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 190;
$watch = true;

$result = $routeOpenshiftIoV1->getWatchRouteOpenshiftIoV1NamespacedRoute($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_route_openshift_io_v1_route_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".RouteOpenshiftIoV1Controller.getWatchRouteOpenshiftIoV1RouteListForAllNamespaces") getWatchRouteOpenshiftIoV1RouteListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Route


```php
function getWatchRouteOpenshiftIoV1RouteListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 190;
$watch = true;

$result = $routeOpenshiftIoV1->getWatchRouteOpenshiftIoV1RouteListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="security_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".SecurityOpenshiftIoController") SecurityOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` SecurityOpenshiftIoController ``` class can be accessed from the API Client.

```php
$securityOpenshiftIo = $client->getSecurityOpenshiftIo();
```

### <a name="get_security_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoController.getSecurityOpenshiftIoAPIGroup") getSecurityOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getSecurityOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $securityOpenshiftIo->getSecurityOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="security_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".SecurityOpenshiftIoV1Controller") SecurityOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` SecurityOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$securityOpenshiftIoV1 = $client->getSecurityOpenshiftIoV1();
```

### <a name="get_security_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.getSecurityOpenshiftIoV1APIResources") getSecurityOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getSecurityOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $securityOpenshiftIoV1->getSecurityOpenshiftIoV1APIResources();

```


### <a name="create_security_openshift_io_v1_namespaced_pod_security_policy_review"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.createSecurityOpenshiftIoV1NamespacedPodSecurityPolicyReview") createSecurityOpenshiftIoV1NamespacedPodSecurityPolicyReview

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicyReview


```php
function createSecurityOpenshiftIoV1NamespacedPodSecurityPolicyReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicyReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $securityOpenshiftIoV1->createSecurityOpenshiftIoV1NamespacedPodSecurityPolicyReview($body, $mnamespace, $pretty);

```


### <a name="create_security_openshift_io_v1_namespaced_pod_security_policy_self_subject_review"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySelfSubjectReview") createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySelfSubjectReview

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySelfSubjectReview


```php
function createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySelfSubjectReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySelfSubjectReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $securityOpenshiftIoV1->createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySelfSubjectReview($body, $mnamespace, $pretty);

```


### <a name="create_security_openshift_io_v1_namespaced_pod_security_policy_subject_review"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySubjectReview") createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySubjectReview

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySubjectReview


```php
function createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySubjectReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySubjectReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $securityOpenshiftIoV1->createSecurityOpenshiftIoV1NamespacedPodSecurityPolicySubjectReview($body, $mnamespace, $pretty);

```


### <a name="create_security_openshift_io_v1_pod_security_policy_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.createSecurityOpenshiftIoV1PodSecurityPolicyReviewForAllNamespaces") createSecurityOpenshiftIoV1PodSecurityPolicyReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicyReview


```php
function createSecurityOpenshiftIoV1PodSecurityPolicyReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicyReview();
$pretty = 'pretty';

$result = $securityOpenshiftIoV1->createSecurityOpenshiftIoV1PodSecurityPolicyReviewForAllNamespaces($body, $pretty);

```


### <a name="create_security_openshift_io_v1_pod_security_policy_self_subject_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.createSecurityOpenshiftIoV1PodSecurityPolicySelfSubjectReviewForAllNamespaces") createSecurityOpenshiftIoV1PodSecurityPolicySelfSubjectReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySelfSubjectReview


```php
function createSecurityOpenshiftIoV1PodSecurityPolicySelfSubjectReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySelfSubjectReview();
$pretty = 'pretty';

$result = $securityOpenshiftIoV1->createSecurityOpenshiftIoV1PodSecurityPolicySelfSubjectReviewForAllNamespaces($body, $pretty);

```


### <a name="create_security_openshift_io_v1_pod_security_policy_subject_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".SecurityOpenshiftIoV1Controller.createSecurityOpenshiftIoV1PodSecurityPolicySubjectReviewForAllNamespaces") createSecurityOpenshiftIoV1PodSecurityPolicySubjectReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySubjectReview


```php
function createSecurityOpenshiftIoV1PodSecurityPolicySubjectReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySubjectReview();
$pretty = 'pretty';

$result = $securityOpenshiftIoV1->createSecurityOpenshiftIoV1PodSecurityPolicySubjectReviewForAllNamespaces($body, $pretty);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="settings_controller"></a>![Class: ](https://apidocs.io/img/class.png ".SettingsController") SettingsController

### Get singleton instance

The singleton instance of the ``` SettingsController ``` class can be accessed from the API Client.

```php
$settings = $client->getSettings();
```

### <a name="get_settings_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsController.getSettingsAPIGroup") getSettingsAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getSettingsAPIGroup()
```

#### Example Usage

```php

$result = $settings->getSettingsAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="settings_v1alpha1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".SettingsV1alpha1Controller") SettingsV1alpha1Controller

### Get singleton instance

The singleton instance of the ``` SettingsV1alpha1Controller ``` class can be accessed from the API Client.

```php
$settingsV1alpha1 = $client->getSettingsV1alpha1();
```

### <a name="get_settings_v1alpha1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.getSettingsV1alpha1APIResources") getSettingsV1alpha1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getSettingsV1alpha1APIResources()
```

#### Example Usage

```php

$result = $settingsV1alpha1->getSettingsV1alpha1APIResources();

```


### <a name="list_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.listSettingsV1alpha1NamespacedPodPreset") listSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodPreset


```php
function listSettingsV1alpha1NamespacedPodPreset(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 118;
$watch = false;
$pretty = 'pretty';

$result = $settingsV1alpha1->listSettingsV1alpha1NamespacedPodPreset($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.createSettingsV1alpha1NamespacedPodPreset") createSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> create a PodPreset


```php
function createSettingsV1alpha1NamespacedPodPreset(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1alpha1PodPreset();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $settingsV1alpha1->createSettingsV1alpha1NamespacedPodPreset($body, $mnamespace, $pretty);

```


### <a name="delete_settings_v1alpha1_collection_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.deleteSettingsV1alpha1CollectionNamespacedPodPreset") deleteSettingsV1alpha1CollectionNamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PodPreset


```php
function deleteSettingsV1alpha1CollectionNamespacedPodPreset(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 118;
$watch = false;
$pretty = 'pretty';

$result = $settingsV1alpha1->deleteSettingsV1alpha1CollectionNamespacedPodPreset($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.getReadSettingsV1alpha1NamespacedPodPreset") getReadSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PodPreset


```php
function getReadSettingsV1alpha1NamespacedPodPreset(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodPreset |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $settingsV1alpha1->getReadSettingsV1alpha1NamespacedPodPreset($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.updateReplaceSettingsV1alpha1NamespacedPodPreset") updateReplaceSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PodPreset


```php
function updateReplaceSettingsV1alpha1NamespacedPodPreset(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodPreset |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1alpha1PodPreset();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $settingsV1alpha1->updateReplaceSettingsV1alpha1NamespacedPodPreset($body, $name, $mnamespace, $pretty);

```


### <a name="delete_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.deleteSettingsV1alpha1NamespacedPodPreset") deleteSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> delete a PodPreset


```php
function deleteSettingsV1alpha1NamespacedPodPreset(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodPreset |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 77;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $settingsV1alpha1->deleteSettingsV1alpha1NamespacedPodPreset($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.patchSettingsV1alpha1NamespacedPodPreset") patchSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PodPreset


```php
function patchSettingsV1alpha1NamespacedPodPreset(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PodPreset |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $settingsV1alpha1->patchSettingsV1alpha1NamespacedPodPreset($body, $name, $mnamespace, $pretty);

```


### <a name="list_settings_v1alpha1_pod_preset_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.listSettingsV1alpha1PodPresetForAllNamespaces") listSettingsV1alpha1PodPresetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PodPreset


```php
function listSettingsV1alpha1PodPresetForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 77;
$watch = false;
$pretty = 'pretty';

$result = $settingsV1alpha1->listSettingsV1alpha1PodPresetForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_settings_v1alpha1_pod_preset_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.createSettingsV1alpha1PodPresetForAllNamespaces") createSettingsV1alpha1PodPresetForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodPreset


```php
function createSettingsV1alpha1PodPresetForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1alpha1PodPreset();
$pretty = 'pretty';

$result = $settingsV1alpha1->createSettingsV1alpha1PodPresetForAllNamespaces($body, $pretty);

```


### <a name="get_watch_settings_v1alpha1_namespaced_pod_preset_list"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.getWatchSettingsV1alpha1NamespacedPodPresetList") getWatchSettingsV1alpha1NamespacedPodPresetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodPreset


```php
function getWatchSettingsV1alpha1NamespacedPodPresetList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 77;
$watch = false;

$result = $settingsV1alpha1->getWatchSettingsV1alpha1NamespacedPodPresetList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_settings_v1alpha1_namespaced_pod_preset"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.getWatchSettingsV1alpha1NamespacedPodPreset") getWatchSettingsV1alpha1NamespacedPodPreset

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PodPreset


```php
function getWatchSettingsV1alpha1NamespacedPodPreset(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PodPreset |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 168;
$watch = true;

$result = $settingsV1alpha1->getWatchSettingsV1alpha1NamespacedPodPreset($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_settings_v1alpha1_pod_preset_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".SettingsV1alpha1Controller.getWatchSettingsV1alpha1PodPresetListForAllNamespaces") getWatchSettingsV1alpha1PodPresetListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PodPreset


```php
function getWatchSettingsV1alpha1PodPresetListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 168;
$watch = true;

$result = $settingsV1alpha1->getWatchSettingsV1alpha1PodPresetListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="storage_controller"></a>![Class: ](https://apidocs.io/img/class.png ".StorageController") StorageController

### Get singleton instance

The singleton instance of the ``` StorageController ``` class can be accessed from the API Client.

```php
$storage = $client->getStorage();
```

### <a name="get_storage_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".StorageController.getStorageAPIGroup") getStorageAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getStorageAPIGroup()
```

#### Example Usage

```php

$result = $storage->getStorageAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="storage_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".StorageV1Controller") StorageV1Controller

### Get singleton instance

The singleton instance of the ``` StorageV1Controller ``` class can be accessed from the API Client.

```php
$storageV1 = $client->getStorageV1();
```

### <a name="get_storage_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.getStorageV1APIResources") getStorageV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getStorageV1APIResources()
```

#### Example Usage

```php

$result = $storageV1->getStorageV1APIResources();

```


### <a name="list_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.listStorageV1StorageClass") listStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind StorageClass


```php
function listStorageV1StorageClass(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 168;
$watch = true;
$pretty = 'pretty';

$result = $storageV1->listStorageV1StorageClass($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.createStorageV1StorageClass") createStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> create a StorageClass


```php
function createStorageV1StorageClass(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1StorageClass();
$pretty = 'pretty';

$result = $storageV1->createStorageV1StorageClass($body, $pretty);

```


### <a name="delete_storage_v1_collection_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.deleteStorageV1CollectionStorageClass") deleteStorageV1CollectionStorageClass

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of StorageClass


```php
function deleteStorageV1CollectionStorageClass(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 168;
$watch = true;
$pretty = 'pretty';

$result = $storageV1->deleteStorageV1CollectionStorageClass($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.getReadStorageV1StorageClass") getReadStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> read the specified StorageClass


```php
function getReadStorageV1StorageClass(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StorageClass |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $storageV1->getReadStorageV1StorageClass($name, $exact, $export, $pretty);

```


### <a name="update_replace_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.updateReplaceStorageV1StorageClass") updateReplaceStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified StorageClass


```php
function updateReplaceStorageV1StorageClass(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StorageClass |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1StorageClass();
$name = 'name';
$pretty = 'pretty';

$result = $storageV1->updateReplaceStorageV1StorageClass($body, $name, $pretty);

```


### <a name="delete_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.deleteStorageV1StorageClass") deleteStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> delete a StorageClass


```php
function deleteStorageV1StorageClass(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StorageClass |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 126;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $storageV1->deleteStorageV1StorageClass($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.patchStorageV1StorageClass") patchStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified StorageClass


```php
function patchStorageV1StorageClass(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StorageClass |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $storageV1->patchStorageV1StorageClass($body, $name, $pretty);

```


### <a name="get_watch_storage_v1_storage_class_list"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.getWatchStorageV1StorageClassList") getWatchStorageV1StorageClassList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of StorageClass


```php
function getWatchStorageV1StorageClassList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 126;
$watch = false;

$result = $storageV1->getWatchStorageV1StorageClassList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_storage_v1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1Controller.getWatchStorageV1StorageClass") getWatchStorageV1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind StorageClass


```php
function getWatchStorageV1StorageClass(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StorageClass |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 218;
$watch = true;

$result = $storageV1->getWatchStorageV1StorageClass($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="storage_v1beta1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".StorageV1beta1Controller") StorageV1beta1Controller

### Get singleton instance

The singleton instance of the ``` StorageV1beta1Controller ``` class can be accessed from the API Client.

```php
$storageV1beta1 = $client->getStorageV1beta1();
```

### <a name="get_storage_v1beta1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.getStorageV1beta1APIResources") getStorageV1beta1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getStorageV1beta1APIResources()
```

#### Example Usage

```php

$result = $storageV1beta1->getStorageV1beta1APIResources();

```


### <a name="list_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.listStorageV1beta1StorageClass") listStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind StorageClass


```php
function listStorageV1beta1StorageClass(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 218;
$watch = true;
$pretty = 'pretty';

$result = $storageV1beta1->listStorageV1beta1StorageClass($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.createStorageV1beta1StorageClass") createStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> create a StorageClass


```php
function createStorageV1beta1StorageClass(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1StorageClass();
$pretty = 'pretty';

$result = $storageV1beta1->createStorageV1beta1StorageClass($body, $pretty);

```


### <a name="delete_storage_v1beta1_collection_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.deleteStorageV1beta1CollectionStorageClass") deleteStorageV1beta1CollectionStorageClass

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of StorageClass


```php
function deleteStorageV1beta1CollectionStorageClass(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 218;
$watch = true;
$pretty = 'pretty';

$result = $storageV1beta1->deleteStorageV1beta1CollectionStorageClass($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.getReadStorageV1beta1StorageClass") getReadStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> read the specified StorageClass


```php
function getReadStorageV1beta1StorageClass(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StorageClass |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $storageV1beta1->getReadStorageV1beta1StorageClass($name, $exact, $export, $pretty);

```


### <a name="update_replace_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.updateReplaceStorageV1beta1StorageClass") updateReplaceStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified StorageClass


```php
function updateReplaceStorageV1beta1StorageClass(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StorageClass |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1StorageClass();
$name = 'name';
$pretty = 'pretty';

$result = $storageV1beta1->updateReplaceStorageV1beta1StorageClass($body, $name, $pretty);

```


### <a name="delete_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.deleteStorageV1beta1StorageClass") deleteStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> delete a StorageClass


```php
function deleteStorageV1beta1StorageClass(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StorageClass |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 54;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $storageV1beta1->deleteStorageV1beta1StorageClass($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.patchStorageV1beta1StorageClass") patchStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified StorageClass


```php
function patchStorageV1beta1StorageClass(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the StorageClass |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $storageV1beta1->patchStorageV1beta1StorageClass($body, $name, $pretty);

```


### <a name="get_watch_storage_v1beta1_storage_class_list"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.getWatchStorageV1beta1StorageClassList") getWatchStorageV1beta1StorageClassList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of StorageClass


```php
function getWatchStorageV1beta1StorageClassList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 54;
$watch = false;

$result = $storageV1beta1->getWatchStorageV1beta1StorageClassList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_storage_v1beta1_storage_class"></a>![Method: ](https://apidocs.io/img/method.png ".StorageV1beta1Controller.getWatchStorageV1beta1StorageClass") getWatchStorageV1beta1StorageClass

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind StorageClass


```php
function getWatchStorageV1beta1StorageClass(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the StorageClass |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 54;
$watch = false;

$result = $storageV1beta1->getWatchStorageV1beta1StorageClass($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="template_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".TemplateOpenshiftIoController") TemplateOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` TemplateOpenshiftIoController ``` class can be accessed from the API Client.

```php
$templateOpenshiftIo = $client->getTemplateOpenshiftIo();
```

### <a name="get_template_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoController.getTemplateOpenshiftIoAPIGroup") getTemplateOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getTemplateOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $templateOpenshiftIo->getTemplateOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="template_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".TemplateOpenshiftIoV1Controller") TemplateOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` TemplateOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$templateOpenshiftIoV1 = $client->getTemplateOpenshiftIoV1();
```

### <a name="get_template_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.getTemplateOpenshiftIoV1APIResources") getTemplateOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getTemplateOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $templateOpenshiftIoV1->getTemplateOpenshiftIoV1APIResources();

```


### <a name="list_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.listTemplateOpenshiftIoV1NamespacedTemplate") listTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Template


```php
function listTemplateOpenshiftIoV1NamespacedTemplate(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 13;
$watch = false;
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->listTemplateOpenshiftIoV1NamespacedTemplate($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.createTemplateOpenshiftIoV1NamespacedTemplate") createTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createTemplateOpenshiftIoV1NamespacedTemplate(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->createTemplateOpenshiftIoV1NamespacedTemplate($body, $mnamespace, $pretty);

```


### <a name="delete_template_openshift_io_v1_collection_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.deleteTemplateOpenshiftIoV1CollectionNamespacedTemplate") deleteTemplateOpenshiftIoV1CollectionNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Template


```php
function deleteTemplateOpenshiftIoV1CollectionNamespacedTemplate(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 13;
$watch = false;
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->deleteTemplateOpenshiftIoV1CollectionNamespacedTemplate($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.getReadTemplateOpenshiftIoV1NamespacedTemplate") getReadTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Template


```php
function getReadTemplateOpenshiftIoV1NamespacedTemplate(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->getReadTemplateOpenshiftIoV1NamespacedTemplate($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.updateReplaceTemplateOpenshiftIoV1NamespacedTemplate") updateReplaceTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Template


```php
function updateReplaceTemplateOpenshiftIoV1NamespacedTemplate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->updateReplaceTemplateOpenshiftIoV1NamespacedTemplate($body, $name, $mnamespace, $pretty);

```


### <a name="delete_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.deleteTemplateOpenshiftIoV1NamespacedTemplate") deleteTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> delete a Template


```php
function deleteTemplateOpenshiftIoV1NamespacedTemplate(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 104;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->deleteTemplateOpenshiftIoV1NamespacedTemplate($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.patchTemplateOpenshiftIoV1NamespacedTemplate") patchTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Template


```php
function patchTemplateOpenshiftIoV1NamespacedTemplate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->patchTemplateOpenshiftIoV1NamespacedTemplate($body, $name, $mnamespace, $pretty);

```


### <a name="list_template_openshift_io_v1_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.listTemplateOpenshiftIoV1TemplateForAllNamespaces") listTemplateOpenshiftIoV1TemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Template


```php
function listTemplateOpenshiftIoV1TemplateForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 104;
$watch = false;
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->listTemplateOpenshiftIoV1TemplateForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_template_openshift_io_v1_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.createTemplateOpenshiftIoV1TemplateForAllNamespaces") createTemplateOpenshiftIoV1TemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createTemplateOpenshiftIoV1TemplateForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$pretty = 'pretty';

$result = $templateOpenshiftIoV1->createTemplateOpenshiftIoV1TemplateForAllNamespaces($body, $pretty);

```


### <a name="get_watch_template_openshift_io_v1_namespaced_template_list"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.getWatchTemplateOpenshiftIoV1NamespacedTemplateList") getWatchTemplateOpenshiftIoV1NamespacedTemplateList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Template


```php
function getWatchTemplateOpenshiftIoV1NamespacedTemplateList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $templateOpenshiftIoV1->getWatchTemplateOpenshiftIoV1NamespacedTemplateList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_template_openshift_io_v1_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.getWatchTemplateOpenshiftIoV1NamespacedTemplate") getWatchTemplateOpenshiftIoV1NamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Template


```php
function getWatchTemplateOpenshiftIoV1NamespacedTemplate(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $templateOpenshiftIoV1->getWatchTemplateOpenshiftIoV1NamespacedTemplate($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_template_openshift_io_v1_template_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".TemplateOpenshiftIoV1Controller.getWatchTemplateOpenshiftIoV1TemplateListForAllNamespaces") getWatchTemplateOpenshiftIoV1TemplateListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Template


```php
function getWatchTemplateOpenshiftIoV1TemplateListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 196;
$watch = true;

$result = $templateOpenshiftIoV1->getWatchTemplateOpenshiftIoV1TemplateListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="api_controller"></a>![Class: ](https://apidocs.io/img/class.png ".APIController") APIController

### Get singleton instance

The singleton instance of the ``` APIController ``` class can be accessed from the API Client.

```php
$client = $client->getClient();
```

### <a name="create_namespaced_processed_template_v1"></a>![Method: ](https://apidocs.io/img/method.png ".APIController.createNamespacedProcessedTemplateV1") createNamespacedProcessedTemplateV1

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createNamespacedProcessedTemplateV1(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $client->createNamespacedProcessedTemplateV1($body, $mnamespace, $pretty);

```


### <a name="create_processed_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".APIController.createProcessedTemplateForAllNamespaces") createProcessedTemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createProcessedTemplateForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$pretty = 'pretty';

$result = $client->createProcessedTemplateForAllNamespaces($body, $pretty);

```


### <a name="create_namespaced_processed_template"></a>![Method: ](https://apidocs.io/img/method.png ".APIController.createNamespacedProcessedTemplate") createNamespacedProcessedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createNamespacedProcessedTemplate(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $client->createNamespacedProcessedTemplate($body, $mnamespace, $pretty);

```


### <a name="create_processed_template_for_all_namespaces_v1"></a>![Method: ](https://apidocs.io/img/method.png ".APIController.createProcessedTemplateForAllNamespacesV1") createProcessedTemplateForAllNamespacesV1

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createProcessedTemplateForAllNamespacesV1(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$pretty = 'pretty';

$result = $client->createProcessedTemplateForAllNamespacesV1($body, $pretty);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="user_openshift_io_controller"></a>![Class: ](https://apidocs.io/img/class.png ".UserOpenshiftIoController") UserOpenshiftIoController

### Get singleton instance

The singleton instance of the ``` UserOpenshiftIoController ``` class can be accessed from the API Client.

```php
$userOpenshiftIo = $client->getUserOpenshiftIo();
```

### <a name="get_user_openshift_io_api_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoController.getUserOpenshiftIoAPIGroup") getUserOpenshiftIoAPIGroup

> *Tags:*  ``` Skips Authentication ``` 

> get information of a group


```php
function getUserOpenshiftIoAPIGroup()
```

#### Example Usage

```php

$result = $userOpenshiftIo->getUserOpenshiftIoAPIGroup();

```


[Back to List of Controllers](#list_of_controllers)

## <a name="user_openshift_io_v1_controller"></a>![Class: ](https://apidocs.io/img/class.png ".UserOpenshiftIoV1Controller") UserOpenshiftIoV1Controller

### Get singleton instance

The singleton instance of the ``` UserOpenshiftIoV1Controller ``` class can be accessed from the API Client.

```php
$userOpenshiftIoV1 = $client->getUserOpenshiftIoV1();
```

### <a name="get_user_openshift_io_v1_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getUserOpenshiftIoV1APIResources") getUserOpenshiftIoV1APIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getUserOpenshiftIoV1APIResources()
```

#### Example Usage

```php

$result = $userOpenshiftIoV1->getUserOpenshiftIoV1APIResources();

```


### <a name="list_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.listUserOpenshiftIoV1Group") listUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Group


```php
function listUserOpenshiftIoV1Group(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 154;
$watch = true;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->listUserOpenshiftIoV1Group($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.createUserOpenshiftIoV1Group") createUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> create a Group


```php
function createUserOpenshiftIoV1Group(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Group();
$pretty = 'pretty';

$result = $userOpenshiftIoV1->createUserOpenshiftIoV1Group($body, $pretty);

```


### <a name="delete_user_openshift_io_v1_collection_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1CollectionGroup") deleteUserOpenshiftIoV1CollectionGroup

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Group


```php
function deleteUserOpenshiftIoV1CollectionGroup(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 154;
$watch = true;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1CollectionGroup($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getReadUserOpenshiftIoV1Group") getReadUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Group


```php
function getReadUserOpenshiftIoV1Group(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Group |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->getReadUserOpenshiftIoV1Group($name, $exact, $export, $pretty);

```


### <a name="update_replace_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.updateReplaceUserOpenshiftIoV1Group") updateReplaceUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Group


```php
function updateReplaceUserOpenshiftIoV1Group(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Group |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Group();
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->updateReplaceUserOpenshiftIoV1Group($body, $name, $pretty);

```


### <a name="delete_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1Group") deleteUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> delete a Group


```php
function deleteUserOpenshiftIoV1Group(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Group |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 245;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1Group($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.patchUserOpenshiftIoV1Group") patchUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Group


```php
function patchUserOpenshiftIoV1Group(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Group |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->patchUserOpenshiftIoV1Group($body, $name, $pretty);

```


### <a name="list_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.listUserOpenshiftIoV1Identity") listUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Identity


```php
function listUserOpenshiftIoV1Identity(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 245;
$watch = true;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->listUserOpenshiftIoV1Identity($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.createUserOpenshiftIoV1Identity") createUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> create an Identity


```php
function createUserOpenshiftIoV1Identity(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Identity();
$pretty = 'pretty';

$result = $userOpenshiftIoV1->createUserOpenshiftIoV1Identity($body, $pretty);

```


### <a name="delete_user_openshift_io_v1_collection_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1CollectionIdentity") deleteUserOpenshiftIoV1CollectionIdentity

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Identity


```php
function deleteUserOpenshiftIoV1CollectionIdentity(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 245;
$watch = true;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1CollectionIdentity($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getReadUserOpenshiftIoV1Identity") getReadUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Identity


```php
function getReadUserOpenshiftIoV1Identity(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Identity |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->getReadUserOpenshiftIoV1Identity($name, $exact, $export, $pretty);

```


### <a name="update_replace_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.updateReplaceUserOpenshiftIoV1Identity") updateReplaceUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Identity


```php
function updateReplaceUserOpenshiftIoV1Identity(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Identity |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Identity();
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->updateReplaceUserOpenshiftIoV1Identity($body, $name, $pretty);

```


### <a name="delete_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1Identity") deleteUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> delete an Identity


```php
function deleteUserOpenshiftIoV1Identity(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Identity |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 204;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1Identity($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.patchUserOpenshiftIoV1Identity") patchUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Identity


```php
function patchUserOpenshiftIoV1Identity(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Identity |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->patchUserOpenshiftIoV1Identity($body, $name, $pretty);

```


### <a name="create_user_openshift_io_v1_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.createUserOpenshiftIoV1UserIdentityMapping") createUserOpenshiftIoV1UserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> create an UserIdentityMapping


```php
function createUserOpenshiftIoV1UserIdentityMapping(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1UserIdentityMapping();
$pretty = 'pretty';

$result = $userOpenshiftIoV1->createUserOpenshiftIoV1UserIdentityMapping($body, $pretty);

```


### <a name="get_read_user_openshift_io_v1_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getReadUserOpenshiftIoV1UserIdentityMapping") getReadUserOpenshiftIoV1UserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> read the specified UserIdentityMapping


```php
function getReadUserOpenshiftIoV1UserIdentityMapping(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->getReadUserOpenshiftIoV1UserIdentityMapping($name, $pretty);

```


### <a name="update_replace_user_openshift_io_v1_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.updateReplaceUserOpenshiftIoV1UserIdentityMapping") updateReplaceUserOpenshiftIoV1UserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified UserIdentityMapping


```php
function updateReplaceUserOpenshiftIoV1UserIdentityMapping(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1UserIdentityMapping();
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->updateReplaceUserOpenshiftIoV1UserIdentityMapping($body, $name, $pretty);

```


### <a name="delete_user_openshift_io_v1_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1UserIdentityMapping") deleteUserOpenshiftIoV1UserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> delete an UserIdentityMapping


```php
function deleteUserOpenshiftIoV1UserIdentityMapping(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1UserIdentityMapping($name, $pretty);

```


### <a name="patch_user_openshift_io_v1_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.patchUserOpenshiftIoV1UserIdentityMapping") patchUserOpenshiftIoV1UserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified UserIdentityMapping


```php
function patchUserOpenshiftIoV1UserIdentityMapping(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->patchUserOpenshiftIoV1UserIdentityMapping($body, $name, $pretty);

```


### <a name="list_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.listUserOpenshiftIoV1User") listUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind User


```php
function listUserOpenshiftIoV1User(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 40;
$watch = false;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->listUserOpenshiftIoV1User($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.createUserOpenshiftIoV1User") createUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> create an User


```php
function createUserOpenshiftIoV1User(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1User();
$pretty = 'pretty';

$result = $userOpenshiftIoV1->createUserOpenshiftIoV1User($body, $pretty);

```


### <a name="delete_user_openshift_io_v1_collection_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1CollectionUser") deleteUserOpenshiftIoV1CollectionUser

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of User


```php
function deleteUserOpenshiftIoV1CollectionUser(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 40;
$watch = false;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1CollectionUser($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getReadUserOpenshiftIoV1User") getReadUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> read the specified User


```php
function getReadUserOpenshiftIoV1User(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the User |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $userOpenshiftIoV1->getReadUserOpenshiftIoV1User($name, $exact, $export, $pretty);

```


### <a name="update_replace_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.updateReplaceUserOpenshiftIoV1User") updateReplaceUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified User


```php
function updateReplaceUserOpenshiftIoV1User(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the User |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1User();
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->updateReplaceUserOpenshiftIoV1User($body, $name, $pretty);

```


### <a name="delete_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.deleteUserOpenshiftIoV1User") deleteUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> delete an User


```php
function deleteUserOpenshiftIoV1User(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the User |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 132;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->deleteUserOpenshiftIoV1User($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.patchUserOpenshiftIoV1User") patchUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified User


```php
function patchUserOpenshiftIoV1User(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the User |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $userOpenshiftIoV1->patchUserOpenshiftIoV1User($body, $name, $pretty);

```


### <a name="get_watch_user_openshift_io_v1_group_list"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getWatchUserOpenshiftIoV1GroupList") getWatchUserOpenshiftIoV1GroupList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Group


```php
function getWatchUserOpenshiftIoV1GroupList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 132;
$watch = true;

$result = $userOpenshiftIoV1->getWatchUserOpenshiftIoV1GroupList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user_openshift_io_v1_group"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getWatchUserOpenshiftIoV1Group") getWatchUserOpenshiftIoV1Group

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Group


```php
function getWatchUserOpenshiftIoV1Group(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Group |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;

$result = $userOpenshiftIoV1->getWatchUserOpenshiftIoV1Group($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user_openshift_io_v1_identity_list"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getWatchUserOpenshiftIoV1IdentityList") getWatchUserOpenshiftIoV1IdentityList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Identity


```php
function getWatchUserOpenshiftIoV1IdentityList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;

$result = $userOpenshiftIoV1->getWatchUserOpenshiftIoV1IdentityList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user_openshift_io_v1_identity"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getWatchUserOpenshiftIoV1Identity") getWatchUserOpenshiftIoV1Identity

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Identity


```php
function getWatchUserOpenshiftIoV1Identity(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Identity |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;

$result = $userOpenshiftIoV1->getWatchUserOpenshiftIoV1Identity($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user_openshift_io_v1_user_list"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getWatchUserOpenshiftIoV1UserList") getWatchUserOpenshiftIoV1UserList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of User


```php
function getWatchUserOpenshiftIoV1UserList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;

$result = $userOpenshiftIoV1->getWatchUserOpenshiftIoV1UserList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user_openshift_io_v1_user"></a>![Method: ](https://apidocs.io/img/method.png ".UserOpenshiftIoV1Controller.getWatchUserOpenshiftIoV1User") getWatchUserOpenshiftIoV1User

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind User


```php
function getWatchUserOpenshiftIoV1User(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the User |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;

$result = $userOpenshiftIoV1->getWatchUserOpenshiftIoV1User($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="oapi_controller"></a>![Class: ](https://apidocs.io/img/class.png ".OapiController") OapiController

### Get singleton instance

The singleton instance of the ``` OapiController ``` class can be accessed from the API Client.

```php
$oapi = $client->getOapi();
```

### <a name="get_oapi_version"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getOapiVersion") getOapiVersion

> *Tags:*  ``` Skips Authentication ``` 

> list supported server API versions


```php
function getOapiVersion()
```

#### Example Usage

```php

$oapi->getOapiVersion();

```

#### Errors

| Error Code | Error Description |
|------------|-------------------|
| 0 | Default Response. |



### <a name="get_api_resources"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getAPIResources") getAPIResources

> *Tags:*  ``` Skips Authentication ``` 

> get available resources


```php
function getAPIResources()
```

#### Example Usage

```php

$result = $oapi->getAPIResources();

```


### <a name="list_applied_cluster_resource_quota_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listAppliedClusterResourceQuotaForAllNamespaces") listAppliedClusterResourceQuotaForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind AppliedClusterResourceQuota


```php
function listAppliedClusterResourceQuotaForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;

$result = $oapi->listAppliedClusterResourceQuotaForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="list_build_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listBuildConfigForAllNamespaces") listBuildConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind BuildConfig


```php
function listBuildConfigForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 18;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listBuildConfigForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_build_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createBuildConfigForAllNamespaces") createBuildConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a BuildConfig


```php
function createBuildConfigForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildConfig();
$pretty = 'pretty';

$result = $oapi->createBuildConfigForAllNamespaces($body, $pretty);

```


### <a name="list_build_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listBuildForAllNamespaces") listBuildForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Build


```php
function listBuildForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 18;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listBuildForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_build_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createBuildForAllNamespaces") createBuildForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Build


```php
function createBuildForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$pretty = 'pretty';

$result = $oapi->createBuildForAllNamespaces($body, $pretty);

```


### <a name="list_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listClusterNetwork") listClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterNetwork


```php
function listClusterNetwork(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 18;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listClusterNetwork($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createClusterNetwork") createClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterNetwork


```php
function createClusterNetwork(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterNetwork();
$pretty = 'pretty';

$result = $oapi->createClusterNetwork($body, $pretty);

```


### <a name="delete_collection_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionClusterNetwork") deleteCollectionClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterNetwork


```php
function deleteCollectionClusterNetwork(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 18;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionClusterNetwork($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterNetwork") getReadClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterNetwork


```php
function getReadClusterNetwork(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterNetwork |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadClusterNetwork($name, $exact, $export, $pretty);

```


### <a name="update_replace_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterNetwork") updateReplaceClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterNetwork


```php
function updateReplaceClusterNetwork(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterNetwork |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterNetwork();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterNetwork($body, $name, $pretty);

```


### <a name="delete_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteClusterNetwork") deleteClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterNetwork


```php
function deleteClusterNetwork(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterNetwork |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 231;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteClusterNetwork($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterNetwork") patchClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterNetwork


```php
function patchClusterNetwork(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterNetwork |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterNetwork($body, $name, $pretty);

```


### <a name="list_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listClusterPolicy") listClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterPolicy


```php
function listClusterPolicy(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 231;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listClusterPolicy($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createClusterPolicy") createClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterPolicy


```php
function createClusterPolicy(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$pretty = 'pretty';

$result = $oapi->createClusterPolicy($body, $pretty);

```


### <a name="delete_collection_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionClusterPolicy") deleteCollectionClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterPolicy


```php
function deleteCollectionClusterPolicy(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 68;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionClusterPolicy($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterPolicy") getReadClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterPolicy


```php
function getReadClusterPolicy(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicy |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadClusterPolicy($name, $exact, $export, $pretty);

```


### <a name="update_replace_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterPolicy") updateReplaceClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterPolicy


```php
function updateReplaceClusterPolicy(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicy |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterPolicy($body, $name, $pretty);

```


### <a name="delete_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteClusterPolicy") deleteClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterPolicy


```php
function deleteClusterPolicy(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicy |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 68;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteClusterPolicy($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterPolicy") patchClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterPolicy


```php
function patchClusterPolicy(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicy |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterPolicy($body, $name, $pretty);

```


### <a name="list_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listClusterPolicyBinding") listClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterPolicyBinding


```php
function listClusterPolicyBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 68;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listClusterPolicyBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createClusterPolicyBinding") createClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterPolicyBinding


```php
function createClusterPolicyBinding(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$pretty = 'pretty';

$result = $oapi->createClusterPolicyBinding($body, $pretty);

```


### <a name="delete_collection_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionClusterPolicyBinding") deleteCollectionClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterPolicyBinding


```php
function deleteCollectionClusterPolicyBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 26;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionClusterPolicyBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterPolicyBinding") getReadClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterPolicyBinding


```php
function getReadClusterPolicyBinding(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadClusterPolicyBinding($name, $exact, $export, $pretty);

```


### <a name="update_replace_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterPolicyBinding") updateReplaceClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterPolicyBinding


```php
function updateReplaceClusterPolicyBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterPolicyBinding($body, $name, $pretty);

```


### <a name="delete_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteClusterPolicyBinding") deleteClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterPolicyBinding


```php
function deleteClusterPolicyBinding(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 118;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteClusterPolicyBinding($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterPolicyBinding") patchClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterPolicyBinding


```php
function patchClusterPolicyBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterPolicyBinding($body, $name, $pretty);

```


### <a name="list_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listClusterResourceQuota") listClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ClusterResourceQuota


```php
function listClusterResourceQuota(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 118;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listClusterResourceQuota($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createClusterResourceQuota") createClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterResourceQuota


```php
function createClusterResourceQuota(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1AppliedClusterResourceQuota();
$pretty = 'pretty';

$result = $oapi->createClusterResourceQuota($body, $pretty);

```


### <a name="delete_collection_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionClusterResourceQuota") deleteCollectionClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ClusterResourceQuota


```php
function deleteCollectionClusterResourceQuota(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 118;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionClusterResourceQuota($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterResourceQuota") getReadClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterResourceQuota


```php
function getReadClusterResourceQuota(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadClusterResourceQuota($name, $exact, $export, $pretty);

```


### <a name="update_replace_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterResourceQuota") updateReplaceClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterResourceQuota


```php
function updateReplaceClusterResourceQuota(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1AppliedClusterResourceQuota();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterResourceQuota($body, $name, $pretty);

```


### <a name="delete_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteClusterResourceQuota") deleteClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterResourceQuota


```php
function deleteClusterResourceQuota(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 209;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteClusterResourceQuota($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterResourceQuota") patchClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterResourceQuota


```php
function patchClusterResourceQuota(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterResourceQuota($body, $name, $pretty);

```


### <a name="get_read_cluster_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterResourceQuotaStatus") getReadClusterResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ClusterResourceQuota


```php
function getReadClusterResourceQuotaStatus(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->getReadClusterResourceQuotaStatus($name, $pretty);

```


### <a name="update_replace_cluster_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterResourceQuotaStatus") updateReplaceClusterResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ClusterResourceQuota


```php
function updateReplaceClusterResourceQuotaStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1AppliedClusterResourceQuota();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterResourceQuotaStatus($body, $name, $pretty);

```


### <a name="patch_cluster_resource_quota_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterResourceQuotaStatus") patchClusterResourceQuotaStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ClusterResourceQuota


```php
function patchClusterResourceQuotaStatus(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterResourceQuotaStatus($body, $name, $pretty);

```


### <a name="list_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listClusterRoleBinding") listClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ClusterRoleBinding


```php
function listClusterRoleBinding(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 209;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listClusterRoleBinding($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createClusterRoleBinding") createClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterRoleBinding


```php
function createClusterRoleBinding(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$pretty = 'pretty';

$result = $oapi->createClusterRoleBinding($body, $pretty);

```


### <a name="get_read_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterRoleBinding") getReadClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterRoleBinding


```php
function getReadClusterRoleBinding(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->getReadClusterRoleBinding($name, $pretty);

```


### <a name="update_replace_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterRoleBinding") updateReplaceClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterRoleBinding


```php
function updateReplaceClusterRoleBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterRoleBinding($body, $name, $pretty);

```


### <a name="delete_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteClusterRoleBinding") deleteClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterRoleBinding


```php
function deleteClusterRoleBinding(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 167;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteClusterRoleBinding($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_cluster_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterRoleBinding") patchClusterRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterRoleBinding


```php
function patchClusterRoleBinding(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRoleBinding |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterRoleBinding($body, $name, $pretty);

```


### <a name="list_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listClusterRole") listClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ClusterRole


```php
function listClusterRole(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 167;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listClusterRole($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createClusterRole") createClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> create a ClusterRole


```php
function createClusterRole(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$pretty = 'pretty';

$result = $oapi->createClusterRole($body, $pretty);

```


### <a name="get_read_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadClusterRole") getReadClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ClusterRole


```php
function getReadClusterRole(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->getReadClusterRole($name, $pretty);

```


### <a name="update_replace_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceClusterRole") updateReplaceClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ClusterRole


```php
function updateReplaceClusterRole(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceClusterRole($body, $name, $pretty);

```


### <a name="delete_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteClusterRole") deleteClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> delete a ClusterRole


```php
function deleteClusterRole(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 4;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteClusterRole($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_cluster_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchClusterRole") patchClusterRole

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ClusterRole


```php
function patchClusterRole(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ClusterRole |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchClusterRole($body, $name, $pretty);

```


### <a name="create_deployment_config_rollback_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createDeploymentConfigRollbackForAllNamespaces") createDeploymentConfigRollbackForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a DeploymentConfigRollback


```php
function createDeploymentConfigRollbackForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfigRollback();
$pretty = 'pretty';

$result = $oapi->createDeploymentConfigRollbackForAllNamespaces($body, $pretty);

```


### <a name="list_deployment_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listDeploymentConfigForAllNamespaces") listDeploymentConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind DeploymentConfig


```php
function listDeploymentConfigForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 4;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listDeploymentConfigForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_deployment_config_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createDeploymentConfigForAllNamespaces") createDeploymentConfigForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a DeploymentConfig


```php
function createDeploymentConfigForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$pretty = 'pretty';

$result = $oapi->createDeploymentConfigForAllNamespaces($body, $pretty);

```


### <a name="list_egress_network_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listEgressNetworkPolicyForAllNamespaces") listEgressNetworkPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind EgressNetworkPolicy


```php
function listEgressNetworkPolicyForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 95;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listEgressNetworkPolicyForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_egress_network_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createEgressNetworkPolicyForAllNamespaces") createEgressNetworkPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an EgressNetworkPolicy


```php
function createEgressNetworkPolicyForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1EgressNetworkPolicy();
$pretty = 'pretty';

$result = $oapi->createEgressNetworkPolicyForAllNamespaces($body, $pretty);

```


### <a name="list_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listGroup") listGroup

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Group


```php
function listGroup(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 95;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listGroup($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createGroup") createGroup

> *Tags:*  ``` Skips Authentication ``` 

> create a Group


```php
function createGroup(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Group();
$pretty = 'pretty';

$result = $oapi->createGroup($body, $pretty);

```


### <a name="delete_collection_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionGroup") deleteCollectionGroup

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Group


```php
function deleteCollectionGroup(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 95;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionGroup($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadGroup") getReadGroup

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Group


```php
function getReadGroup(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Group |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadGroup($name, $exact, $export, $pretty);

```


### <a name="update_replace_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceGroup") updateReplaceGroup

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Group


```php
function updateReplaceGroup(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Group |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Group();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceGroup($body, $name, $pretty);

```


### <a name="delete_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteGroup") deleteGroup

> *Tags:*  ``` Skips Authentication ``` 

> delete a Group


```php
function deleteGroup(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Group |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 54;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteGroup($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchGroup") patchGroup

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Group


```php
function patchGroup(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Group |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchGroup($body, $name, $pretty);

```


### <a name="list_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listHostSubnet") listHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind HostSubnet


```php
function listHostSubnet(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 54;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listHostSubnet($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createHostSubnet") createHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> create a HostSubnet


```php
function createHostSubnet(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HostSubnet();
$pretty = 'pretty';

$result = $oapi->createHostSubnet($body, $pretty);

```


### <a name="delete_collection_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionHostSubnet") deleteCollectionHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of HostSubnet


```php
function deleteCollectionHostSubnet(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 145;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionHostSubnet($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadHostSubnet") getReadHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> read the specified HostSubnet


```php
function getReadHostSubnet(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HostSubnet |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadHostSubnet($name, $exact, $export, $pretty);

```


### <a name="update_replace_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceHostSubnet") updateReplaceHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified HostSubnet


```php
function updateReplaceHostSubnet(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HostSubnet |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1HostSubnet();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceHostSubnet($body, $name, $pretty);

```


### <a name="delete_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteHostSubnet") deleteHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> delete a HostSubnet


```php
function deleteHostSubnet(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HostSubnet |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 145;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteHostSubnet($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchHostSubnet") patchHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified HostSubnet


```php
function patchHostSubnet(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the HostSubnet |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchHostSubnet($body, $name, $pretty);

```


### <a name="list_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listIdentity") listIdentity

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Identity


```php
function listIdentity(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 145;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listIdentity($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createIdentity") createIdentity

> *Tags:*  ``` Skips Authentication ``` 

> create an Identity


```php
function createIdentity(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Identity();
$pretty = 'pretty';

$result = $oapi->createIdentity($body, $pretty);

```


### <a name="delete_collection_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionIdentity") deleteCollectionIdentity

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Identity


```php
function deleteCollectionIdentity(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 103;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionIdentity($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadIdentity") getReadIdentity

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Identity


```php
function getReadIdentity(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Identity |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadIdentity($name, $exact, $export, $pretty);

```


### <a name="update_replace_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceIdentity") updateReplaceIdentity

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Identity


```php
function updateReplaceIdentity(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Identity |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Identity();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceIdentity($body, $name, $pretty);

```


### <a name="delete_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteIdentity") deleteIdentity

> *Tags:*  ``` Skips Authentication ``` 

> delete an Identity


```php
function deleteIdentity(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Identity |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 103;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteIdentity($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchIdentity") patchIdentity

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Identity


```php
function patchIdentity(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Identity |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchIdentity($body, $name, $pretty);

```


### <a name="list_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listImage") listImage

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Image


```php
function listImage(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 103;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listImage($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createImage") createImage

> *Tags:*  ``` Skips Authentication ``` 

> create an Image


```php
function createImage(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Image();
$pretty = 'pretty';

$result = $oapi->createImage($body, $pretty);

```


### <a name="delete_collection_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionImage") deleteCollectionImage

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Image


```php
function deleteCollectionImage(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 195;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionImage($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadImage") getReadImage

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Image


```php
function getReadImage(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Image |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadImage($name, $exact, $export, $pretty);

```


### <a name="update_replace_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceImage") updateReplaceImage

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Image


```php
function updateReplaceImage(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Image |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Image();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceImage($body, $name, $pretty);

```


### <a name="delete_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteImage") deleteImage

> *Tags:*  ``` Skips Authentication ``` 

> delete an Image


```php
function deleteImage(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Image |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 195;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteImage($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchImage") patchImage

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Image


```php
function patchImage(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Image |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchImage($body, $name, $pretty);

```


### <a name="create_image_signature"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createImageSignature") createImageSignature

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageSignature


```php
function createImageSignature(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageSignature();
$pretty = 'pretty';

$result = $oapi->createImageSignature($body, $pretty);

```


### <a name="delete_image_signature"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteImageSignature") deleteImageSignature

> *Tags:*  ``` Skips Authentication ``` 

> delete an ImageSignature


```php
function deleteImageSignature(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageSignature |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->deleteImageSignature($name, $pretty);

```


### <a name="create_image_stream_import_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createImageStreamImportForAllNamespaces") createImageStreamImportForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamImport


```php
function createImageStreamImportForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamImport();
$pretty = 'pretty';

$result = $oapi->createImageStreamImportForAllNamespaces($body, $pretty);

```


### <a name="create_image_stream_mapping_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createImageStreamMappingForAllNamespaces") createImageStreamMappingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamMapping


```php
function createImageStreamMappingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamMapping();
$pretty = 'pretty';

$result = $oapi->createImageStreamMappingForAllNamespaces($body, $pretty);

```


### <a name="list_image_stream_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listImageStreamForAllNamespaces") listImageStreamForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ImageStream


```php
function listImageStreamForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 31;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listImageStreamForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_stream_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createImageStreamForAllNamespaces") createImageStreamForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStream


```php
function createImageStreamForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$pretty = 'pretty';

$result = $oapi->createImageStreamForAllNamespaces($body, $pretty);

```


### <a name="list_image_stream_tag_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listImageStreamTagForAllNamespaces") listImageStreamTagForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ImageStreamTag


```php
function listImageStreamTagForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 31;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listImageStreamTagForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_image_stream_tag_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createImageStreamTagForAllNamespaces") createImageStreamTagForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamTag


```php
function createImageStreamTagForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamTag();
$pretty = 'pretty';

$result = $oapi->createImageStreamTagForAllNamespaces($body, $pretty);

```


### <a name="create_local_resource_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createLocalResourceAccessReviewForAllNamespaces") createLocalResourceAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalResourceAccessReview


```php
function createLocalResourceAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$pretty = 'pretty';

$result = $oapi->createLocalResourceAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_local_subject_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createLocalSubjectAccessReviewForAllNamespaces") createLocalSubjectAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createLocalSubjectAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $oapi->createLocalSubjectAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="list_namespaced_applied_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedAppliedClusterResourceQuota") listNamespacedAppliedClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind AppliedClusterResourceQuota


```php
function listNamespacedAppliedClusterResourceQuota(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 245;
$watch = true;

$result = $oapi->listNamespacedAppliedClusterResourceQuota($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_read_namespaced_applied_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedAppliedClusterResourceQuota") getReadNamespacedAppliedClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> read the specified AppliedClusterResourceQuota


```php
function getReadNamespacedAppliedClusterResourceQuota(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the AppliedClusterResourceQuota |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedAppliedClusterResourceQuota($name, $mnamespace, $pretty);

```


### <a name="list_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedBuildConfig") listNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind BuildConfig


```php
function listNamespacedBuildConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 245;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedBuildConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedBuildConfig") createNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> create a BuildConfig


```php
function createNamespacedBuildConfig(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildConfig();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedBuildConfig($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedBuildConfig") deleteCollectionNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of BuildConfig


```php
function deleteCollectionNamespacedBuildConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 81;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedBuildConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedBuildConfig") getReadNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> read the specified BuildConfig


```php
function getReadNamespacedBuildConfig(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedBuildConfig($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedBuildConfig") updateReplaceNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified BuildConfig


```php
function updateReplaceNamespacedBuildConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildConfig();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedBuildConfig($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedBuildConfig") deleteNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete a BuildConfig


```php
function deleteNamespacedBuildConfig(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 81;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedBuildConfig($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedBuildConfig") patchNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified BuildConfig


```php
function patchNamespacedBuildConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedBuildConfig($body, $name, $mnamespace, $pretty);

```


### <a name="create_namespaced_build_request_instantiate"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedBuildRequestInstantiate") createNamespacedBuildRequestInstantiate

> *Tags:*  ``` Skips Authentication ``` 

> create instantiate of a BuildRequest


```php
function createNamespacedBuildRequestInstantiate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildRequest |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildRequest();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedBuildRequestInstantiate($body, $name, $mnamespace, $pretty);

```


### <a name="create_connect_post_namespaced_binary_build_request_options_instantiatebinary"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createConnectPostNamespacedBinaryBuildRequestOptionsInstantiatebinary") createConnectPostNamespacedBinaryBuildRequestOptionsInstantiatebinary

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to instantiatebinary of BinaryBuildRequestOptions


```php
function createConnectPostNamespacedBinaryBuildRequestOptionsInstantiatebinary(
        $name,
        $mnamespace,
        $asFile = null,
        $revisionAuthorEmail = null,
        $revisionAuthorName = null,
        $revisionCommit = null,
        $revisionCommitterEmail = null,
        $revisionCommitterName = null,
        $revisionMessage = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BinaryBuildRequestOptions |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| asFile |  ``` Optional ```  | asFile determines if the binary should be created as a file within the source rather than extracted as an archive |
| revisionAuthorEmail |  ``` Optional ```  | revision.authorEmail of the source control user |
| revisionAuthorName |  ``` Optional ```  | revision.authorName of the source control user |
| revisionCommit |  ``` Optional ```  | revision.commit is the value identifying a specific commit |
| revisionCommitterEmail |  ``` Optional ```  | revision.committerEmail of the source control user |
| revisionCommitterName |  ``` Optional ```  | revision.committerName of the source control user |
| revisionMessage |  ``` Optional ```  | revision.message is the description of a specific commit |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$asFile = 'asFile';
$revisionAuthorEmail = 'revision.authorEmail';
$revisionAuthorName = 'revision.authorName';
$revisionCommit = 'revision.commit';
$revisionCommitterEmail = 'revision.committerEmail';
$revisionCommitterName = 'revision.committerName';
$revisionMessage = 'revision.message';

$result = $oapi->createConnectPostNamespacedBinaryBuildRequestOptionsInstantiatebinary($name, $mnamespace, $asFile, $revisionAuthorEmail, $revisionAuthorName, $revisionCommit, $revisionCommitterEmail, $revisionCommitterName, $revisionMessage);

```


### <a name="create_connect_post_namespaced_build_webhooks"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createConnectPostNamespacedBuildWebhooks") createConnectPostNamespacedBuildWebhooks

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to webhooks of Build


```php
function createConnectPostNamespacedBuildWebhooks(
        $name,
        $mnamespace,
        $path = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Optional ```  | Path is the URL path to use for the current proxy request to pod. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $oapi->createConnectPostNamespacedBuildWebhooks($name, $mnamespace, $path);

```


### <a name="create_connect_post_namespaced_build_webhooks_with_path"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createConnectPostNamespacedBuildWebhooksWithPath") createConnectPostNamespacedBuildWebhooksWithPath

> *Tags:*  ``` Skips Authentication ``` 

> connect POST requests to webhooks of Build


```php
function createConnectPostNamespacedBuildWebhooksWithPath(
        $name,
        $mnamespace,
        $path)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| path |  ``` Required ```  | path to the resource |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$path = 'path';

$result = $oapi->createConnectPostNamespacedBuildWebhooksWithPath($name, $mnamespace, $path);

```


### <a name="list_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedBuild") listNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Build


```php
function listNamespacedBuild(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 173;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedBuild($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedBuild") createNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> create a Build


```php
function createNamespacedBuild(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedBuild($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedBuild") deleteCollectionNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Build


```php
function deleteCollectionNamespacedBuild(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 131;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedBuild($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedBuild") getReadNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Build


```php
function getReadNamespacedBuild(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedBuild($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedBuild") updateReplaceNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Build


```php
function updateReplaceNamespacedBuild(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedBuild($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedBuild") deleteNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> delete a Build


```php
function deleteNamespacedBuild(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 131;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedBuild($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedBuild") patchNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Build


```php
function patchNamespacedBuild(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedBuild($body, $name, $mnamespace, $pretty);

```


### <a name="create_namespaced_build_request_clone"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedBuildRequestClone") createNamespacedBuildRequestClone

> *Tags:*  ``` Skips Authentication ``` 

> create clone of a BuildRequest


```php
function createNamespacedBuildRequestClone(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the BuildRequest |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1BuildRequest();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedBuildRequestClone($body, $name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_build_details"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedBuildDetails") updateReplaceNamespacedBuildDetails

> *Tags:*  ``` Skips Authentication ``` 

> replace details of the specified Build


```php
function updateReplaceNamespacedBuildDetails(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Build();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedBuildDetails($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_build_log_log"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedBuildLogLog") getReadNamespacedBuildLogLog

> *Tags:*  ``` Skips Authentication ``` 

> read log of the specified BuildLog


```php
function getReadNamespacedBuildLogLog(
        $name,
        $mnamespace,
        $container = null,
        $follow = null,
        $limitBytes = null,
        $nowait = null,
        $pretty = null,
        $previous = null,
        $sinceSeconds = null,
        $tailLines = null,
        $timestamps = null,
        $version = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BuildLog |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | cointainer for which to stream logs. Defaults to only container if there is one container in the pod. |
| follow |  ``` Optional ```  | follow if true indicates that the build log should be streamed until the build terminates. |
| limitBytes |  ``` Optional ```  | limitBytes, If set, is the number of bytes to read from the server before terminating the log output. This may not display a complete final line of logging, and may return slightly more or slightly less than the specified limit. |
| nowait |  ``` Optional ```  | noWait if true causes the call to return immediately even if the build is not available yet. Otherwise the server will wait until the build has started. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| previous |  ``` Optional ```  | previous returns previous build logs. Defaults to false. |
| sinceSeconds |  ``` Optional ```  | sinceSeconds is a relative time in seconds before the current time from which to show logs. If this value precedes the time a pod was started, only logs since the pod start will be returned. If this value is in the future, no logs will be returned. Only one of sinceSeconds or sinceTime may be specified. |
| tailLines |  ``` Optional ```  | tailLines, If set, is the number of lines from the end of the logs to show. If not specified, logs are shown from the creation of the container or sinceSeconds or sinceTime |
| timestamps |  ``` Optional ```  | timestamps, If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line of log output. Defaults to false. |
| version |  ``` Optional ```  | version of the build for which to view logs. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$follow = true;
$limitBytes = 222;
$nowait = true;
$pretty = 'pretty';
$previous = true;
$sinceSeconds = 222;
$tailLines = 222;
$timestamps = true;
$version = 222;

$result = $oapi->getReadNamespacedBuildLogLog($name, $mnamespace, $container, $follow, $limitBytes, $nowait, $pretty, $previous, $sinceSeconds, $tailLines, $timestamps, $version);

```


### <a name="create_namespaced_deployment_config_rollback"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedDeploymentConfigRollback") createNamespacedDeploymentConfigRollback

> *Tags:*  ``` Skips Authentication ``` 

> create a DeploymentConfigRollback


```php
function createNamespacedDeploymentConfigRollback(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfigRollback();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedDeploymentConfigRollback($body, $mnamespace, $pretty);

```


### <a name="list_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedDeploymentConfig") listNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind DeploymentConfig


```php
function listNamespacedDeploymentConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 222;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedDeploymentConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedDeploymentConfig") createNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> create a DeploymentConfig


```php
function createNamespacedDeploymentConfig(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedDeploymentConfig($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedDeploymentConfig") deleteCollectionNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of DeploymentConfig


```php
function deleteCollectionNamespacedDeploymentConfig(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 181;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedDeploymentConfig($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedDeploymentConfig") getReadNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> read the specified DeploymentConfig


```php
function getReadNamespacedDeploymentConfig(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedDeploymentConfig($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedDeploymentConfig") updateReplaceNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified DeploymentConfig


```php
function updateReplaceNamespacedDeploymentConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedDeploymentConfig($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedDeploymentConfig") deleteNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> delete a DeploymentConfig


```php
function deleteNamespacedDeploymentConfig(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 181;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedDeploymentConfig($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedDeploymentConfig") patchNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified DeploymentConfig


```php
function patchNamespacedDeploymentConfig(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedDeploymentConfig($body, $name, $mnamespace, $pretty);

```


### <a name="create_namespaced_deployment_request_instantiate"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedDeploymentRequestInstantiate") createNamespacedDeploymentRequestInstantiate

> *Tags:*  ``` Skips Authentication ``` 

> create instantiate of a DeploymentRequest


```php
function createNamespacedDeploymentRequestInstantiate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentRequest |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentRequest();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedDeploymentRequestInstantiate($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_deployment_log_log"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedDeploymentLogLog") getReadNamespacedDeploymentLogLog

> *Tags:*  ``` Skips Authentication ``` 

> read log of the specified DeploymentLog


```php
function getReadNamespacedDeploymentLogLog(
        $name,
        $mnamespace,
        $container = null,
        $follow = null,
        $limitBytes = null,
        $nowait = null,
        $pretty = null,
        $previous = null,
        $sinceSeconds = null,
        $tailLines = null,
        $timestamps = null,
        $version = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentLog |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| container |  ``` Optional ```  | The container for which to stream logs. Defaults to only container if there is one container in the pod. |
| follow |  ``` Optional ```  | Follow if true indicates that the build log should be streamed until the build terminates. |
| limitBytes |  ``` Optional ```  | If set, the number of bytes to read from the server before terminating the log output. This may not display a complete final line of logging, and may return slightly more or slightly less than the specified limit. |
| nowait |  ``` Optional ```  | NoWait if true causes the call to return immediately even if the deployment is not available yet. Otherwise the server will wait until the deployment has started. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| previous |  ``` Optional ```  | Return previous deployment logs. Defaults to false. |
| sinceSeconds |  ``` Optional ```  | A relative time in seconds before the current time from which to show logs. If this value precedes the time a pod was started, only logs since the pod start will be returned. If this value is in the future, no logs will be returned. Only one of sinceSeconds or sinceTime may be specified. |
| tailLines |  ``` Optional ```  | If set, the number of lines from the end of the logs to show. If not specified, logs are shown from the creation of the container or sinceSeconds or sinceTime |
| timestamps |  ``` Optional ```  | If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line of log output. Defaults to false. |
| version |  ``` Optional ```  | Version of the deployment for which to view logs. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$container = 'container';
$follow = false;
$limitBytes = 17;
$nowait = false;
$pretty = 'pretty';
$previous = false;
$sinceSeconds = 17;
$tailLines = 17;
$timestamps = false;
$version = 17;

$result = $oapi->getReadNamespacedDeploymentLogLog($name, $mnamespace, $container, $follow, $limitBytes, $nowait, $pretty, $previous, $sinceSeconds, $tailLines, $timestamps, $version);

```


### <a name="create_namespaced_deployment_config_rollback_rollback"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedDeploymentConfigRollbackRollback") createNamespacedDeploymentConfigRollbackRollback

> *Tags:*  ``` Skips Authentication ``` 

> create rollback of a DeploymentConfigRollback


```php
function createNamespacedDeploymentConfigRollbackRollback(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfigRollback |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfigRollback();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedDeploymentConfigRollbackRollback($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedScaleScale") getReadNamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> read scale of the specified Scale


```php
function getReadNamespacedScaleScale(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedScaleScale($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedScaleScale") updateReplaceNamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> replace scale of the specified Scale


```php
function updateReplaceNamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1beta1Scale();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="patch_namespaced_scale_scale"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedScaleScale") patchNamespacedScaleScale

> *Tags:*  ``` Skips Authentication ``` 

> partially update scale of the specified Scale


```php
function patchNamespacedScaleScale(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Scale |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedScaleScale($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_deployment_config_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedDeploymentConfigStatus") getReadNamespacedDeploymentConfigStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified DeploymentConfig


```php
function getReadNamespacedDeploymentConfigStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedDeploymentConfigStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_deployment_config_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedDeploymentConfigStatus") updateReplaceNamespacedDeploymentConfigStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified DeploymentConfig


```php
function updateReplaceNamespacedDeploymentConfigStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeploymentConfig();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedDeploymentConfigStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_namespaced_deployment_config_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedDeploymentConfigStatus") patchNamespacedDeploymentConfigStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified DeploymentConfig


```php
function patchNamespacedDeploymentConfigStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedDeploymentConfigStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedEgressNetworkPolicy") listNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind EgressNetworkPolicy


```php
function listNamespacedEgressNetworkPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 109;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNamespacedEgressNetworkPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedEgressNetworkPolicy") createNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create an EgressNetworkPolicy


```php
function createNamespacedEgressNetworkPolicy(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1EgressNetworkPolicy();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedEgressNetworkPolicy($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedEgressNetworkPolicy") deleteCollectionNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of EgressNetworkPolicy


```php
function deleteCollectionNamespacedEgressNetworkPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 67;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedEgressNetworkPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedEgressNetworkPolicy") getReadNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified EgressNetworkPolicy


```php
function getReadNamespacedEgressNetworkPolicy(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedEgressNetworkPolicy($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedEgressNetworkPolicy") updateReplaceNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified EgressNetworkPolicy


```php
function updateReplaceNamespacedEgressNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1EgressNetworkPolicy();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedEgressNetworkPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedEgressNetworkPolicy") deleteNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete an EgressNetworkPolicy


```php
function deleteNamespacedEgressNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 158;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedEgressNetworkPolicy($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedEgressNetworkPolicy") patchNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified EgressNetworkPolicy


```php
function patchNamespacedEgressNetworkPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedEgressNetworkPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_image_stream_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedImageStreamImage") getReadNamespacedImageStreamImage

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ImageStreamImage


```php
function getReadNamespacedImageStreamImage(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStreamImage |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedImageStreamImage($name, $mnamespace, $pretty);

```


### <a name="create_namespaced_image_stream_import"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedImageStreamImport") createNamespacedImageStreamImport

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamImport


```php
function createNamespacedImageStreamImport(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamImport();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedImageStreamImport($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_image_stream_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedImageStreamMapping") createNamespacedImageStreamMapping

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamMapping


```php
function createNamespacedImageStreamMapping(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamMapping();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedImageStreamMapping($body, $mnamespace, $pretty);

```


### <a name="list_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedImageStream") listNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind ImageStream


```php
function listNamespacedImageStream(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 250;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedImageStream($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedImageStream") createNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStream


```php
function createNamespacedImageStream(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedImageStream($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedImageStream") deleteCollectionNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of ImageStream


```php
function deleteCollectionNamespacedImageStream(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 250;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedImageStream($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedImageStream") getReadNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ImageStream


```php
function getReadNamespacedImageStream(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedImageStream($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedImageStream") updateReplaceNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ImageStream


```php
function updateReplaceNamespacedImageStream(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedImageStream($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedImageStream") deleteNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> delete an ImageStream


```php
function deleteNamespacedImageStream(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 208;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedImageStream($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedImageStream") patchNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ImageStream


```php
function patchNamespacedImageStream(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedImageStream($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_secret_list_secrets"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedSecretListSecrets") getReadNamespacedSecretListSecrets

> *Tags:*  ``` Skips Authentication ``` 

> read secrets of the specified SecretList


```php
function getReadNamespacedSecretListSecrets(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the SecretList |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 208;
$watch = true;

$result = $oapi->getReadNamespacedSecretListSecrets($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_read_namespaced_image_stream_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedImageStreamStatus") getReadNamespacedImageStreamStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified ImageStream


```php
function getReadNamespacedImageStreamStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedImageStreamStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_image_stream_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedImageStreamStatus") updateReplaceNamespacedImageStreamStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified ImageStream


```php
function updateReplaceNamespacedImageStreamStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStream();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedImageStreamStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_namespaced_image_stream_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedImageStreamStatus") patchNamespacedImageStreamStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified ImageStream


```php
function patchNamespacedImageStreamStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedImageStreamStatus($body, $name, $mnamespace, $pretty);

```


### <a name="list_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedImageStreamTag") listNamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ImageStreamTag


```php
function listNamespacedImageStreamTag(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 45;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNamespacedImageStreamTag($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedImageStreamTag") createNamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> create an ImageStreamTag


```php
function createNamespacedImageStreamTag(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamTag();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedImageStreamTag($body, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedImageStreamTag") getReadNamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> read the specified ImageStreamTag


```php
function getReadNamespacedImageStreamTag(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedImageStreamTag($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedImageStreamTag") updateReplaceNamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified ImageStreamTag


```php
function updateReplaceNamespacedImageStreamTag(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ImageStreamTag();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedImageStreamTag($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedImageStreamTag") deleteNamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> delete an ImageStreamTag


```php
function deleteNamespacedImageStreamTag(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedImageStreamTag($name, $mnamespace, $pretty);

```


### <a name="patch_namespaced_image_stream_tag"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedImageStreamTag") patchNamespacedImageStreamTag

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified ImageStreamTag


```php
function patchNamespacedImageStreamTag(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the ImageStreamTag |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedImageStreamTag($body, $name, $mnamespace, $pretty);

```


### <a name="create_namespaced_local_resource_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedLocalResourceAccessReview") createNamespacedLocalResourceAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalResourceAccessReview


```php
function createNamespacedLocalResourceAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedLocalResourceAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_local_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedLocalSubjectAccessReview") createNamespacedLocalSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a LocalSubjectAccessReview


```php
function createNamespacedLocalSubjectAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedLocalSubjectAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_pod_security_policy_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedPodSecurityPolicyReview") createNamespacedPodSecurityPolicyReview

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicyReview


```php
function createNamespacedPodSecurityPolicyReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicyReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedPodSecurityPolicyReview($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_pod_security_policy_self_subject_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedPodSecurityPolicySelfSubjectReview") createNamespacedPodSecurityPolicySelfSubjectReview

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySelfSubjectReview


```php
function createNamespacedPodSecurityPolicySelfSubjectReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySelfSubjectReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedPodSecurityPolicySelfSubjectReview($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_pod_security_policy_subject_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedPodSecurityPolicySubjectReview") createNamespacedPodSecurityPolicySubjectReview

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySubjectReview


```php
function createNamespacedPodSecurityPolicySubjectReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySubjectReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedPodSecurityPolicySubjectReview($body, $mnamespace, $pretty);

```


### <a name="list_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedPolicy") listNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Policy


```php
function listNamespacedPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 3;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNamespacedPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedPolicy") createNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> create a Policy


```php
function createNamespacedPolicy(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedPolicy($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedPolicy") deleteCollectionNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Policy


```php
function deleteCollectionNamespacedPolicy(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 94;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedPolicy($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedPolicy") getReadNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Policy


```php
function getReadNamespacedPolicy(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedPolicy($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedPolicy") updateReplaceNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Policy


```php
function updateReplaceNamespacedPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedPolicy") deleteNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> delete a Policy


```php
function deleteNamespacedPolicy(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 94;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedPolicy($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedPolicy") patchNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Policy


```php
function patchNamespacedPolicy(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedPolicy($body, $name, $mnamespace, $pretty);

```


### <a name="list_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedPolicyBinding") listNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PolicyBinding


```php
function listNamespacedPolicyBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 186;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedPolicyBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedPolicyBinding") createNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a PolicyBinding


```php
function createNamespacedPolicyBinding(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedPolicyBinding($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedPolicyBinding") deleteCollectionNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of PolicyBinding


```php
function deleteCollectionNamespacedPolicyBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 186;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedPolicyBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedPolicyBinding") getReadNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified PolicyBinding


```php
function getReadNamespacedPolicyBinding(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedPolicyBinding($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedPolicyBinding") updateReplaceNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified PolicyBinding


```php
function updateReplaceNamespacedPolicyBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedPolicyBinding($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedPolicyBinding") deleteNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a PolicyBinding


```php
function deleteNamespacedPolicyBinding(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 144;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedPolicyBinding($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedPolicyBinding") patchNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified PolicyBinding


```php
function patchNamespacedPolicyBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedPolicyBinding($body, $name, $mnamespace, $pretty);

```


### <a name="create_namespaced_resource_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedResourceAccessReview") createNamespacedResourceAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a ResourceAccessReview


```php
function createNamespacedResourceAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedResourceAccessReview($body, $mnamespace, $pretty);

```


### <a name="list_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedRoleBindingRestriction") listNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind RoleBindingRestriction


```php
function listNamespacedRoleBindingRestriction(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 144;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedRoleBindingRestriction($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedRoleBindingRestriction") createNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBindingRestriction


```php
function createNamespacedRoleBindingRestriction(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1RoleBindingRestriction();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedRoleBindingRestriction($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedRoleBindingRestriction") deleteCollectionNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of RoleBindingRestriction


```php
function deleteCollectionNamespacedRoleBindingRestriction(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 236;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedRoleBindingRestriction($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedRoleBindingRestriction") getReadNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> read the specified RoleBindingRestriction


```php
function getReadNamespacedRoleBindingRestriction(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedRoleBindingRestriction($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedRoleBindingRestriction") updateReplaceNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified RoleBindingRestriction


```php
function updateReplaceNamespacedRoleBindingRestriction(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1RoleBindingRestriction();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedRoleBindingRestriction($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedRoleBindingRestriction") deleteNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> delete a RoleBindingRestriction


```php
function deleteNamespacedRoleBindingRestriction(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 236;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedRoleBindingRestriction($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedRoleBindingRestriction") patchNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified RoleBindingRestriction


```php
function patchNamespacedRoleBindingRestriction(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedRoleBindingRestriction($body, $name, $mnamespace, $pretty);

```


### <a name="list_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedRoleBinding") listNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind RoleBinding


```php
function listNamespacedRoleBinding(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 72;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNamespacedRoleBinding($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedRoleBinding") createNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBinding


```php
function createNamespacedRoleBinding(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedRoleBinding($body, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedRoleBinding") getReadNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> read the specified RoleBinding


```php
function getReadNamespacedRoleBinding(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedRoleBinding($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedRoleBinding") updateReplaceNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified RoleBinding


```php
function updateReplaceNamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedRoleBinding($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedRoleBinding") deleteNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> delete a RoleBinding


```php
function deleteNamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 72;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedRoleBinding($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_role_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedRoleBinding") patchNamespacedRoleBinding

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified RoleBinding


```php
function patchNamespacedRoleBinding(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the RoleBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedRoleBinding($body, $name, $mnamespace, $pretty);

```


### <a name="list_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedRole") listNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind Role


```php
function listNamespacedRole(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 30;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNamespacedRole($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedRole") createNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> create a Role


```php
function createNamespacedRole(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedRole($body, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedRole") getReadNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Role


```php
function getReadNamespacedRole(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedRole($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedRole") updateReplaceNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Role


```php
function updateReplaceNamespacedRole(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedRole($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedRole") deleteNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> delete a Role


```php
function deleteNamespacedRole(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 30;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedRole($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_role"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedRole") patchNamespacedRole

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Role


```php
function patchNamespacedRole(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Role |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedRole($body, $name, $mnamespace, $pretty);

```


### <a name="list_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedRoute") listNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Route


```php
function listNamespacedRoute(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 122;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNamespacedRoute($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedRoute") createNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> create a Route


```php
function createNamespacedRoute(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedRoute($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedRoute") deleteCollectionNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Route


```php
function deleteCollectionNamespacedRoute(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 122;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedRoute($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedRoute") getReadNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Route


```php
function getReadNamespacedRoute(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedRoute($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedRoute") updateReplaceNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Route


```php
function updateReplaceNamespacedRoute(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedRoute($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedRoute") deleteNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> delete a Route


```php
function deleteNamespacedRoute(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 122;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedRoute($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedRoute") patchNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Route


```php
function patchNamespacedRoute(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedRoute($body, $name, $mnamespace, $pretty);

```


### <a name="get_read_namespaced_route_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedRouteStatus") getReadNamespacedRouteStatus

> *Tags:*  ``` Skips Authentication ``` 

> read status of the specified Route


```php
function getReadNamespacedRouteStatus(
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->getReadNamespacedRouteStatus($name, $mnamespace, $pretty);

```


### <a name="update_replace_namespaced_route_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedRouteStatus") updateReplaceNamespacedRouteStatus

> *Tags:*  ``` Skips Authentication ``` 

> replace status of the specified Route


```php
function updateReplaceNamespacedRouteStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedRouteStatus($body, $name, $mnamespace, $pretty);

```


### <a name="patch_namespaced_route_status"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedRouteStatus") patchNamespacedRouteStatus

> *Tags:*  ``` Skips Authentication ``` 

> partially update status of the specified Route


```php
function patchNamespacedRouteStatus(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedRouteStatus($body, $name, $mnamespace, $pretty);

```


### <a name="create_namespaced_self_subject_rules_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedSelfSubjectRulesReview") createNamespacedSelfSubjectRulesReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SelfSubjectRulesReview


```php
function createNamespacedSelfSubjectRulesReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SelfSubjectRulesReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedSelfSubjectRulesReview($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_subject_access_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedSubjectAccessReview") createNamespacedSubjectAccessReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectAccessReview


```php
function createNamespacedSubjectAccessReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedSubjectAccessReview($body, $mnamespace, $pretty);

```


### <a name="create_namespaced_subject_rules_review"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedSubjectRulesReview") createNamespacedSubjectRulesReview

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectRulesReview


```php
function createNamespacedSubjectRulesReview(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SubjectRulesReview();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedSubjectRulesReview($body, $mnamespace, $pretty);

```


### <a name="list_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNamespacedTemplate") listNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Template


```php
function listNamespacedTemplate(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 172;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listNamespacedTemplate($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNamespacedTemplate") createNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createNamespacedTemplate(
        $body,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->createNamespacedTemplate($body, $mnamespace, $pretty);

```


### <a name="delete_collection_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNamespacedTemplate") deleteCollectionNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of Template


```php
function deleteCollectionNamespacedTemplate(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 172;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNamespacedTemplate($mnamespace, $fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNamespacedTemplate") getReadNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Template


```php
function getReadNamespacedTemplate(
        $name,
        $mnamespace,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadNamespacedTemplate($name, $mnamespace, $exact, $export, $pretty);

```


### <a name="update_replace_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNamespacedTemplate") updateReplaceNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Template


```php
function updateReplaceNamespacedTemplate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->updateReplaceNamespacedTemplate($body, $name, $mnamespace, $pretty);

```


### <a name="delete_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNamespacedTemplate") deleteNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> delete a Template


```php
function deleteNamespacedTemplate(
        $body,
        $name,
        $mnamespace,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$mnamespace = 'namespace';
$gracePeriodSeconds = 8;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNamespacedTemplate($body, $name, $mnamespace, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNamespacedTemplate") patchNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Template


```php
function patchNamespacedTemplate(
        $body,
        $name,
        $mnamespace,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$mnamespace = 'namespace';
$pretty = 'pretty';

$result = $oapi->patchNamespacedTemplate($body, $name, $mnamespace, $pretty);

```


### <a name="list_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listNetNamespace") listNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind NetNamespace


```php
function listNetNamespace(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 8;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listNetNamespace($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createNetNamespace") createNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> create a NetNamespace


```php
function createNetNamespace(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1NetNamespace();
$pretty = 'pretty';

$result = $oapi->createNetNamespace($body, $pretty);

```


### <a name="delete_collection_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionNetNamespace") deleteCollectionNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of NetNamespace


```php
function deleteCollectionNetNamespace(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 222;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionNetNamespace($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadNetNamespace") getReadNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> read the specified NetNamespace


```php
function getReadNetNamespace(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the NetNamespace |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadNetNamespace($name, $exact, $export, $pretty);

```


### <a name="update_replace_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceNetNamespace") updateReplaceNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified NetNamespace


```php
function updateReplaceNetNamespace(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetNamespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1NetNamespace();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceNetNamespace($body, $name, $pretty);

```


### <a name="delete_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteNetNamespace") deleteNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> delete a NetNamespace


```php
function deleteNetNamespace(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetNamespace |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 222;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteNetNamespace($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchNetNamespace") patchNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified NetNamespace


```php
function patchNetNamespace(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the NetNamespace |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchNetNamespace($body, $name, $pretty);

```


### <a name="list_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listOAuthAccessToken") listOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthAccessToken


```php
function listOAuthAccessToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 222;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listOAuthAccessToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createOAuthAccessToken") createOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthAccessToken


```php
function createOAuthAccessToken(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAccessToken();
$pretty = 'pretty';

$result = $oapi->createOAuthAccessToken($body, $pretty);

```


### <a name="delete_collection_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionOAuthAccessToken") deleteCollectionOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthAccessToken


```php
function deleteCollectionOAuthAccessToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 58;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionOAuthAccessToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadOAuthAccessToken") getReadOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthAccessToken


```php
function getReadOAuthAccessToken(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAccessToken |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadOAuthAccessToken($name, $exact, $export, $pretty);

```


### <a name="update_replace_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceOAuthAccessToken") updateReplaceOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthAccessToken


```php
function updateReplaceOAuthAccessToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAccessToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAccessToken();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceOAuthAccessToken($body, $name, $pretty);

```


### <a name="delete_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteOAuthAccessToken") deleteOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthAccessToken


```php
function deleteOAuthAccessToken(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAccessToken |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 58;
$orphanDependents = false;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteOAuthAccessToken($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchOAuthAccessToken") patchOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthAccessToken


```php
function patchOAuthAccessToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAccessToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchOAuthAccessToken($body, $name, $pretty);

```


### <a name="list_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listOAuthAuthorizeToken") listOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthAuthorizeToken


```php
function listOAuthAuthorizeToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 150;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listOAuthAuthorizeToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createOAuthAuthorizeToken") createOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthAuthorizeToken


```php
function createOAuthAuthorizeToken(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAuthorizeToken();
$pretty = 'pretty';

$result = $oapi->createOAuthAuthorizeToken($body, $pretty);

```


### <a name="delete_collection_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionOAuthAuthorizeToken") deleteCollectionOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthAuthorizeToken


```php
function deleteCollectionOAuthAuthorizeToken(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 150;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionOAuthAuthorizeToken($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadOAuthAuthorizeToken") getReadOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthAuthorizeToken


```php
function getReadOAuthAuthorizeToken(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadOAuthAuthorizeToken($name, $exact, $export, $pretty);

```


### <a name="update_replace_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceOAuthAuthorizeToken") updateReplaceOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthAuthorizeToken


```php
function updateReplaceOAuthAuthorizeToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthAuthorizeToken();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceOAuthAuthorizeToken($body, $name, $pretty);

```


### <a name="delete_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteOAuthAuthorizeToken") deleteOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthAuthorizeToken


```php
function deleteOAuthAuthorizeToken(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 150;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteOAuthAuthorizeToken($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchOAuthAuthorizeToken") patchOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthAuthorizeToken


```php
function patchOAuthAuthorizeToken(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchOAuthAuthorizeToken($body, $name, $pretty);

```


### <a name="list_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listOAuthClientAuthorization") listOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthClientAuthorization


```php
function listOAuthClientAuthorization(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 108;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listOAuthClientAuthorization($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createOAuthClientAuthorization") createOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthClientAuthorization


```php
function createOAuthClientAuthorization(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClientAuthorization();
$pretty = 'pretty';

$result = $oapi->createOAuthClientAuthorization($body, $pretty);

```


### <a name="delete_collection_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionOAuthClientAuthorization") deleteCollectionOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthClientAuthorization


```php
function deleteCollectionOAuthClientAuthorization(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 108;
$watch = false;
$pretty = 'pretty';

$result = $oapi->deleteCollectionOAuthClientAuthorization($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadOAuthClientAuthorization") getReadOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthClientAuthorization


```php
function getReadOAuthClientAuthorization(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = false;
$export = false;
$pretty = 'pretty';

$result = $oapi->getReadOAuthClientAuthorization($name, $exact, $export, $pretty);

```


### <a name="update_replace_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceOAuthClientAuthorization") updateReplaceOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthClientAuthorization


```php
function updateReplaceOAuthClientAuthorization(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClientAuthorization();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceOAuthClientAuthorization($body, $name, $pretty);

```


### <a name="delete_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteOAuthClientAuthorization") deleteOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthClientAuthorization


```php
function deleteOAuthClientAuthorization(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 199;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteOAuthClientAuthorization($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchOAuthClientAuthorization") patchOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthClientAuthorization


```php
function patchOAuthClientAuthorization(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchOAuthClientAuthorization($body, $name, $pretty);

```


### <a name="list_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listOAuthClient") listOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind OAuthClient


```php
function listOAuthClient(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 199;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listOAuthClient($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createOAuthClient") createOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> create an OAuthClient


```php
function createOAuthClient(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClient();
$pretty = 'pretty';

$result = $oapi->createOAuthClient($body, $pretty);

```


### <a name="delete_collection_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionOAuthClient") deleteCollectionOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of OAuthClient


```php
function deleteCollectionOAuthClient(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 199;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionOAuthClient($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadOAuthClient") getReadOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> read the specified OAuthClient


```php
function getReadOAuthClient(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClient |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadOAuthClient($name, $exact, $export, $pretty);

```


### <a name="update_replace_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceOAuthClient") updateReplaceOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified OAuthClient


```php
function updateReplaceOAuthClient(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClient |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1OAuthClient();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceOAuthClient($body, $name, $pretty);

```


### <a name="delete_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteOAuthClient") deleteOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> delete an OAuthClient


```php
function deleteOAuthClient(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClient |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 158;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteOAuthClient($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchOAuthClient") patchOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified OAuthClient


```php
function patchOAuthClient(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the OAuthClient |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchOAuthClient($body, $name, $pretty);

```


### <a name="create_pod_security_policy_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createPodSecurityPolicyReviewForAllNamespaces") createPodSecurityPolicyReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicyReview


```php
function createPodSecurityPolicyReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicyReview();
$pretty = 'pretty';

$result = $oapi->createPodSecurityPolicyReviewForAllNamespaces($body, $pretty);

```


### <a name="create_pod_security_policy_self_subject_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createPodSecurityPolicySelfSubjectReviewForAllNamespaces") createPodSecurityPolicySelfSubjectReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySelfSubjectReview


```php
function createPodSecurityPolicySelfSubjectReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySelfSubjectReview();
$pretty = 'pretty';

$result = $oapi->createPodSecurityPolicySelfSubjectReviewForAllNamespaces($body, $pretty);

```


### <a name="create_pod_security_policy_subject_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createPodSecurityPolicySubjectReviewForAllNamespaces") createPodSecurityPolicySubjectReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PodSecurityPolicySubjectReview


```php
function createPodSecurityPolicySubjectReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1PodSecurityPolicySubjectReview();
$pretty = 'pretty';

$result = $oapi->createPodSecurityPolicySubjectReviewForAllNamespaces($body, $pretty);

```


### <a name="list_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listPolicyForAllNamespaces") listPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Policy


```php
function listPolicyForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 158;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listPolicyForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_policy_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createPolicyForAllNamespaces") createPolicyForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Policy


```php
function createPolicyForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicy();
$pretty = 'pretty';

$result = $oapi->createPolicyForAllNamespaces($body, $pretty);

```


### <a name="list_policy_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listPolicyBindingForAllNamespaces") listPolicyBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind PolicyBinding


```php
function listPolicyBindingForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 249;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listPolicyBindingForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_policy_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createPolicyBindingForAllNamespaces") createPolicyBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a PolicyBinding


```php
function createPolicyBindingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterPolicyBinding();
$pretty = 'pretty';

$result = $oapi->createPolicyBindingForAllNamespaces($body, $pretty);

```


### <a name="list_project_request"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listProjectRequest") listProjectRequest

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind ProjectRequest


```php
function listProjectRequest(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 249;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listProjectRequest($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_project_request"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createProjectRequest") createProjectRequest

> *Tags:*  ``` Skips Authentication ``` 

> create a ProjectRequest


```php
function createProjectRequest(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ProjectRequest();
$pretty = 'pretty';

$result = $oapi->createProjectRequest($body, $pretty);

```


### <a name="list_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listProject") listProject

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Project


```php
function listProject(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 249;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listProject($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createProject") createProject

> *Tags:*  ``` Skips Authentication ``` 

> create a Project


```php
function createProject(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$pretty = 'pretty';

$result = $oapi->createProject($body, $pretty);

```


### <a name="get_read_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadProject") getReadProject

> *Tags:*  ``` Skips Authentication ``` 

> read the specified Project


```php
function getReadProject(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->getReadProject($name, $pretty);

```


### <a name="update_replace_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceProject") updateReplaceProject

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified Project


```php
function updateReplaceProject(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Namespace();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceProject($body, $name, $pretty);

```


### <a name="delete_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteProject") deleteProject

> *Tags:*  ``` Skips Authentication ``` 

> delete a Project


```php
function deleteProject(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->deleteProject($name, $pretty);

```


### <a name="patch_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchProject") patchProject

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified Project


```php
function patchProject(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the Project |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchProject($body, $name, $pretty);

```


### <a name="create_resource_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createResourceAccessReviewForAllNamespaces") createResourceAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a ResourceAccessReview


```php
function createResourceAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalResourceAccessReview();
$pretty = 'pretty';

$result = $oapi->createResourceAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="list_role_binding_restriction_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listRoleBindingRestrictionForAllNamespaces") listRoleBindingRestrictionForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind RoleBindingRestriction


```php
function listRoleBindingRestrictionForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 86;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listRoleBindingRestrictionForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_role_binding_restriction_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createRoleBindingRestrictionForAllNamespaces") createRoleBindingRestrictionForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBindingRestriction


```php
function createRoleBindingRestrictionForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1RoleBindingRestriction();
$pretty = 'pretty';

$result = $oapi->createRoleBindingRestrictionForAllNamespaces($body, $pretty);

```


### <a name="list_role_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listRoleBindingForAllNamespaces") listRoleBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind RoleBinding


```php
function listRoleBindingForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 86;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listRoleBindingForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_role_binding_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createRoleBindingForAllNamespaces") createRoleBindingForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a RoleBinding


```php
function createRoleBindingForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRoleBinding();
$pretty = 'pretty';

$result = $oapi->createRoleBindingForAllNamespaces($body, $pretty);

```


### <a name="list_role_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listRoleForAllNamespaces") listRoleForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list objects of kind Role


```php
function listRoleForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 44;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listRoleForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_role_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createRoleForAllNamespaces") createRoleForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Role


```php
function createRoleForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1ClusterRole();
$pretty = 'pretty';

$result = $oapi->createRoleForAllNamespaces($body, $pretty);

```


### <a name="list_route_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listRouteForAllNamespaces") listRouteForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Route


```php
function listRouteForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 44;
$watch = false;
$pretty = 'pretty';

$result = $oapi->listRouteForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_route_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createRouteForAllNamespaces") createRouteForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Route


```php
function createRouteForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Route();
$pretty = 'pretty';

$result = $oapi->createRouteForAllNamespaces($body, $pretty);

```


### <a name="create_self_subject_rules_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createSelfSubjectRulesReviewForAllNamespaces") createSelfSubjectRulesReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a SelfSubjectRulesReview


```php
function createSelfSubjectRulesReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SelfSubjectRulesReview();
$pretty = 'pretty';

$result = $oapi->createSelfSubjectRulesReviewForAllNamespaces($body, $pretty);

```


### <a name="create_subject_access_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createSubjectAccessReviewForAllNamespaces") createSubjectAccessReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectAccessReview


```php
function createSubjectAccessReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1LocalSubjectAccessReview();
$pretty = 'pretty';

$result = $oapi->createSubjectAccessReviewForAllNamespaces($body, $pretty);

```


### <a name="create_subject_rules_review_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createSubjectRulesReviewForAllNamespaces") createSubjectRulesReviewForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a SubjectRulesReview


```php
function createSubjectRulesReviewForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1SubjectRulesReview();
$pretty = 'pretty';

$result = $oapi->createSubjectRulesReviewForAllNamespaces($body, $pretty);

```


### <a name="list_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listTemplateForAllNamespaces") listTemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind Template


```php
function listTemplateForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 135;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listTemplateForAllNamespaces($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_template_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createTemplateForAllNamespaces") createTemplateForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> create a Template


```php
function createTemplateForAllNamespaces(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1Template();
$pretty = 'pretty';

$result = $oapi->createTemplateForAllNamespaces($body, $pretty);

```


### <a name="create_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createUserIdentityMapping") createUserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> create an UserIdentityMapping


```php
function createUserIdentityMapping(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1UserIdentityMapping();
$pretty = 'pretty';

$result = $oapi->createUserIdentityMapping($body, $pretty);

```


### <a name="get_read_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadUserIdentityMapping") getReadUserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> read the specified UserIdentityMapping


```php
function getReadUserIdentityMapping(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->getReadUserIdentityMapping($name, $pretty);

```


### <a name="update_replace_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceUserIdentityMapping") updateReplaceUserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified UserIdentityMapping


```php
function updateReplaceUserIdentityMapping(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1UserIdentityMapping();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceUserIdentityMapping($body, $name, $pretty);

```


### <a name="delete_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteUserIdentityMapping") deleteUserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> delete an UserIdentityMapping


```php
function deleteUserIdentityMapping(
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$pretty = 'pretty';

$result = $oapi->deleteUserIdentityMapping($name, $pretty);

```


### <a name="patch_user_identity_mapping"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchUserIdentityMapping") patchUserIdentityMapping

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified UserIdentityMapping


```php
function patchUserIdentityMapping(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the UserIdentityMapping |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchUserIdentityMapping($body, $name, $pretty);

```


### <a name="list_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.listUser") listUser

> *Tags:*  ``` Skips Authentication ``` 

> list or watch objects of kind User


```php
function listUser(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 227;
$watch = true;
$pretty = 'pretty';

$result = $oapi->listUser($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="create_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.createUser") createUser

> *Tags:*  ``` Skips Authentication ``` 

> create an User


```php
function createUser(
        $body,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1User();
$pretty = 'pretty';

$result = $oapi->createUser($body, $pretty);

```


### <a name="delete_collection_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteCollectionUser") deleteCollectionUser

> *Tags:*  ``` Skips Authentication ``` 

> delete collection of User


```php
function deleteCollectionUser(
        $fieldSelector = null,
        $labelSelector = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 227;
$watch = true;
$pretty = 'pretty';

$result = $oapi->deleteCollectionUser($fieldSelector, $labelSelector, $resourceVersion, $timeoutSeconds, $watch, $pretty);

```


### <a name="get_read_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getReadUser") getReadUser

> *Tags:*  ``` Skips Authentication ``` 

> read the specified User


```php
function getReadUser(
        $name,
        $exact = null,
        $export = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the User |
| exact |  ``` Optional ```  | Should the export be exact.  Exact export maintains cluster-specific fields like 'Namespace'. |
| export |  ``` Optional ```  | Should this value be exported.  Export strips fields that a user can not specify. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$name = 'name';
$exact = true;
$export = true;
$pretty = 'pretty';

$result = $oapi->getReadUser($name, $exact, $export, $pretty);

```


### <a name="update_replace_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.updateReplaceUser") updateReplaceUser

> *Tags:*  ``` Skips Authentication ``` 

> replace the specified User


```php
function updateReplaceUser(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the User |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1User();
$name = 'name';
$pretty = 'pretty';

$result = $oapi->updateReplaceUser($body, $name, $pretty);

```


### <a name="delete_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.deleteUser") deleteUser

> *Tags:*  ``` Skips Authentication ``` 

> delete an User


```php
function deleteUser(
        $body,
        $name,
        $gracePeriodSeconds = null,
        $orphanDependents = null,
        $propagationPolicy = null,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the User |
| gracePeriodSeconds |  ``` Optional ```  | The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. |
| orphanDependents |  ``` Optional ```  | Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. |
| propagationPolicy |  ``` Optional ```  | Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = new V1DeleteOptions();
$name = 'name';
$gracePeriodSeconds = 185;
$orphanDependents = true;
$propagationPolicy = 'propagationPolicy';
$pretty = 'pretty';

$result = $oapi->deleteUser($body, $name, $gracePeriodSeconds, $orphanDependents, $propagationPolicy, $pretty);

```


### <a name="patch_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.patchUser") patchUser

> *Tags:*  ``` Skips Authentication ``` 

> partially update the specified User


```php
function patchUser(
        $body,
        $name,
        $pretty = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| body |  ``` Required ```  | TODO: Add a parameter description |
| name |  ``` Required ```  | name of the User |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |



#### Example Usage

```php
$body = array('key' => 'value');
$name = 'name';
$pretty = 'pretty';

$result = $oapi->patchUser($body, $name, $pretty);

```


### <a name="get_watch_build_config_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchBuildConfigListForAllNamespaces") getWatchBuildConfigListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of BuildConfig


```php
function getWatchBuildConfigListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 185;
$watch = true;

$result = $oapi->getWatchBuildConfigListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_build_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchBuildListForAllNamespaces") getWatchBuildListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Build


```php
function getWatchBuildListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 22;
$watch = false;

$result = $oapi->getWatchBuildListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_network_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterNetworkList") getWatchClusterNetworkList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterNetwork


```php
function getWatchClusterNetworkList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 22;
$watch = false;

$result = $oapi->getWatchClusterNetworkList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_network"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterNetwork") getWatchClusterNetwork

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterNetwork


```php
function getWatchClusterNetwork(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterNetwork |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 22;
$watch = false;

$result = $oapi->getWatchClusterNetwork($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterPolicyList") getWatchClusterPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterPolicy


```php
function getWatchClusterPolicyList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 22;
$watch = false;

$result = $oapi->getWatchClusterPolicyList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterPolicy") getWatchClusterPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterPolicy


```php
function getWatchClusterPolicy(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicy |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 235;
$watch = true;

$result = $oapi->getWatchClusterPolicy($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_policy_binding_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterPolicyBindingList") getWatchClusterPolicyBindingList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterPolicyBinding


```php
function getWatchClusterPolicyBindingList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 235;
$watch = true;

$result = $oapi->getWatchClusterPolicyBindingList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterPolicyBinding") getWatchClusterPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterPolicyBinding


```php
function getWatchClusterPolicyBinding(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterPolicyBinding |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 235;
$watch = true;

$result = $oapi->getWatchClusterPolicyBinding($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_resource_quota_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterResourceQuotaList") getWatchClusterResourceQuotaList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ClusterResourceQuota


```php
function getWatchClusterResourceQuotaList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 235;
$watch = true;

$result = $oapi->getWatchClusterResourceQuotaList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_cluster_resource_quota"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchClusterResourceQuota") getWatchClusterResourceQuota

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ClusterResourceQuota


```php
function getWatchClusterResourceQuota(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ClusterResourceQuota |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 71;
$watch = false;

$result = $oapi->getWatchClusterResourceQuota($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_deployment_config_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchDeploymentConfigListForAllNamespaces") getWatchDeploymentConfigListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of DeploymentConfig


```php
function getWatchDeploymentConfigListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 71;
$watch = false;

$result = $oapi->getWatchDeploymentConfigListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_egress_network_policy_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchEgressNetworkPolicyListForAllNamespaces") getWatchEgressNetworkPolicyListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of EgressNetworkPolicy


```php
function getWatchEgressNetworkPolicyListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 71;
$watch = false;

$result = $oapi->getWatchEgressNetworkPolicyListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_group_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchGroupList") getWatchGroupList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Group


```php
function getWatchGroupList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 71;
$watch = false;

$result = $oapi->getWatchGroupList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_group"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchGroup") getWatchGroup

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Group


```php
function getWatchGroup(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Group |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 163;
$watch = true;

$result = $oapi->getWatchGroup($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_host_subnet_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchHostSubnetList") getWatchHostSubnetList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of HostSubnet


```php
function getWatchHostSubnetList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 163;
$watch = true;

$result = $oapi->getWatchHostSubnetList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_host_subnet"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchHostSubnet") getWatchHostSubnet

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind HostSubnet


```php
function getWatchHostSubnet(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the HostSubnet |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 163;
$watch = true;

$result = $oapi->getWatchHostSubnet($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_identity_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchIdentityList") getWatchIdentityList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Identity


```php
function getWatchIdentityList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 163;
$watch = true;

$result = $oapi->getWatchIdentityList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_identity"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchIdentity") getWatchIdentity

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Identity


```php
function getWatchIdentity(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Identity |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 121;
$watch = false;

$result = $oapi->getWatchIdentity($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchImageList") getWatchImageList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Image


```php
function getWatchImageList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 121;
$watch = false;

$result = $oapi->getWatchImageList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchImage") getWatchImage

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Image


```php
function getWatchImage(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Image |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 121;
$watch = false;

$result = $oapi->getWatchImage($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_image_stream_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchImageStreamListForAllNamespaces") getWatchImageStreamListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ImageStream


```php
function getWatchImageStreamListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 121;
$watch = false;

$result = $oapi->getWatchImageStreamListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_build_config_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedBuildConfigList") getWatchNamespacedBuildConfigList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of BuildConfig


```php
function getWatchNamespacedBuildConfigList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 213;
$watch = true;

$result = $oapi->getWatchNamespacedBuildConfigList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_build_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedBuildConfig") getWatchNamespacedBuildConfig

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind BuildConfig


```php
function getWatchNamespacedBuildConfig(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the BuildConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 213;
$watch = true;

$result = $oapi->getWatchNamespacedBuildConfig($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_build_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedBuildList") getWatchNamespacedBuildList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Build


```php
function getWatchNamespacedBuildList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 213;
$watch = true;

$result = $oapi->getWatchNamespacedBuildList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_build"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedBuild") getWatchNamespacedBuild

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Build


```php
function getWatchNamespacedBuild(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Build |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 213;
$watch = true;

$result = $oapi->getWatchNamespacedBuild($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_deployment_config_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedDeploymentConfigList") getWatchNamespacedDeploymentConfigList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of DeploymentConfig


```php
function getWatchNamespacedDeploymentConfigList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $oapi->getWatchNamespacedDeploymentConfigList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_deployment_config"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedDeploymentConfig") getWatchNamespacedDeploymentConfig

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind DeploymentConfig


```php
function getWatchNamespacedDeploymentConfig(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the DeploymentConfig |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $oapi->getWatchNamespacedDeploymentConfig($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_egress_network_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedEgressNetworkPolicyList") getWatchNamespacedEgressNetworkPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of EgressNetworkPolicy


```php
function getWatchNamespacedEgressNetworkPolicyList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $oapi->getWatchNamespacedEgressNetworkPolicyList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_egress_network_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedEgressNetworkPolicy") getWatchNamespacedEgressNetworkPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind EgressNetworkPolicy


```php
function getWatchNamespacedEgressNetworkPolicy(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the EgressNetworkPolicy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 49;
$watch = false;

$result = $oapi->getWatchNamespacedEgressNetworkPolicy($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_image_stream_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedImageStreamList") getWatchNamespacedImageStreamList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of ImageStream


```php
function getWatchNamespacedImageStreamList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 7;
$watch = false;

$result = $oapi->getWatchNamespacedImageStreamList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_image_stream"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedImageStream") getWatchNamespacedImageStream

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind ImageStream


```php
function getWatchNamespacedImageStream(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the ImageStream |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 7;
$watch = false;

$result = $oapi->getWatchNamespacedImageStream($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_policy_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedPolicyList") getWatchNamespacedPolicyList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Policy


```php
function getWatchNamespacedPolicyList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 7;
$watch = false;

$result = $oapi->getWatchNamespacedPolicyList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_policy"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedPolicy") getWatchNamespacedPolicy

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Policy


```php
function getWatchNamespacedPolicy(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Policy |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 99;
$watch = false;

$result = $oapi->getWatchNamespacedPolicy($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_policy_binding_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedPolicyBindingList") getWatchNamespacedPolicyBindingList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PolicyBinding


```php
function getWatchNamespacedPolicyBindingList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 99;
$watch = false;

$result = $oapi->getWatchNamespacedPolicyBindingList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_policy_binding"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedPolicyBinding") getWatchNamespacedPolicyBinding

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind PolicyBinding


```php
function getWatchNamespacedPolicyBinding(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the PolicyBinding |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 99;
$watch = false;

$result = $oapi->getWatchNamespacedPolicyBinding($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_role_binding_restriction_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedRoleBindingRestrictionList") getWatchNamespacedRoleBindingRestrictionList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of RoleBindingRestriction


```php
function getWatchNamespacedRoleBindingRestrictionList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 99;
$watch = false;

$result = $oapi->getWatchNamespacedRoleBindingRestrictionList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_role_binding_restriction"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedRoleBindingRestriction") getWatchNamespacedRoleBindingRestriction

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind RoleBindingRestriction


```php
function getWatchNamespacedRoleBindingRestriction(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the RoleBindingRestriction |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 57;
$watch = false;

$result = $oapi->getWatchNamespacedRoleBindingRestriction($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_route_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedRouteList") getWatchNamespacedRouteList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Route


```php
function getWatchNamespacedRouteList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 57;
$watch = false;

$result = $oapi->getWatchNamespacedRouteList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_route"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedRoute") getWatchNamespacedRoute

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Route


```php
function getWatchNamespacedRoute(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Route |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 57;
$watch = false;

$result = $oapi->getWatchNamespacedRoute($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_template_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedTemplateList") getWatchNamespacedTemplateList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Template


```php
function getWatchNamespacedTemplateList(
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 57;
$watch = false;

$result = $oapi->getWatchNamespacedTemplateList($mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_namespaced_template"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNamespacedTemplate") getWatchNamespacedTemplate

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Template


```php
function getWatchNamespacedTemplate(
        $name,
        $mnamespace,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Template |
| mnamespace |  ``` Required ```  | object name and auth scope, such as for teams and projects |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$mnamespace = 'namespace';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oapi->getWatchNamespacedTemplate($name, $mnamespace, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_net_namespace_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNetNamespaceList") getWatchNetNamespaceList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of NetNamespace


```php
function getWatchNetNamespaceList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oapi->getWatchNetNamespaceList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_net_namespace"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchNetNamespace") getWatchNetNamespace

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind NetNamespace


```php
function getWatchNetNamespace(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the NetNamespace |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oapi->getWatchNetNamespace($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_access_token_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthAccessTokenList") getWatchOAuthAccessTokenList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthAccessToken


```php
function getWatchOAuthAccessTokenList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 149;
$watch = true;

$result = $oapi->getWatchOAuthAccessTokenList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_access_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthAccessToken") getWatchOAuthAccessToken

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthAccessToken


```php
function getWatchOAuthAccessToken(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAccessToken |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 198;
$watch = true;

$result = $oapi->getWatchOAuthAccessToken($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_authorize_token_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthAuthorizeTokenList") getWatchOAuthAuthorizeTokenList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthAuthorizeToken


```php
function getWatchOAuthAuthorizeTokenList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 198;
$watch = true;

$result = $oapi->getWatchOAuthAuthorizeTokenList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_authorize_token"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthAuthorizeToken") getWatchOAuthAuthorizeToken

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthAuthorizeToken


```php
function getWatchOAuthAuthorizeToken(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthAuthorizeToken |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 198;
$watch = true;

$result = $oapi->getWatchOAuthAuthorizeToken($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_client_authorization_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthClientAuthorizationList") getWatchOAuthClientAuthorizationList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthClientAuthorization


```php
function getWatchOAuthClientAuthorizationList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 198;
$watch = true;

$result = $oapi->getWatchOAuthClientAuthorizationList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_client_authorization"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthClientAuthorization") getWatchOAuthClientAuthorization

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthClientAuthorization


```php
function getWatchOAuthClientAuthorization(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClientAuthorization |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 35;
$watch = false;

$result = $oapi->getWatchOAuthClientAuthorization($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_client_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthClientList") getWatchOAuthClientList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of OAuthClient


```php
function getWatchOAuthClientList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 35;
$watch = false;

$result = $oapi->getWatchOAuthClientList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_o_auth_client"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchOAuthClient") getWatchOAuthClient

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind OAuthClient


```php
function getWatchOAuthClient(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the OAuthClient |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 35;
$watch = false;

$result = $oapi->getWatchOAuthClient($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_policy_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchPolicyListForAllNamespaces") getWatchPolicyListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Policy


```php
function getWatchPolicyListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 35;
$watch = false;

$result = $oapi->getWatchPolicyListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_policy_binding_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchPolicyBindingListForAllNamespaces") getWatchPolicyBindingListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of PolicyBinding


```php
function getWatchPolicyBindingListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 35;
$watch = false;

$result = $oapi->getWatchPolicyBindingListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_project_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchProjectList") getWatchProjectList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Project


```php
function getWatchProjectList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 126;
$watch = false;

$result = $oapi->getWatchProjectList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_project"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchProject") getWatchProject

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind Project


```php
function getWatchProject(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the Project |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 126;
$watch = false;

$result = $oapi->getWatchProject($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_role_binding_restriction_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchRoleBindingRestrictionListForAllNamespaces") getWatchRoleBindingRestrictionListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of RoleBindingRestriction


```php
function getWatchRoleBindingRestrictionListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 126;
$watch = false;

$result = $oapi->getWatchRoleBindingRestrictionListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_route_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchRouteListForAllNamespaces") getWatchRouteListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Route


```php
function getWatchRouteListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 126;
$watch = false;

$result = $oapi->getWatchRouteListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_template_list_for_all_namespaces"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchTemplateListForAllNamespaces") getWatchTemplateListForAllNamespaces

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of Template


```php
function getWatchTemplateListForAllNamespaces(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 85;
$watch = false;

$result = $oapi->getWatchTemplateListForAllNamespaces($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user_list"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchUserList") getWatchUserList

> *Tags:*  ``` Skips Authentication ``` 

> watch individual changes to a list of User


```php
function getWatchUserList(
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 85;
$watch = false;

$result = $oapi->getWatchUserList($fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


### <a name="get_watch_user"></a>![Method: ](https://apidocs.io/img/method.png ".OapiController.getWatchUser") getWatchUser

> *Tags:*  ``` Skips Authentication ``` 

> watch changes to an object of kind User


```php
function getWatchUser(
        $name,
        $fieldSelector = null,
        $labelSelector = null,
        $pretty = null,
        $resourceVersion = null,
        $timeoutSeconds = null,
        $watch = null)
```

#### Parameters

| Parameter | Tags | Description |
|-----------|------|-------------|
| name |  ``` Required ```  | name of the User |
| fieldSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their fields. Defaults to everything. |
| labelSelector |  ``` Optional ```  | A selector to restrict the list of returned objects by their labels. Defaults to everything. |
| pretty |  ``` Optional ```  | If 'true', then the output is pretty printed. |
| resourceVersion |  ``` Optional ```  | When specified with a watch call, shows changes that occur after that particular version of a resource. Defaults to changes from the beginning of history. When specified for list: - if unset, then the result is returned from remote storage based on quorum-read flag; - if it's 0, then we simply return what we currently have in cache, no guarantee; - if set to non zero, then the result is at least as fresh as given rv. |
| timeoutSeconds |  ``` Optional ```  | Timeout for the list/watch call. |
| watch |  ``` Optional ```  | Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. |



#### Example Usage

```php
$name = 'name';
$fieldSelector = 'fieldSelector';
$labelSelector = 'labelSelector';
$pretty = 'pretty';
$resourceVersion = 'resourceVersion';
$timeoutSeconds = 85;
$watch = false;

$result = $oapi->getWatchUser($name, $fieldSelector, $labelSelector, $pretty, $resourceVersion, $timeoutSeconds, $watch);

```


[Back to List of Controllers](#list_of_controllers)

## <a name="osapi_controller"></a>![Class: ](https://apidocs.io/img/class.png ".OsapiController") OsapiController

### Get singleton instance

The singleton instance of the ``` OsapiController ``` class can be accessed from the API Client.

```php
$osapi = $client->getOsapi();
```

### <a name="get_osapi_version"></a>![Method: ](https://apidocs.io/img/method.png ".OsapiController.getOsapiVersion") getOsapiVersion

> *Tags:*  ``` Skips Authentication ``` 

> list supported server API versions


```php
function getOsapiVersion()
```

#### Example Usage

```php

$osapi->getOsapiVersion();

```

#### Errors

| Error Code | Error Description |
|------------|-------------------|
| 0 | Default Response. |



[Back to List of Controllers](#list_of_controllers)

## <a name="version_controller"></a>![Class: ](https://apidocs.io/img/class.png ".VersionController") VersionController

### Get singleton instance

The singleton instance of the ``` VersionController ``` class can be accessed from the API Client.

```php
$version = $client->getVersion();
```

### <a name="get_code_version"></a>![Method: ](https://apidocs.io/img/method.png ".VersionController.getCodeVersion") getCodeVersion

> *Tags:*  ``` Skips Authentication ``` 

> get the code version


```php
function getCodeVersion()
```

#### Example Usage

```php

$result = $version->getCodeVersion();

```


[Back to List of Controllers](#list_of_controllers)



